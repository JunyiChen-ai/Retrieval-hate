# CLAUDE.md

## 项目
这是一个 **hateful video detection** 研究项目:把 RGCL / RA-HMD(原本用于 hateful meme detection)适配到仇恨视频检测。

## GPU / 资源
- **所有 GPU / 计算任务必须通过 SLURM 提交**(登录节点即计算节点,非 SLURM 的计算进程会被回收)。
- 环境:`conda activate HateVideo`。
- 提交:`sbatch scripts/slurm/<name>.sbatch`,**不要设 `--time`**。每用户上限:16 CPU / 128 GB / 2 GPU。
- 作业初始通常是 `PENDING (JobHeldUser)` → **等自动放行即可**,不要强行释放。

## 云端探针(Modal)
- 所有探针 / 探索性 / 分诊实验(probing/triage)**一律上 Modal 云端跑**(`scripts/cloud/modal_probe_runner.py`,conda `HateVideo`,profile `jehc223`)——不要为这些排本机 SLURM 队列。
- **正式验证也可上云**(用户裁定 2026-07-31),前提是**同表同硬件**:候选与其配对基线的**全部 seeds** 都在同一云端 GPU 型号 + 同一镜像上跑,且预注册中钉死 GPU 型号/镜像;跨硬件漂移实测 ~1.4pt(seed 噪声级),故云端与本地数字仍**永不**混入同一张对比表——历史本地基线数不能直接当云端表的基线,必须同硬件重跑(head 级重训 ~52s,通常很便宜)。已冻结锚定本地的预注册(如 C02 A0 v9)按冻结执行,本裁定前瞻适用。
- 数据边界:**大数据文件(原始视频等)不上云**(用户裁定 2026-07-31 重申;`modal_probe_runner.py` 的硬拦截不得移除);允许上传派生特征缓存(`.pt` 浮点向量)与标签 JSON。⇒ 需要读原始视频的任务(编码器级抽取、LoRA 训练)仍只能本地。
- 客户端依赖:squid 代理下 modal 需要 `python-socks[asyncio]` + `aiohttp-socks`(缺了会分别断 gRPC 控制面 / volume 上传面)——重建环境必装。
- **并行探针**:不同方向的 probing 可以(且应该)在云端同时多路并行——探针之间互不排他,不必等一个方向出结果再开下一个。
- **过线即排队**:某方向探针一过预注册杀开关,立即走正式流程(prereg→评审→冻结哈希)并**马上提交进本机 SLURM 队列排队**——排队时间与后续文书/其他探针并行,不留空转。仪式不减(单提纪律、独立评审照旧),但各阶段之间不留等待。
- **快杀**:kill 判定一出即关方向、立即换下一个候选;侦察(零 GPU forensic recon)对队列里每个候选提前做,保证弹药池永远有下一发。
- **看队列分流**(用户裁定 2026-07-31):提交任务前先 `squeue` 看队列。队列拥挤(前面压着多个 pending)→ 轻量任务上云;**长耗时训练/推理仍尽量优先本地**。豁免:原始视频等大文件任务无论如何不上云(除非用户查完 DUA 另行解禁);云端正式验证须满足上文"同表同硬件"前提。Modal Starter 每月 $30 免费额度(2026-07-31 pricing 页实证,滚存未验证,按"每月 $30 用完即止"理解);选型对比与年成本盈亏点(~210 A100-h/年以下 Modal 全包最便宜)见 `refine-logs/CLOUD_PROVIDER_RECON.md`。

## 实验流程(用户裁定 2026-08-05,取代旧的重仪式)
- **仪式与"跑废成本"成正比。** GPU 级 / 重跑昂贵的实验才配完整预注册 + 独立评审;**CPU 级便宜实验(≤ ~1 小时)最多一轮评审**,且只有"会产出错误判决 / 触碰测试集"的缺陷才能拦路——"干净跑会 HALT"类缺陷直接接受(代价 = 重跑一次,比评审便宜)。
- **四条硬红线永不砍:** ①零测试集接触;②判决规则在看到结果前冻结;③盲性(设计/实现期间不得计算候选指标);④正式运行单次提交。
- **禁止文档自洽性迭代**:文档/注释里的陈旧数字发现即修,永不作为阻塞项,永不单开评审轮。**修完不复审**——作者自测证据(如合成数据端到端演练输出)即可放行。
- **证据够硬直接裁决**,不把批准问题甩回用户;用户回合只用于真正的范围变更与资源投入。
- **提交前的最便宜检查是提交本身**(sbatch 一次 1 秒的失败顶半天代码评审;C06 的 conda 环境缺失即此教训)。
- **汇报语言**:标准技术词汇直说,不发明黑话、不打比喻、不起外号;先说跑了什么、出了什么数、对决策意味着什么。

## 权责声明(最重要)
- **主对话 = 你和我讨论、决策、汇报**,主对话本身**不执行任何杂活**。
- **一切杂活**(写代码、数据处理、提交与监控 SLURM、调试、跑实验)**一律交给 subagent 或 dynamic workflow 去做**。
- **主对话调用 subagent 只能用 Opus 5**(`model: opus`,即 `claude-opus-5`),**不得降级也不得升级**到其他模型。
