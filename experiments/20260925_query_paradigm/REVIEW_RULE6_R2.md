# Rule-6 code review — 20260925_query_paradigm, revision 2 (anchored two-state answer model, learned chain with its own learning rate)

**Verdict: PASS** (no must-fix items; two should-fix items that are operational and do not change the default path's numbers; three minor notes).

Reviewed 2026-09-25 against README section 9 as committed in `d84bace` (working tree clean in `experiments/20260925_query_paradigm/`; `git diff f63709c d84bace -- experiments/20260925_query_paradigm/` is the reviewed change set: `train.py`, `qtree.py`, `ctree.py`, `cpolicy.py` unchanged but on the call chain, `data.py`, `model.py`, `search.py`, `reeval.py`, `score_decomposition.py`, `launch/run_diag.sh`). Scope per rule 6 only: mechanism enters forward/loss/score, leakage, alignment, hparam/checkpoint chain, shared evaluator. No GPU job or training was run; CPU checks on synthetic data and on the real answer caches / a few real feature files (scripts in the session scratchpad, not kept). Revision-0/1 findings (`REVIEW_RULE6.md`, `REVIEW_RULE6_R1.md`) were re-checked where revision 2 touches them; nothing there is undone.

The default path reviewed (`train.py:67-76` DEFAULTS): prior "chain" with `ctree.Chain` (`chain` "learned"), `answer_model` "anchored" (`qtree.fit_anchored`, n_state 2, length_term false), objective "tree", fusion "tree", order "eig", categories all five, g_head true, text_sources ["bert"]. Diagnostic switches (objective "posterior"/"mil", g_head false, chain "hazard", extra text_sources, n_state 3, length_term true) were checked only for not touching the default path.

## CPU checks run

1. **New `ctree` versus the f63709c `ctree` on identical inputs** (learned `Chain` with random parameters, `Forest` of 7 videos T ∈ {1, 2, 3, 5, 7, 9, 11} padded to width 13 with junk in the padded cells, random answer log-likelihoods on half the nodes): `log_evidence` (l1, l0, lo1, lo0) and `marginals` (p_G, node marginals, per-second posterior) are bit-identical (max |new − old| = 0) in float64 and in float32. The `logA_for` generalisation changes nothing for the learned chain.
2. **Brute-force enumeration** of P(y | x) ∝ exp(g·any(y) + Σ s_t y_t) π0(y_1) Π A(y_{t−1}, y_t) with node answer factors (state 0 if any(y) = 0, else 1 + z_n) over all 2^T labellings: `log_evidence` and `marginals` match for the learned `Chain` (max abs error 1.6e-14) and for `HazardChain` with its per-video A = [[1−1/T, 1/T], [1/T, 1−1/T]], π0 uniform (7.6e-15). Gradient of the default training loss −mean(log P(Y | x)) − mean(log P(o | Y, x) / n_obs) with respect to s, g, `chain.trans`, `chain.init` against autograd through the brute-force expression: 4.9e-9 / 8.0e-9 / 6.1e-8 / 1.8e-8 (float32 chain parameters, float64 everything else); gradient into padded cells exactly 0.
3. **`qtree.fit_anchored`** on 40 synthetic videos (5 % `None` answers): the row counts equal "all non-None nodes of negative videos" and "root [0, T) of positive videos" exactly; softmax(theta) equals the closed form (count + 1) / (N + 4) to 5.9e-8 (one Laplace pseudo-count per level is what the regulariser `− Σ log_softmax(th)` in `qtree.py:164` implements); omega is all zero with length_term false; shapes (5, 2, 4); `init_theta(·, n_state=2)` gives the same distributions to 5.9e-8. `AnswerModel(n_state=2).loglik` returns columns [s0, s0, s1] and ignores the length; `policy.Asker` table columns 1 and 0 coincide and `Asker.loglik` equals `am.loglik`; categories [0] gives a 4-outcome table.
4. **`fit_anchored` on the real train answers** (`data/vlm_tree/*/answers_qwen7b_mod5.shard*.jsonl`, `hc.load_fixed_cohort` train ids, exactly as `train.py:166-172` builds them): HateMM 26,649 state-0 rows and 297 state-1 rows (298 positive train videos; `hate_video_272`, T = 2, has no root answer), HateClipSeg 3,292 / 219 (219 positives). Closed form matched to 7e-9 / 1.1e-8; 0.6 s / 0.0 s. Hate-category P(level | state): HateMM s0 [.790, .001, .095, .113], s1 [.043, .003, .030, .924]; HCS s0 [.718, .001, .102, .180], s1 [.139, .004, .188, .668]. Control: a fit on train + val + test would use 36,761 / 425 (HateMM) and 4,921 / 343 (HCS) rows and differ by .013 / .038 in probability, so the train-only restriction is real and not vacuous.
5. **One replicated training step of the default path** (the exact statements of `train.py:210-285` on `PriorNet(DEFAULTS)`, an anchored `AnswerModel` with `requires_grad_(False)`, `ctree.Chain`, the two-group Adam, 3 synthetic videos T ∈ {5, 9, 7} padded to 9 with cached answers on every queryable node through `data.observed`, CMAL with λ = 1): the optimiser has 2 groups — 27 network tensors at lr 4e-4 and exactly `chain.trans`, `chain.init` at lr_answer; `am.theta` is not in any group and has requires_grad false; `A3` carries no gradient; `loss_g` and `loss_ans` equal the brute-force −log P(Y | x) and −log P(o | Y, x) / n_obs (diff < 1e-6); after `backward()` `am.theta.grad` is None, the chain gradients are non-zero, 26/27 network tensors have non-zero gradients (the 27th is the unused `g0`); after `opt.step()` `am.theta` is bit-identical to the fitted value, the chain and the network moved, `g0` did not; `am.state_dict()` = {"theta"} equal to the fitted theta.
6. **Arms through the real code paths**: (b) `cpolicy.run_batch(fusion="flat")` asks the same nodes and gives the same p_G as fusion "tree"; `scores[0]` equals the zero-answer chain posterior; `scores[1]` equals logit(p0) + [ll(s = 1) − ll(s = 0)] on the asked node's seconds. (d) `order="bfs"` asks the first 4 queryable nodes in breadth-first order. (e) `qtree.TreeBatch.log_evidence` runs with the anchored two-state model and equals the `init_theta` start to 1.9e-6. (c) `ctree.up` with A3 = 0 gives the same l1, l0 as `log_evidence` (0.0). `HazardChain` runs through `run_batch` with VOI recorded. `run_batch` tree scores after 1..4 answers versus enumeration: 2.8e-13.
7. **`data.Store` old (f63709c) versus new** with the default `text_sources` on 4 real videos per corpus (2 train, 1 val, 1 test): `at` arrays, `T`, `W` identical, shapes (T, 896), `n_missing_text` 0 = 0. **`model.PriorNet` old versus new** with the default cfg and seed 234: all shared parameters identical, the only extra key is `g0`, `fc_a.in_features` 896 = 896 = `qdata.A_IN`, forward outputs identical, torch RNG state after construction identical (so the network initialisation of a trial is unchanged).

## Checked and found correct (no action)

1. **`qtree.fit_anchored` (`qtree.py:132-170`) uses training answers only.** Its input `ta` (`train.py:166`) and the length statistics `loglen` (`train.py:168`) are built from `ids["train"]`; state 0 = every non-None node of a label-0 video (`:144-145`), state 1 = the node with a == 0 and b == T of a label-1 video (`:146-147`), all other positive-video nodes skipped (`:148-149`); labels are Python ints 0/1 (`hc.load_fixed_cohort`), so `label == 0` is exact. One Laplace pseudo-count per level (`:164`); L-BFGS reaches the closed form (check 3, 4). Return shapes (N_CAT, 2, N_LEV) match `AnswerModel(n_state=2)` (`train.py:176-177`), whose `loglik` maps two tables to the three answer states as [0, 0, 1] (`qtree.py:106-107`), the same mapping as `policy.Asker` (`policy.py:37-38`) used by `cpolicy.run_batch` (`cpolicy.py:44`). Nothing from val/test is read in the fit (check 4 control).
2. **The answer model is frozen in the default path.** `am.requires_grad_(False)` (`train.py:178-179`); the small-parameter group is `[] + chain.parameters()` when anchored (`:187`), so the optimiser holds the network at `lr` and only `chain.trans`, `chain.init` at `lr_answer` (`:188-189`, check 5); no code assigns to `am` afterwards; `best["am"]` (`:300`) and `model.pth["am"]` (`:308`) therefore hold the fitted theta (check 5). The chain is in the optimiser and receives the gradient of both loss terms (check 2, 5).
3. **The default loss is README 9.3.** objective "tree" → `direct` true, `s_tree = s`, `g_tree = g` (`train.py:216-220`); the else-branch (`:244-257`) writes all cached train answers' log-likelihoods from the frozen model into `A3` at the video-offset node ids, `n_obs` = answered nodes per video, `ctree.log_evidence` gives l1/l0/lo1/lo0, `loss_ans = −mean(where(label, lo1, lo0) / n_obs)`, `loss_g = −mean(where(label, l1, l0))` (`:258`), `total = loss_g + loss_ans + λ·CMAL` with `p_video.detach()` into CMAL (`:269, 277-282`). Verified against enumeration (check 5). Gradients reach the network and the chain only (check 5).
4. **Evaluation path with the changed `ctree.up`.** `up` now takes the transition from `chain.logA_for(forest)` (`ctree.py:116`); `Chain.logA_for` returns the (2, 2) `logA()` (`:43-44`) so `per_video` is false and every downstream expression (`_merge` with `la = logA[None]`, `logW0` with `logA[0, 0]`; `:101, 152`) reduces to the previous code — bit-identical (check 1). `_DoubleChain` (`:180-188`) defers to the wrapped chain's `logA_for` and detaches/doubles it, as before. `HazardChain` (`:47-59`) returns a (B, 2, 2) table indexed by `forest.node_video` at the merge (`:141`) and by video at `logW0` (`:152`); exact against enumeration (check 2); has no parameters, so with it the optimiser has one group and `chain.state_dict()` is empty (saved/restored without error). `cpolicy.run_batch` and its VOI passes call `ctree.marginals` unchanged (`cpolicy.py:52, 87`) and are exact (check 6).
5. **Each README-9 arm changes one thing.** (a) `categories [0]` → `AnswerModel.cats` (training A3) and `Asker.cats` (EIG tables, test log-likelihoods) both use category 0 only; `fit_anchored` still fits all five, the unused four are never read. (b) `fusion "flat"` → only the score list changes (`cpolicy.py:58`: logit of the zero-answer chain posterior `p0` + mean over asked nodes covering t of `ll[2] − ll[0]`, `:105-107`, which under the [0, 0, 1] mapping is log P(o | s = 1) − log P(o | s = 0)); question choice, p_G, training untouched (check 6); the validation checkpoint criterion reads the same list (`train.py:290-291`). (c) `objective "label"` → `direct` stays true, `A3 = 0`, `loss_ans = 0` (`train.py:221-243`), the label term is identical to `log_evidence`'s l1/l0 (check 6); the chain still learns from the label term; answers enter only through `Evaluator`. (d) `order "bfs"` → `cpolicy.py:73-75` picks the first remaining queryable node (breadth-first id order), evaluation and checkpoint only (check 6). (e) `prior "independent"` → `use_chain` false, `chain = None`, `small = []` (optimiser has one group), `qtree.TreeBatch` path with BCE on g (`train.py:262-268`, revision-0 structure), per-video `policy.run_video` at evaluation, no VOI rule (`record_voi=chain is not None`, `rules` list); the anchored two-state model plugs in through the same `loglik` columns (check 6). (f) `answer_model "joint"` → `theta0 = init_theta(ta, 2)` (5, 2, 4), omega None, `am.parameters()` (= theta, 40 numbers) join the chain in the `lr_answer` group (`train.py:174-175, 187`); its start equals the anchored fit to 7e-9 on the real caches (check 4), so (f) isolates "learned versus fixed".
6. **`search.py`.** Only the `lr_answer` range changed (`search.py:35`, [1e-4, 1e-1] log); lr, λ_cma, dropout unchanged; the four keys are README 9's space. Study creation (`load_if_exists`), `budget.json`, `n_done`, trial directories, the objective (`s["test"]` = fixed primary budget, `search.py:92-100`) and the validation-selected record are unchanged since R1. Arms go through `--extra-config` with the DEFAULTS-key assert (`:64-68`); `categories`, `fusion`, `objective`, `order`, `prior`, `answer_model` are all DEFAULTS keys.
7. **Leakage.** Training reads labels of `ids["train"]` only (`qdata.TrainSet`, `fit_anchored`); checkpoint selection = validation fixed-8 EIG through `Evaluator.run` + `gt["val"]` (`train.py:290-301`); the adaptive thresholds are calibrated on validation (`:344`); test labels appear only in the analysis fields `silent_group`, `test_mean_calls_pos/neg` (`:352-353, 362-366`). The `posterior` diagnostic runs the policy on `ids["train"]` (`:207-209`). `score_decomposition.py` is a rule-10 analysis script over written test score files, not imported by `train.py`/`search.py`. Val/test answers are read only as the simulated oracle. Every reported test number still goes through `hc.run_evaluator` → `scripts/reproduction_baselines/eval_baseline_scores.py` (`:327-332`).
8. **`data.Store` and `PriorNet` for the default.** `Store(corpus, ids, ["bert"])` concatenates [audio, bert] exactly as the old `text_source="bert"` did (`data.py:61-74`); identical arrays on real videos (check 7). `PriorNet.fc_a` input = `A_IN + 768·(len(text_sources) − 1)` = `A_IN` for one source (`model.py:69`); the new `g0` parameter is zeros (no RNG), unused when `g_head` is true (`:89`), gets no gradient and is skipped by Adam; initialisation, forward and RNG stream are identical to the old module (check 7).

## Should-fix (operational; do not change the default path's numbers if README 9 is followed)

1. **`launch/run_search.sh:7` defaults `out_root` to `runs/20260925_query_paradigm`, the revision-0 study root.** README 9 says the revision-2 search writes to `runs/20260925_query_paradigm_r2/`; the launcher only does so if the third argument is given. If it is omitted, `search.py` `load_if_exists` resumes the revision-0 studies: HateClipSeg has 20 COMPLETE trials → `remaining = 0`, nothing runs and `study_summary.json` is rewritten; HateMM has 10 COMPLETE (+ 1 stale RUNNING) → 10 revision-2 trials would be appended to the revision-0 study (different code, `lr_answer` absent from the first 10 trials), and the "best trial" would mix revisions. Fix: change the default to `runs/20260925_query_paradigm_r2` (or make the argument required), and start the r2 searches with the root written out explicitly.
2. **`search.py` does not check that an existing study's parameter set matches `sample()`.** Resuming a study whose trials lack `lr_answer` or have a different `lr_answer` range does not raise (Optuna drops the parameter from the TPE intersection space). A one-line assert that every existing COMPLETE trial has exactly `{"lr", "lamda_cma", "dropout", "lr_answer"}` with the current bounds (`t.distributions`) would make the mix-up in item 1 loud. Not needed if item 1 is applied and a fresh root is used.

## Minor (not conclusion-changing, no action required)

1. `reeval.py:46` `model.load_state_dict(ck["model"])` is strict; revision-0/1 checkpoints have no `g0` key and will fail to load. `reeval.py` is not on the search path (R1 minor 3 still applies to it as well).
2. Carried: `search.py:105-107` counts FAIL trials toward the trial budget; `hate_video_272` (HateMM train, T = 2) has no queryable node and contributes nothing to the anchored fit or the answer term (handled at `train.py:248`); no val/test video is that short.
3. `fit_anchored` fits `theta` for all five categories regardless of `cfg["categories"]`; arm (a) ignores the four unused tables consistently in training and evaluation, so this is bookkeeping only.
