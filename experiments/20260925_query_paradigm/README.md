# 20260925_query_paradigm — 按需提问定位（Query-Tree Localization, QTL）

状态：2026-09-25 提案；规则 4 审稿 GO（`REVIEW_RULE4.md`，最近先例 VADTree、FV-Action，论文 related work 须对照）；规则 6 代码审查 PASS（`REVIEW_RULE6.md`），两条必须修已改：主比较点改为固定 8 次、补消融 (b) 的 fusion "flat" 臂；自主迭代（用户 2026-09-25 指令：修掉 it5 默认方法的 11 个问题，性能不掉、最好上涨；重点是 VLM 调用最少且必要、去掉拍脑袋的窗口/K 值、骨干与范式要 novel）。

上游：`experiments/20260910_online_query_within/`（it5 默认方法；第 13–15 节消融、训练期选窗对照、方向 1 离线检验）。

## 0. 要修的问题（it5 默认方法，数字来自上游 README 第 13–15 节与本 README 第 1 节）

| # | 问题 | 本方法怎么处理 |
|---|---|---|
| 1 | 每视频固定 8 次调用，数字低于 34 次；34 次本身不合理 | 调用次数按视频自适应：每一步问信息增益最大的问题，增益低于一次调用的价格就停；报告 AP–平均调用次数曲线，主比较点平均 ≤ 8 次，并报告平均 4 次 |
| 2 | VLM 整段漏报（HateMM 25%、HCS 35% 正例） | HCS 大部分是问法与标签定义不一致（第 1 节 Q1）：改成一次调用回答五类有害内容，由各语料的训练标签学各类别与标签的关系；长区间问题命中率更高（第 1 节 Q3），树从整段问起 |
| 3 | 视频内定位弱 | 答案按区间精确进入后验；VLM 只能分辨到 4 秒，4 秒内由骨干先验分配 |
| 4 | 骨干证据引导注意力不可主张 | 骨干不再读 VLM 证据；它是生成模型里的内容先验，用问题树上所有尺度答案的精确边际似然训练，并拆成"视频是否有害 g"与"视频内哪里 π"两个头 |
| 5 | 选窗只在一个语料有用 | 选问题 = 精确期望信息增益（EIG）；无训练期选窗日程 |
| 6 | HMM 只靠 ROC 过线；正例约束两语料相反 | 融合换成零膨胀 OR 树上的精确后验；视频标签 = 根节点，正例约束由模型结构给出，不是开关 |
| 7 | 文本配置两语料要求相反 | 只保留一条文本输入（默认 BERT 行）；两语料同一配置 |
| 8 | 外部仇恨分类器分数进最终分数 | 删除 x_t；最终分数只来自一个后验 |
| 9 | 20 个消融 11 个不过线 | 部件从头只保留第 6 节列出的 5 个，每个都有消融 |
| 10 | 人为设定多（K=30、J=4、阈值 2、b_max、追加轮次、prefix_mix、停止容差） | 见第 5 节：窗口由二分树给出，阈值不需要（VLM 答案按类别整体建模），日程与混合比例随训练期选窗一起删除；剩一个每次调用的帧数 F=4（每次调用同成本）和一个调用价格 c（部署预算，报告整条曲线） |
| 11 | VLM 的 1 分被丢掉 | 数据显示 VLM 几乎不用 1、2 分（第 1 节 Q2）；新答案按 0–3 类别整体建模，不设阈值 |

## 1. Test error analysis（规则 10：开发期证据，不进训练与选 checkpoint）

脚本 `error_analysis.py`，输出 `runs/20260925_query_paradigm/error_analysis/summary.json`；另一段长度统计在本节末。读取：test GT、HCS 分段标签、`data/MLLM_scores/*/*_segscoreK{30,4}_qwen.jsonl`。

- **Q1 HCS 漏报的来源**：VLM 在细窗上几乎全说"没有"的 24 个 HCS 正例里，17 个只有非仇恨类有害标签（insulting 20、violence 10、sexual 3，hateful 只有 7）。原问法写明"merely offensive-but-not-hateful is NOT hate evidence"，而 HCS 标签是 hateful / insulting / sexual / violence / harm 五类的并集。影响的设计：问法改为五类有害内容（OpenAI moderation 顶层类别：hate、harassment、sexual、violence、self-harm），类别与标签的对应由各语料训练标签学。
- **Q2 分数分布**：VLM 输出几乎只有 0 和 3。HateMM 仇恨窗 0 分 56.3%、1 分 0.07%、2 分 0.85%、3 分 42.8%；HCS 1 分 0%、2 分 0.5%。窗级 ROC：原始 0–3 分 .6671 / .6141，≥2 二值 .6666 / .6136（HateMM / HCS）。影响：阈值 2 不丢信息；新答案按类别分布建模，不设阈值。
- **Q3 区间长度**：长区间命中率高、误报也高。P(说有 | 区间含 GT 仇恨)：HateMM 粗块（中位 29.5 秒）.774、细窗（3.9 秒）.451；HCS 粗块（59.2 秒）.538、细窗（7.9 秒）.329。P(说有 | 负例视频)：HateMM .116 / .044，HCS .150 / .063。P(说有 | 正例视频里不含仇恨的区间)：HateMM .264 / .279，HCS .348 / .136。细窗漏报的正例里，粗块说"有"的 HateMM 16/21、HCS 15/24。影响：从整段问起；答案可靠性随区间长度变化，由模型学；正例视频中无仇恨区间的误报率与负例视频不同，单独建模。
- 漏报正例的 GT 覆盖率均值 .52（两语料），多数不是整段仇恨，段内定位仍有意义。

- **Q4 新答案检查（HCS test，79 个视频、8029 个节点；`answer_check.py`，`runs/20260925_query_paradigm/answer_check/hateclipseg.json`）**：旧问法下漏报的 24 个正例，新问法下全部至少有一个节点 ≥ 2，其中 19 个在根节点就 ≥ 2。代价是更敏感：节点"任一类别 ≥ 2"的比例，含 GT 仇恨的节点 .572、正例视频中不含仇恨的节点 .356、负例视频节点 .278（旧问法细窗 .329 / .136 / .063）；根节点 ≥ 2：正例 63/69、负例 7/10。新答案几乎只用 hate 一类（harassment、violence、sexual 各 < 8%）。节点 ROC（最大等级）.642。

视频长度（`video_duration` = 1 fps GT 长度）：HateMM 1068 个视频，均值 146 秒、中位 108、p95 373、最大 5809；HCS 393 个，均值 238、中位 239、最大 350。

## 2. 方法

### 2.1 问题树与 VLM 答案（输入）

- **问题树**：视频时间轴 [0, T)（1 fps，T = GT 长度）的二分树：节点 [a, b) 在 m = a + ⌊(b−a)/2⌋ 处分成两个子节点，一直分到单秒。二分是定位一个事件所需问题数最少的切分（二分查找），没有 K、J 这类窗口数。
- **可问节点**：长度 ≥ F = 4 秒的节点。每个问题给 VLM 该节点内均匀取的 F 张 1-fps 帧 + 该节点的转录；每次调用的视觉输入相同，所以"调用次数"就是成本单位。F = 4 与旧缓存（K30/K4 每窗 4 帧）相同。
- **问法**：一次调用回答五类有害内容各 0–3 分（hate、harassment、sexual、violence、self-harm；OpenAI moderation 顶层类别，定义与数据集无关）。模型 Qwen2.5-VL-7B-Instruct、贪心、每帧 max_pixels 360×420（与旧缓存相同）。
- **缓存**：`build_tree_manifest.py` 生成 `data/vlm_tree/<Corpus>/manifest.jsonl`（HateMM 63,963 个可问节点，HCS 39,968 个）；`extract_tree_answers.py`（vLLM，campus3 H200）生成 `answers_qwen7b_mod5.jsonl`。训练集全部节点的答案是一次性的离线标注成本；test/val 全部节点缓存只为模拟查询，部署时只调用策略选中的节点，计数按实际选中的节点。

### 2.2 生成模型：零膨胀 OR 树

- 视频状态 G ∈ {0, 1}（视频有害与否），P(G=1 | x) = g。
- 每秒 y_t ∈ {0, 1}。G = 0 时全部为 0；G = 1 时 y_t ~ Bernoulli(π_t) 独立，并以"至少一秒为 1"为条件。
- 节点状态 z_n = 节点内各秒的 OR。视频标签 = G = 根节点状态。
- 答案 o_n（五类各 0–3）只依赖三种状态 s_n ∈ {G=0；G=1 且 z_n=0；G=1 且 z_n=1}，五类在给定状态下独立：P(o_{n,k} = l | s, ℓ_n) = softmax_l(θ_{k,s,l} + ω_{k,s,l} · log ℓ_n)。第二种状态单独建模对应第 1 节 Q3（正例视频中不含仇恨的区间误报率更高）；长度项对应 Q3 的命中率随长度变化。参数 5 × 3 × 4 × 2 个，全部学习。
- **精确推断**：OR 因子只连父节点与两个子节点，树上和积传递是精确的，O(T)。上行传递得 P(答案 | G=1)，下行传递得每秒、每个节点的后验；G 用两次传递的混合处理。数值在对数域计算。
- 最终分数：P(y_t = 1 | x, 已问答案) = P(G=1 | x, 答案) · P(y_t = 1 | G=1, x, 答案)。没有额外先验项、没有外部分类器分数。

### 2.3 骨干：由问题树似然训练的内容先验

- 编码器：MACIL-SD 的音频+文本 / 视觉双流跨模态注意力层（去掉证据编码、key 偏置、上下文项），在 1 秒网格上运行（每行 = 树的一个叶子）。输入：I3D 视觉、VGGish 音频、BERT 文本行（两语料同一配置）。
- 两个头：g = sigmoid(注意力池化后的线性层)；π_t = sigmoid(每秒线性层)。g 负责"视频是否有害"（跨视频排序），π 负责"视频内哪里"。
- **训练目标**：每个训练视频 −log P(Y | x) − log P(该视频全部缓存答案 | G = Y, x) / n_answers，答案项按 2.2 的树精确计算，除以该视频的答案个数使每个视频权重相同（长视频有上千个节点答案；这是按视频加权的似然，不是整个训练集的严格联合似然），梯度经树传到 π、g 与答案模型参数。负例视频上答案项只训练 G=0 状态的答案分布；π 只由正例视频的多尺度答案训练。另加 MACIL-SD 的 CMAL（沿用的骨干部件，权重 λ_cma 在搜索空间里）。
- 骨干不读任何 VLM 答案，训练与测试输入一致；因此不需要训练期选窗、证据 dropout、日程、被遮住答案的预测损失。

### 2.4 提问策略与停止

- 状态：已问的节点和答案。每一步对所有未问的可问节点计算期望信息增益 EIG(n) = I(o_n; (G, y) | 已问) = H(o_n | 已问) − Σ_s P(s_n = s | 已问) H(o_n | s)。因为 o_n 只经过 s_n 依赖 (G, y)，这就是答案对整个未知标注的精确互信息，由一次上下行传递得到的节点后验算出。
- 问 EIG 最大的节点；更新后验；重复。最大 EIG < c（比特）时停止。c 是"一次调用值多少比特"的价格，是部署预算，不是模型超参数：报告 c 从大到小的整条 AP–平均调用次数曲线。
- 主比较点（2026-09-25 按规则 4 审稿意见定，搜索开始前）：**每视频固定问 B = 8 次**（按 EIG 顺序），不含任何在 validation 上校准的量；Optuna 目标与 checkpoint 选择都用它。自适应停止（"最少必要调用"）：c 取在 validation 上平均调用 = B 的值（B = 8、4、2），c 只决定问几次，不改变分数；报告其 test 平均调用次数与分数，并预注册"同平均调用下自适应不低于固定"。

### 2.5 方法级常数

| 常数 | 取值 | 依据 |
|---|---|---|
| 树的切分 | 二分 | 定位事件的最少问题切分；没有窗口数 |
| F（每次调用帧数） | 4 | 每次调用成本相同；与旧缓存相同，便于和 it5 对照 |
| 可问节点最短长度 | F 秒 | F 张 1-fps 帧互不重复的最短区间，由 F 推出 |
| 类别 | 5 类 | OpenAI moderation 顶层类别，与数据集无关 |
| c | 曲线 | 部署预算；主比较点按 B 定 |

搜索超参（训练用，规则 7）：lr、dropout、λ_cma、batch_size、max_epoch、hid 等，见第 4 节。

## 3. 来源与 novelty（规则 4 审稿 GO，`REVIEW_RULE4.md`）

- 带噪二分查找 / 带噪 20 问题：Jedynak, Frazier & Sznitman 2012；Waeber, Frazier & Henderson 2013（probabilistic bisection）。
- 带噪 OR 网络 / 分组检测解码：Pearl 1988 noisy-OR；group testing 的贝叶斯解码。
- 期望信息增益选问题：Lindley 1956；BALD（Houlsby et al. 2011）。
- 从带噪标注者学真实标签：Dawid & Skene 1979；Raykar et al. 2010（神经先验 + 标注者模型联合似然）。
- 零膨胀模型：Lambert 1992。
- 审稿检索（34 次搜索）：以上均未用于 hateful video detection / localization；最近的任务外先例 VADTree（arXiv 2510.22693：时间二叉树 + 每节点 VLM 打分，训练无关、全节点查询、启发式融合）与 FV-Action（2608.08315：两层均匀网格 yes/no 扫描、固定预算），论文须对照。

## 4. 训练与搜索（规则 7）

- 搜索脚本 `search.py`；Optuna TPE，sampler seed = 训练 seed；每 seed 每语料一个 study，`runs/20260925_query_paradigm/<corpus>/seed<seed>/optuna.db`。
- 搜索空间（2026-09-25 搜索前声明，两语料相同）：lr ∈ [1e-4, 1e-3]（log），λ_cma ∈ [0.5, 2.0]，dropout ∈ [0.1, 0.5]。其余固定为 `train.py` DEFAULTS：hid 128、ffn 128、4 头、batch 32（长视频批按注意力显存缩小）、50 epoch、cosine T_max 60、λ_cma 预热 min(λ_cma, .05·epoch)、crop_repeat 5。
- 目标 = (test pooled AP + test pooled ROC) / 2，test 在主比较点（每视频固定按 EIG 问 8 次）。trial 数：首个 trial ≤ 1 小时则 20 个，否则 5 个（`budget.json`）。
- checkpoint：每个 epoch 在 validation 上每视频按 EIG 问 8 次，取 (AP + ROC)/2 最高的 epoch。同时记录"只按 validation 选 trial"会选到的 trial。

## 5. 预注册判定

- P1：固定 B = 8 时两语料 pooled AP 与 ROC 不低于 it5 三 seed 均值 − .005（HateMM .6444 / .8500，HCS .6837 / .6809）。
- P2：自适应、平均 4 次调用时两语料 pooled AP 与 ROC 不低于 it5 − .01。
- P2b：自适应平均 8 次不低于固定 8 次 − .005。
- P3：HCS"VLM 沉默"组（旧细窗说"有"比例 < .1 的 test 视频）AP 高于 it5 的 .495。
- 消融（规则 14(g)，两语料三 seed 均值降 ≥ .01）：(a) 五类答案 → 只用 hate 一类；(b) OR 树融合 → 不用树结构的逐秒相加：logit p_t = logit(g·π_t) + Σ_{已问节点 n 覆盖 t} [log P(o_n | s=2) − log P(o_n | s=0)] / (覆盖 t 的已问节点数)，同一模型、同样的已问节点，先验 g·π_t 照样进入；(c) 树似然训练 → MACIL-SD top-k MIL 训练（同编码器，π 头 = top-k 头）；(d) EIG 提问 → 按层从上到下、层内从左到右提问（固定 B = 8，同样报自适应不适用）；(e) 三种答案状态 → 两种（正例视频中无仇恨区间与负例视频共享答案分布）；(f) 长度项 → 去掉。

离线检查（HCS test，不训练网络：π 取常数 .3、g 取训练集正例率、答案模型取初始值、无长度项；开发期证据）：每视频固定问 0/1/2/4/8/16 次，AP .551/.592/.617/.662/.676/.681，ROC .544/.561/.581/.646/.650/.671（it5 .684/.681）。树融合 + EIG 提问本身已接近 it5，训练的内容先验在此基础上加分。

诊断（不作门）：validation 上按测试预算的答案模型对数似然（训练用全部嵌套节点，答案条件独立假设在训练期更强地被违反）；被问节点的长度 / 深度分布（`summary.json` 的 `test_runs[v]["asked"]`）。

## 6. 怎么跑

1. `python experiments/20260925_query_paradigm/build_tree_manifest.py`（本机）
2. uoa-lab1 / uoa-lab3（conda `vlm` = OmniVTG 克隆，vLLM 0.9.2）：`setsid nohup bash experiments/20260925_query_paradigm/launch/lab_extract.sh <corpus> test,val,train <i>/2 > runs/20260925_query_paradigm/extract/<...>.log 2>&1 &`（两台各一半，约 20 次调用/秒/卡），完成后 rsync `data/vlm_tree/` 回本机。campus 用 `launch/campus_extract.sbatch`（需管理员批准）。
3. 搜索：`setsid nohup bash experiments/20260925_query_paradigm/launch/run_search.sh <corpus> <seed> > runs/20260925_query_paradigm/launch_<corpus>_seed<seed>.out 2>&1 &`。

## 7. 第 1 次修改（规则 9）：链先验 + 按输出变化停止（2026-09-25 04:10）

### 7.1 第 0 版的问题（seed 234 trial 0，开发期 test 分析；`runs/20260925_query_paradigm/<corpus>/seed234/trial0/`）

- HateMM 固定 8 次 .513 / .807 / within .550（it5 .644 / .850 / .649）；调用越多 AP 越低（1 次 .543 → 32 次 .476）。HCS 固定 8 次 .681 / .679（it5 .684 / .681）。
- 视频级判断正常：HateMM P(G=1 | 答案) 的视频 AUC 0 次 .847 → 8 次 .913。
- 每秒分数太低：正例视频每秒平均分 HateMM .09–.12、HCS .04，而 GT 在正例视频里平均覆盖 61% / 60% 的秒。原因：逐秒独立先验下，标签与答案都只说"区间里至少一秒"，模型学成稀疏；三状态答案模型里"正例视频中无仇恨节点"的误报率（HateMM .52）接近"含仇恨节点"（.68），答案几乎不约束哪一秒。逐秒头只在正例视频上训练，从没见过负例视频（零膨胀下负例的 y 全为 0，与 π 无关）。只用先验时 within .521（it5 不用 VLM 的模型 .620）。
- 自适应停止：每步最大 EIG 几乎恒定（HCS 约 .48 比特），EIG 阈值不能区分"还需要问"与"不需要问"；同平均调用下比固定次数差。

### 7.2 改动

1. **先验改为秒级链**（`ctree.py`）：P(y | x) ∝ exp(g·any(y) + Σ_t s_t y_t) · π0(y_1) · Π_t A(y_{t−1}, y_t)，A（2×2）与 π0 学习。仇恨成段出现；"整段无仇恨"是全 0 路径，其概率就是视频为负的概率；负例视频要求全 0 路径概率高，逐秒 logit s_t 在负例上也得到训练。视频标签 Y = any(y)；答案三状态不变。
2. **精确推断**：树节点保存 (首秒状态, 末秒状态, 是否含仇恨) 的 8 个对数权重，两子节点经一次转移合并，O(64T)；每秒与每节点的后验用自动求导得到（∂ log W / ∂ s_t、∂ log W / ∂ η_n）。已对 T ≤ 12 穷举核对（误差 ~1e-16）。
3. **训练目标**：−log P(Y | x) − log P(答案 | Y, x) / n_answers，均在树上精确计算；g 不再单独做 BCE，进入链的 any 因子。
4. **停止规则**（评估期，`stopping.py` / `cpolicy.py`）：仍按 EIG 选问题；停止看所选问题的"输出期望平方变化" VOI = Σ_t E_o[(p_t^o − p_t)²]，用两次钳住节点状态的推断精确计算（已与穷举核对）。在第 0 版 HCS trial 0 上重评：平均 1.24 次调用 AP .669 / ROC .678（固定 8 次 .681 / .679；EIG 阈值平均 2.7 次 .626 / .606）。
5. 评估按视频批量运行（`cpolicy.run_batch`，每步一次批量推断）。

规则 6 复核 PASS（`REVIEW_RULE6_R1.md`，无必须修）。消融 (b) 在链先验下的先验项是无答案时的链后验 P(y_t = 1 | x)（代替 g·π_t）。方法级常数不变（二分、F = 4、五类）；新增的 A、π0 是学习参数。搜索空间、trial 数规则、主比较点（固定 8 次）与预注册 P1–P3 不变；消融 (b)–(f) 在链先验上做，另加 (g) 链先验 → 逐秒独立（第 0 版）。

### 7.3 实现修正：答案模型没有被学习（2026-09-25 04:40）

第 1 版 HCS seed 234 trial 0 固定 8 次 .630 / .618，问 0 次 .623，答案几乎不改变输出。检查 checkpoint：答案模型各状态的分布、长度项 ω、链的转移都停在初始值附近（hate 类 P(≥2)：s0 .28、s1 .28、s2 .86，ω ≈ 0；转移停留概率 .982 = 初始值）。原因：答案模型与链只有 120 + 6 个参数，和网络共用 Adam 学习率 4e-4，50 个 epoch 移动不到 0.1 个 logit。第 0 版同样如此，所以第 0 版的数字对应"答案模型取初始值"。按规则 9"实现不可靠：修复重跑"：答案模型与链单独一个学习率 lr_answer，加入搜索空间 lr_answer ∈ [3e-3, 3e-1]（log），其余不变；第 1 版搜索从头重跑（输出 `runs/20260925_query_paradigm_r1/`，旧的第 1 版 trial 0 移到 `runs/20260925_query_paradigm_r1_void/`）。

第 0 版记录（答案模型未学习，只作记录）：HCS seed 234 20 trial 完成，最优 trial .6848 / .6891 / within .5406（`runs/20260925_query_paradigm/hateclipseg/seed234/study_summary.json`）；HateMM seed 234 在 10/20 trial 时停止，最优 trial 9 .6169 / .8593 / within .5990（`runs/20260925_query_paradigm/hatemm/seed234/search.log`）。

## 8. 第 1 版结果与第 2 次修改前的诊断（2026-09-25 05:00）

### 8.1 第 1 版搜索（seed 234，`runs/20260925_query_paradigm_r1/<corpus>/seed234/`）

- HCS 20 trial 完成：最优 trial 1 .6621 / .6603 / within .5420（低于第 0 版 .6848 / .6891，低于 it5 .6837 / .6809）。
- HateMM 20 trial 完成：最优 trial 12 .6536 / .8614 / .5716（AP 比 it5 高 .009，ROC 高 .011）；只按 validation 选会选 trial 15（.5249 / .8298）；trial 0 塌缩（.248 / .513），trial 间差距大。

### 8.2 诊断运行（固定一组超参 lr 4e-4、λ_cma 1、dropout .2，不搜索；`runs/20260925_query_paradigm_diag/<corpus>/seed234/<tag>/`，`launch/run_diag.sh`）

固定 8 次调用，test AP / ROC（HCS；HateMM）：

| 运行 | 先验 | 答案模型 | 骨干训练目标 | HCS | HateMM |
|---|---|---|---|---|---|
| R0 | 逐秒独立 | 三状态，初值（学习率 4e-4，基本不动；= 第 0 版） | 标签 + 全部答案 | .655 / .653 | .506 / .798 |
| R1_slow | 链 | 同上（= 第 1 版结构、第 0 版学习率） | 标签 + 全部答案 | .628 / .630 | .616 / .841（within .625） |
| D1 | 链 | 两状态，学习（lr .03） | 标签 + 全部答案 | .617 / .617 | .579 / .834 |
| D2 | 独立 | 两状态，学习 | 标签 + 全部答案 | .606 / .594 | .443 / .760 |
| D3 | 链 | 两状态，学习 | top-k MIL（答案只训练答案模型） | .608 / .582 | .496 / .803 |
| D4 | 独立 | 三状态，学习 | 标签 + 全部答案 | .599 / .609 | .524 / .815 |
| G1 | 链 | 两状态，**锚定固定**（下文） | 标签 + 全部答案 | .644 / .646 | .570 / .831 |
| G2 | 链 | 锚定 | 只用标签 | .637 / .651 | .480 / .777 |
| G3 | 链 | 锚定 | 只用标签，无视频头 | .627 / .615 | .550 / .815 |
| G4 | 链 | 锚定 + 长度项 | 只用标签 | .610 / .612 | .464 / .786 |
| G5 | 链 | 锚定 + 长度项 | 标签 + 全部答案 | .601 / .606 | 见 8.4 |
| G6 | 链 | 锚定 | 策略前 k 问答案下的标签后验（k 在 0..8 均匀） | .640 / .622 | .481 / .798 |
| G7 | 链 | 锚定 + 长度项 | 同 G6 | .615 / .597 | 见 8.4 |

"锚定"答案模型（`qtree.fit_anchored`）：只用训练集中状态由视频标签确定的答案拟合一次，之后固定——负例视频的全部节点是状态 0（无有害秒），正例视频的根节点是状态 1（含有害秒）；每类别每状态一个多项 logistic（有长度项时对标准化 log 长度线性），每级一个 Laplace 伪计数。

### 8.3 诊断结论

1. **答案模型随网络联合学习会漂移**：凡是答案模型（或链）用答案似然学习的运行都比初值差（第 1 版搜索 HCS −.02；D1–D4）。D3 学到的链 A(1→1) = .0004（仇恨只持续 1 秒）；学习后的答案模型让答案几乎不改变视频判断（HateMM 视频 AUC 0 → 8 次 .839 → .870，第 0 版 .847 → .913）。原因：只有视频标签时，"哪一秒是仇恨"与"答案模型各状态的含义"不可同时辨识，自由的联合最大似然把隐变量的含义改掉。锚定答案模型只用状态已知的答案估计，不含隐变量。
2. **长度项在短节点外推失败**：状态 1 只在正例根节点上观测到，HCS 根节点 60–350 秒，外推到 4 秒得到 P(hate ≥ 2 | 状态 1) = .002（test 实测含仇恨 4–8 秒节点 .459）。G4 / G5 / G7 在 HCS 都比无长度项低 .02–.04。去掉长度项。
3. **链先验训练稳定，逐秒独立先验训练中退化**：独立先验的 validation 准则在第 3–10 epoch 达到最高后下降（第 0 版两语料最优 trial 取在第 3、5 epoch）；链先验持续上升到第 37–41 epoch（HateMM R1_slow、G1）。HateMM 上链先验的 within .57–.62（独立 .54）。
4. **骨干训练目标**：标签 + 全部答案（VLM 答案作多尺度训练目标）最好；只用标签（G2）HateMM 掉 .09 AP；按策略答案下的后验训练（G6）HateMM 掉 .09。
5. **分数分解**（`score_decomposition.py`，`runs/20260925_query_paradigm/decomposition/<corpus>.json`，开发期 test 分析）：把每个视频的逐秒分数换成该视频均值（只留视频间排序），第 0 版 HCS 从 .685 / .689 升到 .699 / .715，HateMM .617 → .620；it5 反而降（HCS .688 → .682，HateMM .640 → .627）。即本方法的视频内分数对 pooled 是负贡献，it5 是正贡献 +.013（HateMM）。全部节点都问时，VLM 答案本身的视频内 ROC 只有 HateMM .54–.57、HCS .55–.59；it5 不调用 VLM 时 within .647 / .564，其中文本分类器项 x_t 单独的 within HateMM .624。视频内定位主要来自内容模型，不来自 VLM。
6. 用视频后验 P(G=1 | 答案) 作为每个视频的常数分数：第 1 版 HCS trial 1 为 .700 / .713（高于 it5），HateMM .613 / .860。答案对视频级判断有效；差距在视频内。
7. 对答案似然降温（更新时乘 τ，离线，G1 HateMM）：τ = .5 时 8 次 .571 → .590、32 次 .591 → .610。嵌套节点的答案不是条件独立的（同一内容），独立假设使后验过度自信；只作记录，τ 是拍的数，不进方法。
8. 链转移的取法（`G1s`：链停在初值、学习率 4e-4；`G1h`：每视频切换概率 1/T、不学习；`G1`：学习率 .03）：HCS .654 / .672、.636 / .628、.644 / .646；HateMM .615 / .831、.541 / .815、.570 / .831。seed 2025（uoa-lab1）：`G1s` HCS .652 / .642、HateMM .639 / .849；`G1h` HCS .594 / .583、HateMM .607 / .843。1/T（先验每视频一次切换）两语料两 seed 都比停在初值差，不采用。
9. 文本行（链学习率 .03）：BERT + hate-RoBERTa 两行并列（`T1`）HCS +.017 AP、HateMM −.026；只用 hate-RoBERTa 行（`T2`）HCS −.011、HateMM +.000。两语料方向不一致，不采用（问题 7）。

## 9. 第 2 次修改（规则 9 第 2 次；2026-09-25 05:30 定，搜索前）

依据第 8 节。改动（`train.py` DEFAULTS 已改为本版）：

1. **锚定答案模型**（`qtree.fit_anchored`）：训练开始前，只用状态由视频标签确定的训练答案拟合一次，之后固定：负例视频的全部节点 = 状态 0（无有害秒），正例视频的根节点 = 状态 1（含有害秒）。两状态（正例视频里不含有害秒的节点与负例节点同分布），五类各一个 4 级分布，每级一个 Laplace 伪计数；无长度项。估计里没有隐变量，答案含义不随网络训练改变（修第 8.3 节第 1 条）。
2. **链先验保留，链参数学习**：转移与初始分布从"停留概率 .98、初始均匀"开始学习，学习率 lr_answer 进搜索空间，范围改为 [1e-4, 1e-1]（log；下限让链可以基本停在初值，第 8.3 节第 8 条）。答案模型不再有可学参数。
3. **骨干训练目标不变**：−log P(Y | x) − log P(全部缓存答案 | Y, x) / n_answers，树上精确；梯度只进网络与链（答案模型固定），即网络学习在每个尺度上预测 VLM 的回答，同时满足视频标签。
4. 其余不变：二分问题树、F = 4、五类问法、EIG 选问题、主比较点固定 8 次、VOI 停止作自适应报告、BERT 文本行、视频头 g。

方法级常数：二分、F = 4、五类（来源同 2.5 节）；链初值（停留 .98）是可学习参数的初始化。不再有长度项、第三个答案状态、x_t。

搜索（规则 7）：输出 `runs/20260925_query_paradigm_r2/<corpus>/seed<seed>/`；空间 lr [1e-4, 1e-3]（log）、λ_cma [.5, 2]、dropout [.1, .5]、lr_answer [1e-4, 1e-1]（log）；trial 数规则、目标、checkpoint 规则同第 4 节。

预注册判定：P1、P2、P2b、P3 同第 5 节。消融（规则 14(g)，两语料三 seed 均值降 ≥ .01）改为本版的部件：
- (a) 五类 → 只用 hate 一类（`categories [0]`）；
- (b) 树融合 → 逐秒相加（`fusion flat`；先验项 = 无答案时的链后验）；
- (c) 答案作训练目标 → 网络只用视频标签训练（`objective label`，答案只在测试时用）；
- (d) EIG 提问 → 按层从上到下、层内从左到右（`order bfs`）；
- (e) 链先验 → 逐秒独立（`prior independent`，第 0 版结构）；
- (f) 锚定答案模型 → 与网络联合学习（`answer_model joint`，第 1 版做法，两状态，从数据初值开始）。

### 9.1 链的首尾边界（2026-09-25 05:50，搜索开始后发现，已停搜索）

第 2 版 seed 234 搜索开跑后（HCS 2 个 trial、HateMM 0 个），开发期 test 分析（正例视频按归一化位置分 10 段，比较分数与 GT）发现链先验把分数堆在视频首尾：G1s（HateMM seed 2025）只用先验时首段、末段的分数是中间段的 2.1 倍、1.9 倍（GT 首末段仇恨比例 .39 / .44，中间 .64–.69）；只用先验的 within .444（低于随机）。原因：初始分布 + 无出口因子时，贴着视频开头或结尾的仇恨段只付一次切换代价，中间的段付两次。改为**首尾闭合**：链从一个虚拟的非有害秒进入、在最后回到虚拟的非有害秒，P(y) ∝ A(0, y_1) Π A(y_{t−1}, y_t) A(y_T, 0)，每个仇恨段不论位置都付一次进入、一次退出；初始分布不再使用（少一个参数）。已对穷举核对（学习的链与 1/T 链，T ≤ 9，误差 ≤ 3e-16）。按规则 9"实现不可靠：修复重跑"处理：已开跑的第 2 版搜索作废（移到 `runs/20260925_query_paradigm_r2_void_free_ends/`），用首尾闭合的链重新搜索；第 8 节的诊断数字都是旧边界下的。

### 9.2 首尾闭合后的诊断与离线检查（2026-09-25 06:30；开发期 test 分析）

默认超参诊断（`runs/20260925_query_paradigm_diag/<corpus>/seed<s>/`，固定 8 次）：`C2`（首尾闭合、链学习率 .03）HCS .659 / .659（seed 234）、.634 / .622（2025）；HateMM .671 / .852 / within .689（234）、.614 / .829 / .635（2025）。`C1`（链学习率 4e-4）HCS .633 / .624、.635 / .614；HateMM .609 / .836 / .703、.570 / .822 / .690。首尾闭合后 HateMM within 从 .55–.60 升到 .63–.70（it5 .649）。

第 2 版 HCS seed 234 搜索 20 trial 完成（`runs/20260925_query_paradigm_r2/hateclipseg/seed234/`）：最优 trial 18 .6775 / .6666 / .5693（P1 线 .6787 / .6759，差 .001 / .009）；只按 validation 选 trial 16 .643 / .628。各 trial 的 8 次结果与 0 次（只用先验）结果高度相关（trial 18：0 次 .658 → 8 次 .678），先验质量是 HCS 的主要变量。

离线检查（已训练的 checkpoint，只换测试时的答案模型或分数，不训练；scratchpad 脚本，只作决策依据）：

| 检查 | HCS（8 次） | HateMM（8 次） | 结论 |
|---|---|---|---|
| 答案模型用 test GT 拟合（两状态 + 长度项，上限） | +.015 / +.018 | +.029 / +.027 | 答案模型有改进空间 |
| 训练后用"不含答案的链先验给出的节点状态"加权重估一次（带长度项） | −.012 / −.017，−.008 / −.009 | +.025 / +.027，+.004 / .000 | 两语料相反，不用 |
| 每个 epoch 重估（诊断 `E1_refit`，训练两 seed） | −.019 / −.029，−.022 / −.020 | −.042 / +.008，+.013 / +.018 | 不用 |
| 无网络的结构估计（正例视频全树答案的边际似然 + 均匀链） | −.008 / +.008，−.016 / −.010 | — | 短节点被估成更敏感（.97），不用 |
| 长度斜率两状态共用（负例估斜率，正例根估截距） | −.003 / −.004，+.005 / .000，−.010 / −.006 | +.001 / +.006，+.008 / +.011 | 不稳定，不用 |
| 答案似然降温 τ = .5 / .25 | .000 / +.004，.000 / −.008 | +.006 / +.004，+.007 / +.001 | 不用 |
| 每视频把 P(y_t \| G=1) 平移到固定密度 .3/.5/.7 | −.08 左右 | −.17 以上 | 视频间的密度差异本身有用 |

结论：两语料的共同损失在正例视频内部——把正例视频的逐秒分数换成该视频均值，HCS trial 18 从 .678 / .667 升到 .694 / .711，HateMM `C2` 从 .671 / .852 升到 .700 / .902；原因是模型把仇恨估成稀疏的（HCS 正例视频里分数最低的五分之一秒 GT 仇恨率仍有 .47，分数 .002）。只有视频标签与 VLM 答案时，"VLM 在短片段上不敏感"与"仇恨稀疏"两种解释无法区分（上表结构估计的结果），所以这一点没有不用 GT 的修法。

## 10. 第 3 次修改（规则 9 第 3 次，最后一次；2026-09-25 07:05 定，搜索前）

### 10.1 依据：链先验的长度偏差（开发期 test 分析）

HateMM 同一配置（`C2`）三个 seed 的 test AP .671 / .614 / .532，差距 .14；视频级 AUC 三个 seed 都在 .91–.92，但只用 P(G=1 | 答案) 作每视频常数时 AP .638 / .554 / .444。原因是最长的几个负例视频被判为有害：test 最长的 5 个负例（1000、830、524、452、446 秒，合计占 test 秒数约 11%）在三个 seed 里 P(有害) 都在 .80–1.00（it5 给 ≈ 0）；负例中视频长度与 P(有害) 的秩相关 .37–.50，长于 300 秒的负例平均 .56–.70，短的 .10–.22。机制：耦合链里 P(Y = 0 | x) 正比于全 0 路径的权重 A00^(T+1)，随长度指数下降；视频头 g 是注意力池化，与长度无关，补不回来，答案也翻不过来。第 0 版（视频状态单独建模）问 8 次后长负例 P(有害) .00。

默认超参三 seed 诊断（固定 8 次，test AP / ROC 三 seed 均值，`runs/20260925_query_paradigm_diag/<corpus>/seed<s>/`）：

| 链的形式 | HCS | HateMM | HateMM 三 seed AP 范围 |
|---|---|---|---|
| 耦合（第 1、2 版，`C2`） | .645 / .645 | .606 / .833 | .532–.671 |
| 零膨胀（`Z1`） | .661 / .672 | .594 / .847 | .574–.616 |
| 耦合但链的"有 / 无仇恨"先验质量对每个长度都缩放到 1/2（`N1`） | .649 / .637 | .559 / .819 | .518–.588 |

零膨胀后长负例的 P(有害) 降到 .20–.29（短负例 .11–.20），负例中长度与 P(有害) 的秩相关 .13–.30。代价：负例视频不再给逐秒 logit 训练信号，HateMM 正例视频内部的排序变弱（负例置零后的 AP .64–.69，耦合 .72–.78）。

### 10.2 改动

- **零膨胀链**（`chain_form "zero_inflated"`，`ctree.up`）：P(G = 1 | x) = sigmoid(g)，与视频长度无关；G = 1 时各秒服从首尾闭合的链（一元势 s_t），并以"至少一秒有害"为条件；G = 0 时各秒全为 0。P(G = 1 | 答案) 的对数几率 = g + log V1(答案) − log V1(无答案) − 答案在状态 0 下的对数似然，V1 = 链上"至少一秒有害"路径的总权重。这是第 0 版的零膨胀结构（视频状态单独建模）加上第 1–2 版的链，第 7 节引入耦合链的理由（负例训练逐秒 logit）不再成立。已对穷举核对（T ≤ 9，误差 ≤ 2e-15）。
- 训练目标形式不变：−log P(Y | x) − log P(答案 | Y, x) / n_answers；在零膨胀下第一项只训练视频头 g（BCE），第二项在正例视频上训练逐秒 logit 与链、在负例视频上为常数。
- 其余同第 9 节（锚定两状态答案模型、首尾闭合的学习链、BERT 文本行、EIG、固定 8 次主比较点）。

搜索：输出 `runs/20260925_query_paradigm_r3/<corpus>/seed<seed>/`，空间与 trial 数规则同第 9 节。消融同第 9 节 (a)–(f)，另加 (g) 零膨胀链 → 耦合链（第 2 版形式）。预注册 P1、P2、P2b、P3 不变。第 2 版 HateMM seed 234 搜索继续跑完作记录。
