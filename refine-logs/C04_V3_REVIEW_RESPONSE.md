# C04 V3 Response to V2 Fresh Review

**Prior verdict:** `REVISE (0C/4H/3I)`  
**Requested action:** fresh design-only review

- H1: all three fallback controls now freeze producer state/content, split-local
  fitting, expected direction and conjunctive small/full numeric/CI gates.
- H2: only infrastructure failures HALT; valid-output semantic reliability
  failure scientifically kills C04 and cannot trigger a prompt/model/threshold
  rewrite fork.
- H3: seed 0 is fixed for small/full OOF; all strong arms receive the identical
  inner grid/tie/budget; five outer-fold inner scores produce one final tuple per
  arm for full-train/native-dev, reused by paired seeds.
- H4: LOWER_ORDER now uses a dense, deterministic, block-covered 256x3598
  SHA-Rademacher map; actual canonical payload hashes remain mandatory
  pre-execution code-review blockers.
- I1: `N=0` is eight black frames plus failure state, with no retry.
- I2: every tuple/slot/role/noise/remove/fallback control has an explicit
  full-bank `+.020` dual-metric and paired-CI gate on both datasets.
- I3: a pre-submit ledger and internal allocation-start watchdog stop before the
  cap without SLURM `--time`; partial output cannot seal or retry.

All previously accepted V2 clauses and the user contract remain unchanged. Even
a V3 design GO authorizes only prospective implementation/code/resource review.

