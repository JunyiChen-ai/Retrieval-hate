# C04 V2 Response to Independent Review

**Status:** `FROZEN / SUBMITTED FOR FRESH REVIEW`  
**Prior verdict:** `REVISE_USER_AMENDMENT_REQUIRED (2C/5H/4I)`

## Critical

- **C1 closed:** user approved the bounded C04-only matched local-teacher route.
  The old summary+density+cue proxy is explicitly nonbinding. The serial route
  is 200+200 -> seal -> small PASS -> fresh GO -> conditional complete 744/579,
  with `<=2` then `<=8 GPU-hour` caps.
- **C2 closed:** every slot has confidence `0..4`, a four-state reliability
  variable, exact A/B agreement, primary-output rule and deterministic
  conflict/missing sentinel. No row is dropped/reweighted. Coverage,
  disagreement and fallback corruption reports are mandatory.

## High

- **H1 closed:** exact `q4`, all 14 lower-order subsets, epsilon-safe zero flag,
  CONCAT_ALL4_MLP, retained independent4, slotwise/tuple shuffle, role
  permutation, noise and remove controls are binding with <=1% parameter and
  <=5% FLOP matching.
- **H2 closed:** historical discarded-head P4 is not the comparator. The
  retained independent-four student and flexible concat MLP directly test
  P4/C5/LEAF-style structured/label-free KD alternatives.
- **H3 closed:** DIRECT and STUDENT are both nested train-OOF; retention is
  computed only within that same arena. Native-only dev is separate and has no
  teacher.
- **H4 closed:** the novelty table now compares RAMF, LEAF, TFN/LMF, DR-HM,
  Intent Projection and P4/C5/KD at the component and mechanism level. The claim
  depends on beating strong controls, not absence of an exact conjunction.
- **H5 closed:** deterministic outer5/inner4 folds and paired complete
  adaptation+head seeds `0/1/2` are frozen.

## Important

- **I1 closed:** Prompt A is primary; B is an independent challenger. Enum
  agreement is exact, proposition agreement is normalized cosine `>=0.80`, and
  canonical conflict resolution is deterministic.
- **I2 closed:** `eps=1e-12`, q includes an explicit all-zero flag, and the
  SHA256-stream Fisher–Yates signed-orthogonal algorithm is exact. Actual
  materialized map payload SHA256 values are mandatory code-review blockers,
  appropriately absent before implementation authority.
- **I3 closed:** the exact label-blind sample cannot be redrawn. After seal it
  must pass class-count/prevalence guards. The small and total GPU ledgers are
  separate serial caps, not competing estimates.
- **I4 closed:** tuple shuffle, every slot shuffle, cyclic role permutation,
  covariance-matched noise, factor removal and fallback corruption have exact
  split-local semantics.

## Requested verdict

Review the V2 files for a design-only `GO`, `REVISE`, or `KILL`. Even `GO` must
not be interpreted as implementation, teacher, GPU, SLURM or test authority.

