# TARGET FINDINGS

- **Iteration 0 · 2026-07-10 · target not met:** all prior MLLM classification routes fail the fixed two-dataset, three-seed, same-protocol **+3.0 acc and +3.0 macro-F1** standard; the recurring barrier is semantic signal that is redundant/orthogonal to vote correctness or competes with the retrieval-memory readout, so the next hypothesis must prove train-only conditional information gain and survive remove/shuffle mechanism controls before test.
- **Iteration 1 · 2026-07-10 · Gate 1 READY, target still not met:** four-round independent refinement advanced SSR-MemRGCL from 6.6 to 9.03/10. The frozen method uses only video-level binary gold labels; reliability-filtered MLLM stance–target–mechanism outputs are weak train-only pseudo-signals that select paired-seed directed memory constraints, with no test-time MLLM. Proceed to B0/B1 only; common-family reliability, conditional information, dual-metric oracle headroom and exact-null feasibility remain unproven fast-fail gates.
- **Iteration 0 supervision amendment · 2026-07-10:** the video-level binary label is the project's only gold supervision; no segment-level gold exists or may be assumed. All MLLM segment/stance/target/mechanism outputs are weak/privileged pseudo-signals, may not act as test-time annotations, and require confidence-aware use, deterministic missing fallback, plus remove/shuffle/noise controls.
- **Iteration 1 B0 static integrity · 2026-07-10 · target not met:** after two independent code reviews, frozen config/fold and static sanity jobs 12686–12688 passed. Dev/test are used only as label-free ID sets for split disjointness; all OOF optimization/relation endpoints are train-only; strict JSON cannot be repaired; forbidden label/prediction/rank/margin/fold/seed/loss/event/family payload keys and segment schema fields are absent. This proves implementation integrity only, not relation quality, conditional information, headroom, B1 GO, or final accuracy/macro-F1 improvement.
- **Iteration 1 SSR terminal preflight · 2026-07-10 · route stopped, target not met:** strict OOF/mining jobs 12691–12702 show that SSR's whole selected candidate universe contains too few unique exact correctable errors. Under the impossible best case that every candidate is an accepted reliable MLLM relation, oracle gains are only MHC MI +0.0036 acc/+0.0048 mF1 (2 touched), MHC SC +0.0128/+0.0176 (7), MHC_zh MI +0.0052/+0.0065 (3), and MHC_zh SC +0.0259/+0.0307 (15), all below the frozen +0.05/+0.05 gate. Job 12705 verified `B1_DECISION=STOP`; 7B extraction, human audit, conditional permutations, exact shuffle, B2 and B3 were not run. **Anti-repeat:** no prompt/teacher/reliability/loss rescue on the same one-neighbour candidate/event universe; future methods must prove sufficient video-label-only OOF correctable coverage before MLLM calls and must not assume segment gold.
- **Iteration 2 EDCM Gate 1 · 2026-07-10 · 9.11 READY, target still not met:** three-round same-reviewer refinement froze one zero-new-parameter mechanism: label-blind train-only `V/S/O` coalition pseudo-signatures control the listwise gradient of ordinary full-video keys used by final kNN. `6.93 → 8.28 → 9.11 READY`, no drift and no-segment-gold audit PASS. Before any MLLM call, A0 must prove two-dataset video-label-only top-64/two-swap reachability; A1 then requires dense teacher-specific TV/`R`/`DeltaD` support and separation from Label-only plus one teacher-semantic-free strength-matched modality/content proxy. Remove/shuffle/noise, actual kNN repair, two datasets × three seeds and +3/+3 remain binding; no EDCM experiment result exists yet.
- **Iteration 2 EDCM A0 terminal screen · 2026-07-10 · route stopped, target not met:** reuse audit job 12710 passed, but the exact video-label-only top-64/two-swap oracle failed all non-provenance gates on both datasets. MHC reached only 15 errors with 36.79% support and `+0.0273 acc / +0.0394 mF1`; MHC-ZH reached 22 with 62.87% support and `+0.0380 / +0.0444`, below the registered counts and `+0.05/+0.05` thresholds. Joint job 12713 re-derived every ranking, canonical witness, metric, and gate before writing `STOP`, `A1_unlocked=false`; no MLLM/OCR/teacher call or cache occurred. **Anti-repeat:** do not tune EDCM prompts, teacher, loss, depth, swap count, or thresholds. The next route must change the video-level correctable unit, pass a new two-dataset dual-metric oracle screen before teacher spend, and continue to forbid any segment-gold assumption.
- **Iteration 3 CTE Gate 1 · 2026-07-11 · 9.20 READY, target still not met:** three rounds with one continuous independent reviewer refined CTE from `6.65 → 8.75 → 9.20 READY`, with anchor/no-drift/video-label-only audits PASS and no remaining method-specification blocker. The MLLM sees no gold and may emit only confidence-bearing `preserve/weaken/reverse/unclear` relations for full versus whole visual/language withholding. Those weak relations supervise a fixed-anchor, train-supported tangent of the exact epoch-refreshed full-bank true-class margin under one shared query/key encoder; test remains ordinary full-video kNN. Zero/blank/black-frame neutralization, segment/timestamp/span/localization gold, teacher keys, concat/fusion/rerank/router/scale and SSR/EDCM reuse are forbidden. Next is experiment planning and a vectorized full-bank SLURM microbenchmark, then CTE-0 only; CTE-1 stays locked and capped at 128 train videos/dataset. READY is specification readiness, not performance success.
- **Iteration 3 CTE C0 terminal numerical screen · 2026-07-11 · route stopped, target not met:** after final independent review reached `0 CRITICAL / 0 HIGH`, SLURM jobs 12715--12719 froze and executed the complete zero-teacher screen. Five-fold support/resource gates passed on both datasets (selected pair `(0.20,0.30)`, minimum joint support 0.9681 EN/0.9438 ZH, peak memory ≈0.096 GiB), as did margin and gradient parity. The frozen `2e-5` tangent/cost parity gate failed: max T/cost errors were `8.053e-5/2.223e-5` EN and `1.010e-4/2.191e-5` ZH. Independent job 12719 wrote `C0_DECISION=STOP`, `C1_unlocked=false`; C1, C2--C4 and all MLLM/OCR/teacher calls were skipped. **Anti-repeat:** do not relax numerical tolerance, change precision, anchor/radii or grid to rescue this exact CTE route. This is a bounded exact-kernel numerical failure, not evidence against all shared-representation methods or an MLLM result. The only gold remained the video binary label; no segment gold exists or was used.
- **Iteration 4 SQ-RGCL Gate 1 · 2026-07-11 · 9.12 READY, target still not met:** four rounds with canonical reviewer `/root/sq_reviewer_replacement` refined SQ from `6.88 → 7.90 → 8.46 → 9.12 READY`; anchor/no-drift/no-segment-gold/CTE-interpretation audits PASS. A label-blind train-only six-way whole-video presentation posterior supplies only confidence-bearing nuisance assignments. One crossed ranking loss directly shapes the shared final memory embedding: same-label/different-presentation full-bank positives outrank different-label/same-presentation memories only under exact harmful top-20 vote exposure; rank>20 exposure is zero and test is unchanged ordinary full-video kNN. Presentation-only provenance/semantic audits, environment×class positivity/ESS, P2 wrong-neighbor enrichment, P4-field-prediction, ENV-SUPCON/Yang, label-only/shuffle/random/base/cheap controls are binding. No code/job/new teacher call occurred. Next is an independent plan/audit and P0/SQ-0 only; SQ-0 must pass both datasets at `+0.05 accuracy/+0.05 macro-F1` before any teacher call. No SQ performance result exists, so the final two-dataset three-seed +3/+3 target remains active.
- **Iteration 4 SQ-RGCL S0 execution · 2026-07-11 · route stopped before learned performance, target not met:** after repairing an initial `4 CRITICAL / 9 HIGH` implementation audit to `0 / 0`, SLURM jobs 12722--12730 froze and ran the provenance/audit fast-fail. Both v2 train archives had exact ID/hash coverage and zero forbidden reader accesses, and local-CLIP six-way proxy coverage was 1.0, but original prompt/model-revision/input/code cryptographic linkage was absent. The proxy therefore remained `PROXY_ONLY_CHEAP_FORMAT`. Two label-free 64-whole-video QC sheets were prepared, but human ratings were not fabricated; missing blind QC was binding. Job 12730 independently wrote `S0=STOP`, `lambda_Q=null`, `S1_unlocked=false`, and `S2_unlocked=false`. P0/power/micro, learned SQ-0 and all teacher stages were skipped. This is not a classification result or upper bound. New teacher/MLLM/OCR calls were zero, and only video binary gold was used; no segment gold exists.
- **Iteration 5 ECM-RGCL method review · 2026-07-11 · 4.98 RETHINK / ABANDONED, target not met:** one independent GPT-5.5-high review preserved the ordinary full-video kNN endpoint, all-train strict-OOF/no-direct-outcome teacher protocol, train-only weak modes, no test teacher and no-segment-gold audit, but rejected the core mechanism. Soft mode risks followed by the proposed projected/minimax QP are algebraically dynamic sample reweighting plus generic gradient surgery, overlapping PG-DRO/JTT/EIIL/PCGrad/MGDA/CAGrad; additionally, raw-gradient constraints do not constrain the actual AdamW update. An MLLM may reconstruct scalar error propensity from video+prediction even without a literal correctness field, so error AUC is not semantic necessity. ECM was stopped before code, jobs, caches or teacher calls. **Anti-repeat:** do not rename pseudo-groups plus robust weighting/gradient surgery or the frozen QP; any future route requires a fresh Gate 0, realized full-bank or actual-update intervention, numerical non-reducibility to scalar weighting, matched ERROR-PROPENSITY controls, and only video-level binary gold.
- **Iteration 6 LB-SCGP method refinement · 2026-07-11 · 9.15 READY, target not met:** continuous review scored `6.74 -> 7.86 -> 8.69 -> 9.15`, anchor/no-drift/no-segment-gold PASS. The frozen route is one label-blind structural-reflection certificate compiled post-cache into an exact-vote-safe full-bank PSD Gram target and uniformly fitted by the shared encoder; test is ordinary kNN. No code/job/teacher call/result exists. Next is independent audit plus sealed microbenchmark (`<160 GPU-hours`), then zero-teacher SCGP-0 only; teacher remains locked until both datasets strict-OOF acc/mF1 each gain `+0.050` and all numerical/Farkas gates pass.
- **Iteration 6 LB-SCGP G0 implementation handoff · 2026-07-11 · blocked before execution, target not met:** implementation worker patched the real-fold G0 path and verifier but did not run jobs or self-certify audit closure. The known mixed-cache issue is now fail-closed: combined train/subclip caches, whole mixed bank archives, and fold JSON are not formal G0 inputs because they package held labels/content. This entry is superseded by Round3: formal G0 now requires only the train-only whole-video feature artifact with a frozen hash, and subclips are not G0/G1 inputs. Teacher/MLLM/OCR calls and SLURM submissions remain zero. Canonical handoff: `refine-logs/lb_scgp/G0_IMPLEMENTATION_HANDOFF.md`.
- **Iteration 6 LB-SCGP G0 Round2 repair handoff · 2026-07-11 · prepared for review, target not met:** Round2 repairs add a pre-freeze quarantine sanitizer and independent sanitizer verifier, sanitized formal provenance without mixed-cache locators/hashes, protected-path dirty hashing, independent real Dykstra/rank-cell replay surfaces, separate GPU fit/rollback replay, full registered singleton/pair/triplet/SupCon cone oracle checks, exact H10 formula, and actual one-GPU runtime evidence gates. No sanitizer/G0/replay SLURM job was submitted, no Python experiment computation was run on login, no teacher/MLLM/OCR call occurred, and no 0C/0H closure is self-certified. Global final ordinary-kNN +3/+3 target remains active and unmet.
- **Iteration 6 LB-SCGP G0 Round3 repairs · 2026-07-11 · prepared for independent review, not run; target not met:** Round2 independent review failed 3 Critical / 3 High. Round3 repairs remove the segment-cache path from sanitizer, formal config/freeze, producer and replay; G0/G1 subclips are not inputs, `lambda_seg=0`, `segment_cache=None`, and inherited parent labels are not segment gold. The sanitizer now produces only the train-only whole-video feature artifact plus sanitized provenance/decision; formal records reject quarantine/mixed/protected locators and source/mixed/legacy hash surfaces. Real Dykstra/rank-cell evidence now includes selected and adjacent per-projector transition/correction-state hashes for independent reconstruction; the Farkas cone definition is pinned. The first aborted Round2 review attempt was rejected as a process matter because it spawned sidecars without xhigh proof; the canonical Round2 report was by a sole explicit GPT-5.5 xhigh reviewer with no delegation. No job or Python experiment was run, no teacher/MLLM/OCR call occurred, no 0C/0H closure is self-certified, and G1/teacher remain locked. Global final ordinary-kNN +3/+3 target remains active and unmet.
- **Iteration 6 LB-SCGP pre-G0 sanitizer build attempt · 2026-07-11 · stopped before artifacts, target not met:** canonical Round3 review is FAIL only on physical C1, with 0 High code findings. The authorized sanitizer build was submitted under SLURM as job `12737` using `sbatch --export=ALL,TASK=build scripts/slurm/lb_scgp_sanitize_inputs.sbatch`; it failed in 4 seconds with exit code `1:0` before artifact creation. The log (`slurm/logs/lbscgp_sanitize_12737.out`, SHA256 `80d071490eb9f799ed119dce04d9004aad30a5fd2189ccc6b2c09d7d4fa4b4d7`) shows `AccessLedger.hash_file` raising a relative-vs-absolute `Path.relative_to(ROOT)` `ValueError` while loading the relative config path. At that point, `artifacts/lb_scgp` was absent, so `outer_train_features.pt`, `sanitized_provenance.json`, quarantine `sanitizer_manifest.json`, and `sanitizer_decision.json` did not exist. The verifier was not submitted; no freeze/synthetic/realfold/replay/decision/G1/teacher/MLLM/OCR work ran; no performance result existed; `segment_artifact_created=false`, `segment_objective_allowed=false`, and G1/teacher remained locked. This failure history is superseded only by the later physical-verification entry below.
- **Iteration 6 LB-SCGP sanitizer path-normalization repair · 2026-07-11 · prepared for independent review, target not met:** a shared fail-closed ROOT canonicalization helper now accepts relative or absolute config/artifact paths, resolves relative inputs under `/data/jehc223/RGCL`, rejects escapes, and records stable ROOT-relative paths in AccessLedger and reachable analogous LB-SCGP path surfaces. The sanitizer wrapper may still use relative `CONFIG` and `SOURCE_CONFIG`. At that point no rerun had occurred; the later physical-verification entry below records the successful `12738/12739` rerun. No segment path/objective was reintroduced; `lambda_seg=0` and `segment_cache=None` remain binding. No G0/decision/G1/teacher/MLLM/OCR/performance work occurred in the repair step. Handoff: `refine-logs/lb_scgp/G0_SANITIZER_PATH_FIX.md`.
- **Iteration 6 LB-SCGP sanitizer physical verification · 2026-07-11 · C1 closed at artifact level, target not met:** path-fix review passed 0 Critical / 0 High, then SLURM build job `12738` completed and verifier job `12739` completed with `status=PASS`. Physical hashes match: feature `ea5f0ace7fa614b243269e155ef12e44cfa646f7e2063ec7f0d7aaee11d87496`, sanitized provenance file `b921477c2cc8858f2f9dfe9b6da21a0aaea2287fdaf9059c2f1dba08010d8007`, quarantine manifest file `055dffed9b61053293741ec5ba0ce3577daf458ceef4a3f81143a81d937c684b`, verifier log `40f333bb82884e9ad412fc87a20165e1640238815c3c6b6951d003e1cb0ec247`, decision file `172c9db7589c5b80af7fe6f8476dd9866a4eb840bc1b4524a79b6010c2c3c954`, and decision payload `8685c805995f97ad0513658c1345b6320dc794e2bad02b907d9a2d07ff16cb1f`. The decision records 464 feature IDs, 115 held-query sentinels, all gates true, no segment artifact/objective, zero formal query reads, and zero teacher/MLLM/OCR/network calls. Independent physical review `refine-logs/lb_scgp/G0_SANITIZER_PHYSICAL_REVIEW.md` reports 0 Critical / 0 High and closes the remaining C1 artifact blocker only. At sanitizer close this was not G0 PASS, no G0 freeze/realfold/replay/decision/performance result existed, and G1/teacher remained locked.
- **Iteration 6 LB-SCGP G0 freeze · 2026-07-11 · freeze completed, target not met:** the registered next action was eligible after sanitizer decision `PASS`, absent formal namespace, present no-clobber locks, and no segment/subclip artifacts. SLURM command `TASK=freeze RUN_ID=LBSCGP-G0-FREEZE-v1 sbatch scripts/slurm/lb_scgp_g0_cpu.sbatch` submitted job `12742`, which completed with exit `0:0` in `00:00:03` on `8 CPU / 64G`. It produced `artifacts/lb_scgp/v1/CONFIG_FREEZE.json` SHA256 `b6697472b61a61706c694a67b21618d618fcad6e7f59265d8696aee79dc46889`, payload `b3b33090b39b3b975c2cf213aab669041b345c6ef3a3f7c200366a506bcebfd5`, publish-lock hash `34a05bde46775bcb75384c1c853cef7985f5c05efcdf3afec5fad6d85ef57d8d`, and log hash `70cd194ed6a811be6956644e832055907f94750ccec6876352a8c1d6b5e98628`. The artifact records only parent-video binary gold, `segment_gold_exists=false`, `segment_gold_used=false`, no segment/subclip artifact, zero MLLM/OCR/teacher/cache/held-label/held-content/val/test reads, and `G1_G4_locked=true`. No synthetic, realfold, replay, decision, G1, teacher, MLLM or OCR job was submitted. This is not G0 PASS and not a performance result. The next gate is the independent formal code audit artifact `LBSCGP-G0-CODE-AUDIT-v1`; this worker did not create or self-certify it. Residual audit note: the freeze hash-binds mutable intermediate records before this post-run audit-trail update, so the formal code audit must assess frozen-input drift risk before any numerical stage.
- **Iteration 6 LB-SCGP G0 v2 freeze repair · 2026-07-11 · v2 freeze completed, target not met:** the v1 formal code audit failed on C1 mutable freeze inputs and H1 pre-freeze sanitizer schema underspecification, so a no-clobber v2 lineage was created without editing v1. The plan now registers a dedicated pre-freeze sanitizer schema and states that full generic manifest/decision schema applies from `G0_FREEZE` onward. `configs/lb_scgp/lb_scgp_v2.json` uses exact run ID `LBSCGP-G0-FREEZE-v2` and namespace `artifacts/lb_scgp/v2`; mutable records (`EXPERIMENT_TRACKER.md`, `TARGET_LOOP.md`, `TARGET_STATE.json`, target findings, handoff/execution docs) are excluded from v2 formal inputs. SLURM job `12746` ran only the v2 freeze and completed `0:0` in `00:00:03`, producing `CONFIG_FREEZE.json` SHA256 `4c6f0199a429bbf766cb284b9ecaedd9dcaf38733834dd54574245ab86b633ae`, payload `bda18d7dfda00ab5808595afec04e5b925d3a4920f55fe963dec5e9d99795c0b`, lock `22426f693ba3f72d52928b0a86d08fe439d7e7fd43f9c74c61aedb710443e211`, and log `e95d167af054b91582e1d1f8fbf66fb57a3b5cd67e298883940d385a65ccf563`. All frozen immutable input hashes and allowed NPZ member hashes matched immediately after freeze. This is still not G0 PASS, not a formal code audit, not a numerical/performance result, and not a G1/teacher unlock; the next gate is fresh independent `LBSCGP-G0-CODE-AUDIT-v2`.
- **Iteration 6 LB-SCGP G0 v3 dirty-state repair · 2026-07-11 · v3 freeze completed, target not met:** the v2 formal code audit failed with exactly one Critical: mandatory post-freeze formal review records were not excluded from the dirty-state predecessor hash, so a downstream decision gate would STOP even if a code-audit PASS artifact existed. A no-clobber v3 lineage was created without modifying v1/v2 freeze artifacts or locks. `configs/lb_scgp/lb_scgp_v3.json` binds exact v3 run IDs, namespace `artifacts/lb_scgp/v3`, config-explicit dirty exclusions for only mutable progress/audit records including future `G0_FORMAL_CODE_AUDIT_REVIEW_V3.md`, and formal artifact exclusions for v1/v2/v3 namespaces. Producer/common and independent verifier now share the same config-driven dirty policy; the GPU replay wrapper reads the replay ID from config and fails closed. SLURM command `CONFIG=configs/lb_scgp/lb_scgp_v3.json TASK=freeze RUN_ID=LBSCGP-G0-FREEZE-v3 sbatch scripts/slurm/lb_scgp_g0_cpu.sbatch` submitted job `12748`, which completed `0:0` in `00:00:03`, producing `artifacts/lb_scgp/v3/CONFIG_FREEZE.json` SHA256 `9fba7f1649dd67d4bb0fcc193e555d8246d7a4966307732e87d5e9fca7346dd9`, payload `352ec2215e2225b1768a13f39f96ef935b91966606d8db874d6de4410b1a9f3d`, dirty hash `91cf2890acc543fdb2f3988f5063461f70d855469df386908436b4273054a4b1`, lock `9c32d07c524e466ad06c06fc2a472829764cd1facff22390eac8b2879d329b8f`, and log `f60d2301e7460ab91b25f6c323c578e49a00001b46c67a590f9f3c3d58abf545`. This is still not G0 PASS, not a formal code audit PASS, not a numerical/performance result, and not a G1/teacher unlock. MLLM/OCR/teacher/cache calls and outer-held/val/test reads remained `0`; `segment_gold_exists=false`, `segment_gold_used=false`.
- **Iteration 6 LB-SCGP G0 v3 formal code audit review · 2026-07-11 · review-passed only, target not met:** fresh independent audit `refine-logs/lb_scgp/G0_FORMAL_CODE_AUDIT_REVIEW_V3.md` reports `PASS_REVIEW_ONLY` with 0 Critical / 0 High / 2 Important, no-segment-gold PASS, and the exact v2 Critical closed for v3. The frozen dirty hash and audit recomputes before report creation, after report creation, after report update, and after required documentation updates all equal `91cf2890acc543fdb2f3988f5063461f70d855469df386908436b4273054a4b1`, proving the mandatory v3 review path is excluded narrowly enough for this lineage. No formal PASS artifact was created because no existing authorized schema/no-clobber producer for `artifacts/lb_scgp/v3/g0/code_audit/{review.md,audit.json}` was found; hand-written PASS JSON remains forbidden. No synthetic, realfold, replay, decision, G1, teacher, MLLM, OCR, held/val/test, or performance work was run. G0 is not passed; the next boundary is an authorized formal audit artifact producer or explicit authorization, then fresh schema/hash verification before any synthetic stage.
- **Iteration 6 LB-SCGP G0 v4 tooling-lineage repair and freeze · 2026-07-11 · freeze completed, target not met:** v4 adds the missing authorized formal code-audit producer without changing the scientific method, thresholds, fixtures, or performance logic. `configs/lb_scgp/lb_scgp_v4.json` registers namespace `artifacts/lb_scgp/v4`, exact v4 run IDs, exact future review path `G0_FORMAL_CODE_AUDIT_REVIEW_V4.md`, and strict future machine record path `G0_FORMAL_CODE_AUDIT_REVIEW_V4.record.json`. The new independent-verifier `audit-publish` path validates a machine-verifiable independent review record, recomputes freeze/config/implementation/verifier/dirty hashes, rehashes all frozen inputs and allowed NPZ members without opening forbidden query members, checks v1-v3 no-overwrite hashes, enforces zero forbidden counters and no-segment-gold, and publishes formal artifacts transactionally only after all checks pass. SLURM job `12759` ran only the authorized v4 freeze and completed `0:0`, producing `artifacts/lb_scgp/v4/CONFIG_FREEZE.json` SHA256 `dcf65eceba04e7c4f08145b2012653705f7347c6e96ebc8b2b769280dff48fd0`, payload `92301ad95870b8a1af41e7e69e45054a2e63fe80a021cf4c1c22906aea0872bf`, lock `09003ce9e741d7c0310045f854479deb8fecff74bfddd33f6b9d80dc6df9572a`, dirty hash `8ca10aec315f800959e7869beb200f4bbc5f5d27841d8c307d896f1644803e7a`, implementation hash `c7e9371494f991d88a7ab93cc64769fa1e6a92913df3afd2f647201d0eef1bf1`, and verifier hash `03a78a89867d3cea468b5319463ccabcefa4b4a589a61863bffd3e14c9df5402`. This repair did not create a v4 review, review record, or formal PASS artifact and did not run audit-publish, synthetic, realfold, replay, decision, G1, teacher, MLLM, OCR, held/val/test, or performance work. The next boundary is a fresh independent GPT-5.5 xhigh auditor creating the v4 review plus strict machine record and invoking audit-publish.
- **Iteration 6 LB-SCGP G0 v4 operational failure and v5 NameError repair · 2026-07-11 · v5 freeze completed, target not met:** v4 formal code-audit files exist and are internally consistent, but `refine-logs/lb_scgp/runtime/v4_audit_checks/publication_verification.json` records `all_ok=false` because producer `_load_freeze_and_audit` raised `NameError: name 'git_state' is not defined` while consuming the strict schema. v4 is operationally `FAIL/BLOCKED`; v4 formal outputs are failed-lineage evidence only, never unlock evidence. v5 repairs only the missing import and v4/v5 strict lineage/path/schema/no-clobber support. Focused SLURM regression job `12820` under conda `HateVideo` passed valid v5 producer consumption and fail-closed wrong hash, dirty hash, run ID, and path cases using temporary fixtures, leaving no formal v5 namespace. The only authorized formal v5 workload then ran: `CONFIG=configs/lb_scgp/lb_scgp_v5.json TASK=freeze RUN_ID=LBSCGP-G0-FREEZE-v5 sbatch scripts/slurm/lb_scgp_g0_cpu.sbatch`; job `12823` completed `0:0` in `00:00:02` on `8 CPU / 64G` and produced `artifacts/lb_scgp/v5/CONFIG_FREEZE.json` SHA256 `254e45afe9c0355892824c0c26bc73b4b0854cb20c67c3703982762fad010931`, payload `d89f7cd4ad43c8ef83a04b7530ec19186ec1cb0e96958d239f1ce5c2b146bb4d`, lock `54dc06d236d5fc1f3ac96400f1a81faeb1d2c0c8e5af075065d1964260de98a9`, and log `c43a2b16fc8c95bdfafb5c48c674fa4b778dd9c3d3c3764e24fe50ed038a0526`. No v5 review, record, audit-publish, synthetic, realfold, replay, decision, G1, teacher, MLLM, OCR, held/val/test, or performance work is authorized. The only gold remains the parent-video binary label; `segment_gold_exists=false`, `segment_gold_used=false`, and all forbidden counters remain zero.
- **Iteration 6 LB-SCGP G0 v5 formal code audit · 2026-07-11 · PASS/post-verified, target not met:** fresh independent audit produced finalized review SHA256 `495b5f3bc453034ae5f9830a77bc9b4a2b04af181b0d4365e95bdbaf450bd36b` and strict record SHA256 `4cb399a0209025581cef094f9f339b6617d9a0ad1d22d5925c131107118a3770`, with Critical `0`, High `0`, Important `3`, and no-segment-gold PASS. Audit-only SLURM job `12825` passed negative fail-closed checks; job `12830` published the formal no-clobber v5 bundle; job `12831` post-verified producer `_load_freeze_and_audit` and the strict decision consumer against the actual bundle with `producer_consumer_ok=true`, `decision_consumer_ok=true`, `all_ok=true`, and `dirty_equal_frozen=true`. Synthetic-only `LBSCGP-G0-SYNTH-v5` is unlocked but not run; realfold, replay, decision, G1, teacher, MLLM, OCR, held, val, test, and performance work remain locked. The only gold remains `parent_video_binary_label`; `segment_gold_exists=false`, `segment_gold_used=false`, and all forbidden counters remain zero.
- **Iteration 6 · LB-SCGP G0 v5 synthetic numerical STOP · 2026-07-11 · route stopped at synthetic, target not met:** the single authorized synthetic job `12833` (`LBSCGP-G0-SYNTH-v5`) completed `FAILED`, exit `2:0`, elapsed `00:00:44`. Manifest `artifacts/lb_scgp/v5/g0/synthetic/manifest.json` records `status=FAIL`, `thresholds_ok=true`, `expected_statuses_ok=false`, `dykstra_gate=false`, with `rank_gate/farkas_gate/factor_gate/rollback_gate=true` and `overflow_nan_inf_count=0`; the concrete mismatch is Dykstra expected-status parity, where `feasible_interior`, `feasible_boundary`, and `feasible_oriented_boundary` returned `BOUNDED_SEARCH_FEASIBLE` instead of the frozen `LOCAL_STATIONARY_CERTIFIED`. Realfold/replay/decision/G1/teacher stayed locked and no performance result exists. Source: `refine-logs/lb_scgp/G0_V5_NUMERICAL_EXECUTION.md`; `TARGET_STATE.json` `gate_6_g0_v5_synthetic_execution`; `TARGET_LOOP.md` v5-synthetic-STOP entry. This bullet backfills the gap between the v5-audit-PASS entry above and the v6 work below. (recorded post-hoc by assistant, 2026-07-12, per user request)
- **Iteration 6 · LB-SCGP G0 v6 result-to-claim · 2026-07-12 · KILL (claim partial / supplement), target not met:** the authorized single v6 repair failed to certify the actual oriented fixture. Cell 0 Phase-I had a replayed full-rank feasible point, but Cell 0 Phase-II returned `BOUNDED_REMOVE` (not `LOCAL_STATIONARY_CERTIFIED`) with original-objective stationarity infinity-norm `0.0022782803493536386` and PSD minimum eigenvalue `-5.8836793827797916e-09`; Cell 1 was `NO_WITNESS`, its candidates satisfying all 589 residuals at positive eig margin yet realizing a non-target top20 hash. Diagnosed root causes: strict rank-cell encoding ambiguity plus absent original-objective local stationarity; no conic Farkas incompatibility certificate exists, so this is not an infeasibility proof. The review authorized at most one v7 repair and pre-registered the global-Gram pivot fallback. Source: `refine-logs/lb_scgp/G0_V6_ACTUAL_RESULT_TO_CLAIM_REVIEW.md` (claim_supported line 16; cell metrics lines 84-96; pivot boundary lines 376-383). (recorded post-hoc by assistant, 2026-07-12, per user request)
- **Iteration 6 · LB-SCGP G0 v7 result-to-claim · 2026-07-12 · KILL + PIVOT (claim no / pivot), local rank-cell route retired, target not met:** the final authorized local repair (certificate job `12896`, independent replay job `12898`) enumerated both canonical compatible cells but failed the preregistered strict signed-gap `G[q,a]-G[q,b] >= tau+eta` (`tau=1e-7`, `eta=1e-12`): cell 0 `min_margin=-6.736095449260811e-15`, cell 1 `min_margin=-1.7186069668545097e-14`, both `pass=false`. Phase II was empty, and replay preserved `NO_COMPATIBILITY_WITNESS_NO_FARKAS` (neither compatibility nor a Farkas infeasibility proof). The local rank-cell stationarity route is retired as the main G0 gate and v8 solver tuning is forbidden. The mechanism-preserving pivot skeleton is: train-only label-blind structural certificates -> one global full-bank PSD/unit-diagonal Gram target -> uniform encoder fit -> ordinary test kNN, still targeting MHC-EN and MHC-ZH. Source: `refine-logs/lb_scgp/G0_V7_RESULT_TO_CLAIM_PIVOT_REVIEW.md` (claim_supported line 27; margins lines 69-72 and 136-137; jobs lines 18-19; pivot spec lines 224-226). (recorded post-hoc by assistant, 2026-07-12, per user request)
- **Iteration 7 · LB-SCGP Global-R2 method refinement · 2026-07-12 · 9.1 READY, target not met:** the v7-authorized pivot opened a new method family, LB-SCGP Global-R2 (`refine-logs/lb_scgp_global/`): a closed train-only label-blind structural-certificate cache compiled into one replayable full-bank PSD/unit-diagonal Gram target fitted uniformly by the shared encoder, with test as ordinary full-video train-memory top20 kNN. Final target is MHC-EN and MHC-ZH, seeds 0/1/2, accuracy and macro-F1 each `+0.030` over the strongest same-protocol non-MLLM comparator, with paired-bootstrap/Holm and REMOVE/SHUFFLE/NOISE/direct controls. Independent GPT-5.5 xhigh research-refine scored `7.1 REVISE -> 8.3 REVISE -> 9.1 READY` over three rounds. READY is method-specification readiness only; no performance result exists. Source: `refine-logs/lb_scgp_global/score-history.md`, `REFINE_STATE.json`, `FINAL_PROPOSAL.md`. (recorded post-hoc by assistant, 2026-07-12, per user request)
- **Iteration 7 · LB-SCGP Global-R2 M0 contract freeze, Run2 infra crashes, and Welch-bound repair · 2026-07-12 · M0 not passed, execution unauthorized, target not met:** M0 contract-freeze Run1 job `12901` completed FROZEN (`artifacts/lb_scgp_global/v1/m0/contract_freeze.json`, sha256 `09b78682389f1c9774c9dffc43c759bceeec9d7f44eca1ce4cd626d0cd6d12da`, all access counters zero). The v1 Run2 synthetic-KKT was attempted twice and failed fail-closed before any published artifact: job `12902` `KeyError: 'finite_vi_diagnostic'` (older producer path, exit `1:0`, `00:00:04`) and job `12904` `KeyError: 'payload_schema'` (newer validator preflight, exit `1:0`, `00:00:01`); both are pure infrastructure/interface failures, not scientific/KKT/numerical results, and the v1 Run2 lineage must not be retried. A prospective non-clobbering v2 lineage was authorized; its fix2 is implemented and statically frozen but `ready_for_execution=false`, locked pending a fresh independent 0C/0H implementation/code review, exact-hash no-clobber review, and separate execution authorization. Welch-bound feasibility finding: keeping `G0=I_N` while requiring a PSD unit-diagonal rank-`<=d=3` target on `N=10` within off-diagonal coordinate-trust `<=0.02` is impossible, because any rank-`d` correlation matrix obeys `mean_{i<j} G_ij^2 >= (N-d)/(d(N-1))`, so for `N=10,d=3` some `|G_ij| >= sqrt(7/27) ~= 0.509 >> 0.02`; fix2 therefore uses a unit-diagonal local-projection baseline `G0` instead of identity. Unresolved adjudication: whether `G0=I_N` was a frozen scientific semantic (then H5 cannot be closed under `N=10,d=3,trust=0.02`) or a fixable implementation choice — deferred to the user / fresh reviewer. Sources: `refine-logs/lb_scgp_global/M0_CONTRACT_FREEZE_EXECUTION.md`; `M0_RUN2_RESULT_TO_CLAIM_REVIEW_FRESH.md` (crash classifications lines 42, 56, 66, 71); `M0_RUN2_V2_IMPLEMENTATION_FIX2_FREEZE.md` (Welch bound lines 11-15, semantic pending line 71); `EXPERIMENT_TRACKER.md` Run2-v2 notes. (recorded post-hoc by assistant, 2026-07-12, per user request)
- **Registry erratum · HateMM strongest-non-MLLM baseline · 2026-07-12 · applied to TARGET_STATE.json:** the Iteration-0 HateMM comparator was corrected from the erroneous `0.8732 / 0.8686` to the verified `0.8279 / 0.8172` (binding `+0.030` target `0.8579 / 0.8472`). `0.8732` was a Val_Retrieval ROC-AUC mis-transcribed as a test accuracy; `0.8686` was a phantom number with no source reading. The correct value is the val-selected epoch-24 Test_Retrieval test acc / macro-F1 at `slurm/logs/rgcl_HateMM_openai_clip-vit-large-patch14-336_HF_1035814.trainlog:257,259` (n=215), erratum commit `66012e9`. The historical Iteration-0 registry table in `TARGET_LOOP.md` was corrected in place on 2026-07-12 (HateMM row value, hard target, and provenance caveat, with an inline correction marker); this erratum remains authoritative. (recorded post-hoc by assistant, 2026-07-12, per user request)
- **Iteration 8 candidate-registry recovery · 2026-07-28 · target unmet, no execution:** identity re-detected as `jehc223`/`slurm_only`; historical state and 76-entry eliminated ledger preserved; active serial slots set to A/B/C = C01 Policy-Contrastive Discourse Transport / C02 Evidence-Density Quotient Geometry / C03 Policy-Anchored Native MNTP, with C04–C14 ordered backlog, unified fold-head pilot gates, and conformal three-way output plus cross-bank drift certificate explicitly kept outside the full-coverage `+0.030 Acc/+0.030 macro-F1 on >=2 datasets` loop. No computation, teacher call, test touch or SLURM job.
- **Iteration 8 C01 A0 implementation/static-NO-GO repair · 2026-07-28 · READY_NOT_RUN, pre-Stage0 kill-only, target unmet:** the CPU/SLURM-only standard-L24 versus one-word-L24 endpoint diagnostic now exact-validates an internal canonical binding for every run/science/statistics/output decision field, rejects empty aggregate families, consistently names the secondary `common_interaction`, freezes decision schema `c01_a0_decision_v1`, removes all force/overwrite behavior, and derives test/cache access counts from a runtime ledger. Existing cache provenance was found to contain only exact sizes plus SHA256 16-hex prefixes, so a new independent read-only Slurm preflight (`scripts/slurm/c01_hash_inputs.sbatch`) must exclusive-create an eight-file 64-hex manifest; A0 rehashes and exact-matches it before every `torch.load`. `next=hash_preflight_then_runtime_smoke`; even survival is not Stage0 success or Gate0→Gate1 advancement. No Python/pytest ran on the login node, no SLURM job was submitted, no cache/test input was opened by this repair turn, and no manifest/result/decision/new metric exists. External reviewer raw persistence remains reviewer-owned and pending in `TARGET_REVIEW_RAW.md`.
- **Iteration 8 C01 A0 external Gate1 re-evaluation · 2026-07-28 · 8.1 REVISE, not run, target unmet:** reviewer `/root/idea_reviewer` confirmed claim narrowing, the full fixed orthogonal-rotation/block-L2 control, and same-pooling-only positive/negative semantics, but found the small-displacement authorization audit incomplete: fix concentration is gated only against `endpoint_concat`, whereas final gain/net-fix decisions use the dynamically strongest ordinary control. A0 is therefore not authorized for hash preflight/runtime until the audit also uses the exact decision `strongest_control_name`; endpoint-concat may remain as a mechanism diagnostic. Full Round 1 and Round 2 raw reviews are appended in `TARGET_REVIEW_RAW.md`; no code was run, job submitted, cache/test path opened, or manifest/result/decision/metric created.
- **Iteration 8 C01 A0 strongest-control reference repair · 2026-07-28 · implemented/static-only, pending reviewer confirmation, target unmet:** one shared accuracy/macro-F1/frozen-order selector now supplies both net-fix binding and the small-displacement concentration gate. The endpoint-concat calculation remains `diagnostic_only` and carries no gate. The selector reads only frozen ordinary-control metrics, excludes the primary, and is independent of the small-displacement outcome, so no baseline/gate cycle is introduced. Config/canonical binding, decision schema, preregistration, loop, and state are synchronized. No Python/pytest, SLURM job, cache/test access, artifact, or metric occurred.
- **Iteration 8 C01 full-SHA256 preflight · 2026-07-28 · COMPLETED/validated, A0 not run, target unmet:** after confirming the exclusive namespace was absent, the user queue was empty, and the wrapper had no time/GPU/disk-guard/force path, exact command `sbatch scripts/slurm/c01_hash_inputs.sbatch` submitted job `13710` once. The expected `JobHeldUser` state was allowed to clear automatically; job `13710` then completed `0:0` in `00:00:01` with empty stderr. The exclusive 6051-byte manifest `artifacts/c01_policy_contrastive/v1/hash_preflight/C01-HASH-v1/full_sha256_manifest.json` has SHA256 `083275d39a1026bde3b6583bd5608d41cec5b431da9ffda87ae8ab1046cf2305`; shell/jq validation confirmed exactly eight MHC_zh/HateMM train/dev_seen standard/oneword inputs, eight full 64-hex hashes, eight successful non-test ledger entries, and zero test-like attempts/opens. This is provenance-only evidence: no A0 result/decision/metric, test access, Stage0 success, or Gate0→Gate1 advancement is claimed, and the A0 scientific verdict is unchanged.
- **Iteration 8 C01 A0 runtime · 2026-07-28 · HALT_FAIL_CLOSED_NO_DECISION, target unmet:** after all manifest/namespace/queue/wrapper gates passed, exact command `sbatch scripts/slurm/c01_a0_cpu.sbatch` submitted job `13712` once. It failed `1:0` after `00:00:53` on the frozen row-norm guard: `HateMM/train/standard/img` has one row at/below epsilon `1e-12` (index 355). Stdout is empty and stderr is 1218 bytes; the created A0 namespace is empty and neither result nor decision JSON exists. Therefore no R0/arm/rotation metric, delta, bootstrap/Holm, fix-break, shuffle, small-displacement, fired-rule, CONTINUE, or KILL value is reportable; this is an operational HALT before publication, not a scientific KILL. No test access or next-candidate submission was detected/authorized, no retry occurred, and the Feishu hard-blocker notification succeeded.
- **Iteration 8 C01 zero-contract probe · 2026-07-28 · COMPLETED_VALIDATED diagnostic, target unmet:** after scoped re-review `GO (0C/0H/0I)` and absent-namespace/empty-queue/safe-wrapper/exact-manifest gates, exact command `sbatch scripts/slurm/c01_zero_contract_probe.sbatch` submitted job `13717` once; `JobHeldUser` cleared automatically and the job completed `0:0` in `00:00:03` with empty stderr. Artifact `artifacts/c01_policy_contrastive/v2/zero_contract_probe/C01-ZERO-PROBE-v1/zero_contract_probe.json` is valid, 18,090 bytes, SHA256 `bee4964ce7e4ca81cfdb72c3859f78196568badf982aef587bc14ee6dbe63526`. All MHC_zh train/dev_seen and HateMM dev_seen policy/modality cells have zero exact-zero/tiny/non-finite rows; HateMM train has only row 355, ID `hate_video_95`, label 1, exact-zero in both img/text under both standard/one-word, with the other modality also `exact_zero`. All endpoint masks match, no tiny/non-finite row exists, and all standard-only/one-word-only sets are empty. Thus the three preregistered v2 necessary conditions are true, but historical same-null baseline consumption remains `REPORTED_EXTERNAL_NOT_VERIFIED_BY_PROBE`; `allow_zero_block_in_a0=false` remains binding, with no A0 change/retry or new scientific decision authorized.
- **Iteration 8 C01 A0 v2 structural-null adjudication · 2026-07-28 · GO_CONTRACT_ONLY/NO_GO_EXECUTION, target unmet:** independent read-only review closed the external evidence chain: `hate_video_95` row 355 is the unique HateMM/train standard/one-word × img/text exact-zero caused by Decord+PyAV decode failure and the extractor's all-cell guard, while `READOUT_SUBMIT_RECORD.md` establishes that deployed R0 consumed the same row in a full-cache bit-exact cache. A prospective v2 may preserve zero only for this probe/hash/identity-bound tuple, with label used solely for drift checking. All other zero/tiny/nonfinite/mismatch cases remain fail-closed; the sole null must be fixed in all 256 shuffles, stay exact-zero in every derived arm, never enter top20, and pass exact with/remove-null score/prediction/gate parity. Historical R0 comparability is preserved iff those guards pass; all existing scientific thresholds remain unchanged and new guards are HALT-only. No implementation was changed/reviewed, no computation or job ran, no A0 retry/result/metric/decision was authorized or prewritten, and v1 `allow_zero_block_in_a0=false` remains active.
- **Iteration 8 C01 A0 v2 contract implementation · 2026-07-28 · V2_READY_NOT_RUN_PENDING_REVIEW, target unmet:** `C01-A0-v2` exact-binds the manifest/probe and only HateMM/train `hate_video_95` row 355. Review repair now excludes the registered null from every displacement quantile/tiny reduction and requires a retained-array masked route to match a physically removed-row route for threshold, dev small mask, tiny fractions/counts, small-row dominance and final gate. With/remove retrieval arrays require dtype/shape/C-order-byte equality with hashes; metrics use canonical typed IEEE-754 serialization/hashes, signed zeros differ, and NaN/Inf is forbidden. Raw/derived nulls remain numeric exact-zero mask checks. These HALT-only guards prevent null influence on bootstrap/Holm/net/decision without changing R0, rotation/shuffle, `+0.020/+0.020`, or net-fix gates. V1/job `13712`/empty namespace remain preserved; no Python, job, result, decision, or metric occurred.
- **Iteration 8 C01 A0 v2 runtime · 2026-07-29 · HALT_FAIL_CLOSED_NO_DECISION, target unmet:** after re-review `GO (0C/0H/0I)` and empty-queue/absent-namespace/exact-manifest/exact-probe/safe-wrapper gates, exact command `sbatch scripts/slurm/c01_a0_cpu_v2.sbatch` submitted job `13730` once. `scontrol` confirmed no dependency/array/chain and CPU/memory-only resources; `JobHeldUser/PENDING_APPROVAL` cleared automatically after 32m16s. The job failed `1:0` after `00:01:11` at `real/endpoint_std with-null/remove-null retrieval mismatch`. Stdout is empty; stderr is 1,245 bytes, SHA256 `fd74537da9cdd2cb435bf160328b65289d4d9a8e2ea2ef5c654f764f22eaf5b4`; the v2 namespace is empty and no result/decision exists. Therefore every raw arm/control metric, delta, bootstrap/Holm, fix-break-net, rotation/shuffle/small-displacement/null-guard aggregate, fired scientific rule and CONTINUE/KILL verdict is unavailable for both datasets. This is an operational HALT, not scientific KILL; no retry/C02/follow-up job occurred, and the Feishu error notification returned HTTP 200.
- **Iteration 8 C01 retrieval-equivalence probe preparation · 2026-07-29 · DEBUG_READY_NOT_RUN, target unmet:** static inspection found no evident mapping or duplicate-FAISS-add bug in the 13730 path, but its log did not identify the unequal component. The repaired read-only, exclusive 8CPU/32G probe matches A0's thread environment (`OMP/MKL/OPENBLAS/NUMEXPR=8`), imports A0's exact endpoint helpers and requires independent local byte parity, uses a corrected sign-aware ordered-float32 ULP map with signed-zero/cross-sign/NaN/Inf policies and known-bit runtime self-checks, and reserves `RAW_FAISS_TIE_ORDER` for byte-identical stable neighbors plus similarities. It compares raw FAISS and deterministic `(-similarity, original index)` results with limited mapping/null/top20/set/order/ULP/score/prediction/metric/tie evidence. A0 was not modified; no Python/job/result occurred and execution remains unauthorized.
- **Iteration 8 C01 retrieval-equivalence probe execution · 2026-07-29 · COMPLETED_DIAGNOSED, target unmet:** scoped repair re-review was `GO (0C/0H/0I)` and all absent-namespace/empty-queue/exact-evidence/safe-wrapper gates passed before exact single submission of job `13732`. `JobHeldUser` auto-cleared after `00:17:36`; the job completed `0:0` in `00:00:03`. Artifact `artifacts/c01_policy_contrastive/v2/retrieval_equivalence_probe/C01-RETRIEVAL-EQUIV-PROBE-v1/retrieval_equivalence_probe.json` is 29,347 bytes, SHA256 `724c87cd2fbdb763180b663bc6492322887bc2077f378c5b21c4184c4ba80e6f`. Imported-A0/local endpoint keys and ordered-ULP self-checks passed; mapping is exact and the null never enters top-20. Full versus remove-null raw/stable neighbor arrays are identical, but 22 float32 similarities differ by at most `2.980232238769531e-7` / `5 ULP`, producing 20 finite score differences up to `2.2706531321858847e-8`; predictions and canonical accuracy `0.8411214953271028`, macro-F1 `0.8390977443609022`, ROC-AUC `0.9164244186046512` are byte-identical. Diagnosis is `FLOAT_VARIATION_WITH_STABLE_NEIGHBOR_IDENTITY`, not mapping/null/tie-order/candidate-set change. This is diagnostic-only; no A0 retry, C02, test access, feature serialization or scientific decision occurred.
- **Iteration 8 C01 numerical-equivalence guard adjudication · 2026-07-29 · GO_CONTRACT_ONLY/NO_GO_EXECUTION, target unmet:** the exact-byte similarity/score guard is approved for prospective replacement by a dimension/precision-derived bound, not by thresholds fitted to job `13732`. Retained normalized operands, mapping, null top20=0, raw/stable neighbor identity/order, labels, prediction bytes, canonical metric bytes, scientific booleans and finiteness remain exact. Similarity uses the power-of-two envelope derived from `u32=2^-24`, actual arm `d`, `gamma32(d)` and audited operand norm scale, plus its induced ordered-ULP code interval; weighted-vote scores must satisfy the exact 20→1/210 propagated bound plus `2^-45`, with cutoff-interval stability. A deterministic binary64 exact-neighbor reference ablation is mandatory; any violation HALTs. Since v2/job `13730` is immutable, implementation requires a new `C01-A0-v3` config/run/result/decision/schema/wrapper/namespace. All scientific CONTINUE/KILL gates remain unchanged. No implementation was changed/reviewed, no run/job/result/decision was authorized or prewritten, and no v2 retry/C02/test access is allowed.
- **Iteration 8 C01 A0 v3 numerical-equivalence implementation · 2026-07-29 · V3_READY_NOT_RUN_PENDING_REVIEW, target unmet:** new v3 analysis/config/wrapper/record and exclusive namespace identity implement the reviewed contract while SHA-binding the complete frozen v2 scientific config/source. Exact operands, mapping, null exclusion, raw/stable neighbors, labels, predictions, canonical metrics and scientific-boolean basis precede formula-derived actual-`d` `gamma32/rho/B_sim`, outward ordered-ULP, 20→1/210 score propagation plus `2^-45`, and cutoff-interval guards. A fixed chunked binary64 rescore consumes only agreed float32 top-20 IDs for every real arm, rotation and fixed-null shuffle; it never remine/replaces neighbors or uses observed results to set a bound. V1/v2 files, namespaces and job records remain frozen; no Python/job/result/decision/metric occurred and v3 execution is unauthorized pending fresh review.
- **Iteration 8 C01 A0 v3 static-NO-GO repair · 2026-07-29 · IMPLEMENTED_PENDING_REREVIEW, target unmet:** the repair closes the numerical-schema diff whitelist while preserving the SHA-frozen v2 science; establishes an acyclic source→config/evidence, wrapper→source/config, record→wrapper, TARGET→record provenance chain; makes FAISS/binary64 similarity and score zero-position/signbit disagreement an explicit HALT; checks the authorized tuple field-by-field against both frozen v2 and the diagnostic projection; expands decision validation to complete v2/v3 provenance, schema, types, small-displacement, continue/label, result and numerical-contract checks; and derives every public guard summary from actual runtime caches/audits/counters/ledger. Frozen hashes are source `40b35eee...4dfb`, config `4ddb0f6f...cda5`, wrapper `e61b9962...af99`, record `3af07f73...38fb`. No formula/scientific arm changed, no Python/job/result/decision/metric occurred, and execution remains unauthorized.
- **Iteration 8 C01 A0 v3 job 13735 · 2026-07-29 · HALT_FAIL_CLOSED_NO_DECISION, target unmet:** the scoped static `GO (0C/0H/0I)` was invalidated by a missed deterministic aggregate-schema bug. After one exact authorized submission and automatic `JobHeldUser` release, job `13735` failed `1:0` at HateMM's derived public contract summary. `avg_score_equivalence` emits `status=PASS` without `registered_null_top20_count`, while the aggregate requires that field to be zero for every audit not marked `NO_REGISTERED_NULL`; it therefore derives the registered-null aggregate false even though average-score is not a retrieval arm. This is a fail-closed guard implementation blocker, not a scientific/numerical verdict. The namespace is empty and no raw metric, public guard, result, decision or CONTINUE/KILL verdict was published; no repair, retry or C02 was submitted.
- **Iteration 8 C01 A0 v4 typed-audit repair · 2026-07-29 · V4_READY_NOT_RUN_PENDING_REVIEW, target unmet:** v4 preserves frozen v3 bytes/job `13735` history and exact-compares the complete v3 numerical-equivalence contract, while replacing the untyped mixed audit aggregate with exact-key/type variants for direct registered-null retrieval, derived average-score control, and `NO_REGISTERED_NULL`. `avg_score` now carries an explicit null direct count plus a linked proof that `endpoint_std` and `endpoint_ow` each have literal integer count zero; MHC-ZH carries an explicit zero occurrence count with no-null applicability semantics. Public/final guards aggregate typed summaries without permissive defaults. A pre-namespace fail-closed self-test registers two valid paths and rejects missing/wrong average counts, nonzero source counts, and a missing MHC no-null count. Frozen v4 hashes are config `2d9488e6...7ca6`, source `3c545eed...5514`, wrapper `9ae7f103...f107`, record `916074b8...d136`. No Python, SLURM job, cache/test access, namespace, result, decision, metric, retry, or C02 action occurred; execution is unauthorized pending fresh review.
- **Iteration 8 C01 A0 v4 job 13738 · 2026-07-29 · COMPLETED / KILL_CURRENT_ENDPOINT_ROUTE_ONLY, target unmet:** the one reviewed 8CPU/32G CPU-only submission completed `0:0`; all schema, lineage, numerical-equivalence, null, binary64-reference, shuffle and scientific-basis validity guards passed, so this is a valid scientific KILL rather than an engineering HALT. `common_displacement` underperforms the strongest ordinary control on MHC-ZH by `-0.02564` accuracy / `-0.02938` macro-F1 and on HateMM by `-0.00935 / -0.00983`; it has zero Holm rejections in every primary-control and primary-rotation accuracy/F1 family, loses to the best rotation controls, and has net fixes `-2/-1` against required `+2/+3`. Although all 8 real-versus-shuffle hypotheses reject and displacement stability passes, that evidence is insufficient for the frozen gain/control gates. The KILL retires only this endpoint-contrast route; C02 moves to design/independent review with no job authorized.
- **Iteration 8 C02 design review · 2026-07-29 · KILL_C02_DESIGN_COLLISION_OR_INFEASIBILITY, target unmet:** EDQ-Orbit was independently rejected before implementation because its proposed Stage-A0 used P3 image-pooling variants to proxy a transcript-density target, while evidence-core deletion was not safely label-preserving. A read-only asset audit found no matched HateMM+MHC-ZH bank for full-transcript-preserving repeat/localized-repeat views; therefore C02 cannot satisfy the frozen existing-bank `+0.050` accuracy and macro-F1 Stage-0 gate without first performing the prohibited new extraction. This is a design infeasibility under the current registry, not an experimental refutation of abstract EDQ. No Python, cache/test access, teacher/GPU work, SLURM job, implementation, artifact, or metric occurred; C02 is frozen and C03 design/review is next.
- **Iteration 8 C03 design review · 2026-07-29 · KILL_C03_DESIGN_INFEASIBILITY, target unmet:** the prospective native policy-MNTP design closed label-dependent mask/loss selection and inference leakage, but the file-level audit found no matched HateMM+MHC-ZH Stage-0 bank: F72/F92/F93/F70-C01 measure mask-only, readout, incompatible transplant, or confounded endpoint changes. Because the registry requires an actual fold/deployed-head `+0.050` accuracy and macro-F1 oracle on both datasets before any GPU/extraction, and producing the missing native bank would violate that order, the independent reviewer returned `CONFIRM_KILL (0 Critical / 0 High)`. This kills executability under the current contract, not abstract MNTP. No Python, cache/test access, teacher/GPU work, SLURM job, implementation, artifact, or metric occurred. Active C01-C03 are exhausted; reopen Gate 0 before C04.
- **Iteration 8 C04 design review · 2026-07-29 · REVISE_USER_AMENDMENT_REQUIRED, target unmet:** SPaSH-Tensor formalizes an ordered source–proposition–presenter-stance–harm-act interaction as a train-only privileged dense target for a native teacher-free kNN embedding, but independent review returned `2 Critical / 5 High / 4 Important`. The binding proxy mismatch is that existing P8 summaries, K4 density scores and deterministic S/T cues do not instantiate the proposed four factors, so they may neither PASS nor scientifically KILL C04; the frozen pre-teacher Stage-0 order therefore has no executable matched gate. The second Critical is missing explicit per-slot reliability/confidence and deterministic conflict/missing fallback. C04 remains scientifically alive but blocked on an explicit user decision: a C04-only train-only local-teacher amendment, first 200 label-blind hash-selected IDs per HateMM/MHC-ZH under a 2 GPU-hour total cap, then only on PASS+fresh GO completion of the 744/579 train banks under an 8 GPU-hour total cap, with no dev/test teacher and the original `+0.050/+0.050` full-bank gate retained. Approval would still require method/control/fold/novelty repair and fresh review. No Python, test, cache opening, teacher, GPU, test access, implementation, SLURM job, artifact or metric occurred.
- **Iteration 8 C04 V2 design freeze · 2026-07-29 · PENDING_FRESH_INDEPENDENT_REVIEW, target unmet:** the user approved only the bounded C04 matched-teacher amendment. V2 preserves the fixed 200+200 hash sample, local two-prompt/eight-frame/capped-transcript input, pre-label seal, serial `<=2 GPU-hour` small ceiling and conditional exact 744/579 completion under a total `<=8 GPU-hour` ceiling; no dev/test teacher/API/OCR/cross-dataset data is allowed and the full-bank `+0.050/+0.050` gate is unchanged. It closes the prior 2C/5H/4I on reliability/fallback, strong composition/KD controls, exact perturbations, nested same-arena OOF/native-only dev, paired seeds and expanded novelty boundaries. No implementation, Python/test, cache/teacher, GPU, test, SLURM, artifact or metric occurred.
- **Iteration 8 C04 V2 review / V3 repair · 2026-07-30 · REVISE_0C_4H_3I_THEN_V3_FROZEN, target unmet:** the V2 reviewer exact-matched all hashes and accepted the user amendment, proxy disposition, core reliability, retained KD/concat controls, OOF/native arenas, novelty and budget skeleton, but required exact fallback-control semantics/gates, semantic-unreliability KILL taxonomy, unique fair tuning/seed rules, dense block-covered lower-order compression, zero-frame behavior, full perturbation margins and cap-overshoot prevention. The minimal V3 overlays freeze those items without changing the user contract. No implementation, Python/test, cache/teacher, GPU, test, SLURM, artifact or metric occurred; fresh V3 review is next.
- **Iteration 8 C04 V3 review / V4 repair · 2026-07-30 · REVISE_0C_2H_1I_THEN_V4_FROZEN, target unmet:** V3 exact-hash review accepted semantic-KILL, tuning/seed skeleton, dense lower-order control, zero-frame handling, perturbation coverage and watchdog, but found that fallback-drop gates rewarded dependence on the reliability mask, three mandatory retained controls were omitted from equal tuning, and malformed-output wording conflicted with single-valid tolerance. V4 freezes STATE_ONLY loss plus STATE_BLIND retention/equivalence, makes collapse/shuffle/noise equivalence controls with exact applicability, adds full eight-trial treatment for STANCE/HARM/CAPACITY, and limits semantic reliability KILL to five numeric completed-cache rates. No contract, implementation, computation or execution action changed.
- **Iteration 8 C04 V4 design review · 2026-07-30 · GO_0C_0H_0I_IMPLEMENTATION_REVIEW_NEXT, target unmet:** the independent read-only reviewer exact-matched all V4 hashes, confirmed unchanged V3 bases and accepted the state-only/state-blind/equivalence anti-artifact gates, complete equal tuning for every retained control and the five-rate semantic reliability taxonomy with no remaining defect. GO is design-only: it permits prospective implementation followed by fresh code/resource review, not implementation execution, teacher/cache generation, Python/tests, GPU, SLURM, labels/test access or a result claim.
- **Iteration 8 C04 implementation-v5 / CPU-preflight authority · 2026-07-30 · CODE_RESOURCE_GO_0C_0H_0I_AUTHORITY_PENDING_UNLOCK_REVIEW, target unmet:** the prospective implementation converged through v1-v5 to independent static `GO (0C/0H/0I)`. The prepared v5 CPU-preflight authority binds the reviewed config/record, code/resource verdict, normalized config contract, all implementation/design/source/model/processor hashes and exact preflight-pending prompt/map semantics. Only implementation+CPU-preflight are true; every teacher/GPU/Slurm-GPU/small/reconciliation/forbidden authority is false. Namespace and stage payload artifacts are absent, and no Python, data/model access, SLURM job, metric or result occurred. Fresh independent unlock review is required before submission.
- **Iteration 8 C04 v5 CPU-preflight unlock · 2026-07-30 · GO_0C_0H_0I_READY_NOT_SUBMITTED, target unmet:** independent static review exact-matched the authority/config/request/TARGET closure and confirmed the fixed wrapper/sbatch can perform only CPU self-test+freeze. Exactly one CPU-preflight submission is now authorized; no job was submitted by this update. All teacher/GPU/Slurm-GPU/small/reconciliation/forbidden permissions and later stage reviews remain false/pending.
- **Iteration 8 C04 v5 CPU-preflight job 13805 · 2026-07-30 · ENGINEERING_HALT_NO_SCIENTIFIC_VERDICT, target unmet:** the single authorized CPU-preflight submission was spent and died fail-closed. Job `13805` was submitted `08:48:50`, held `JobHeldUser` ~7h52m, auto-released and both started and ended `16:40:57` with state `FAILED` exit `1:0`, `00:00:00` elapsed, 8 CPU / 64 GB / no GPU on `foscsmlprd01`; stdout is 0 bytes and stderr 1191 bytes. The cause is a self-contradictory static contract, not data, model, resource, environment or SLURM: `c04_a0t_small_v1_v5_preflight.py:152` requires `prompt_hashes() == cfg["prompt_hashes"]` while `configs/c04/c04_a0t_small_v1_v5.json:115-120` still carries `PENDING_CPU_PREFLIGHT_HASH_FREEZE` for all four keys, and materializing exactly those hashes is what this preflight exists to do, so the freeze run can never pass its own gate; the v5 producer asserts the same equality at line 177, so the defect is in the shared v5 contract rather than one entrypoint. Fail-closed behavior held: `artifacts/c04/` does not exist, nothing was staged or partially published, no train ASR/video/model byte was read, no label value was materialized, and no teacher or GPU ran. **No metric, result, decision or CONTINUE/KILL verdict was published; this consumes no scientific gate and is no evidence for or against SPaSH-Tensor.** The termination went unrecorded for ~2h24m while the state files still claimed `READY_NOT_SUBMITTED`. The v5 pair is retired as unrunnable and may not be resubmitted; repair requires a fresh versioned v6 closure and fresh independent review before any new submission.
- **Iteration 8 Stage-0 bounded-extraction amendment · 2026-07-30 · REGISTRY RULE CHANGE, target unmet:** the user ruled that extraction is authorized (可以去抽), generalising the one-off bounded-teacher exception already granted to C04. `unified_pilot_gate.stage_0_reachability` may from now on be adjudicated **either** on an existing representation bank **or** on a bank produced under a bounded extraction budget of **≤4.0 GPU-hours per candidate**, and only if: (a) the view definitions, arms, controls, arena, metrics, decision rule and every threshold are hash-frozen and independently GO(0C/0H/0I)-reviewed **before any extraction job is submitted** — without this the extraction is test-driven selection; (b) extraction covers **train + dev_seen only** and the loader's test-like path rejection stays asserted in code; (c) the **+0.050 accuracy / +0.050 macro-F1 two-dataset bar is unchanged**, net-fix clause included; (d) **no other hard_constraint is relaxed** (no OCR, no cross-dataset mixing, no external API, single-dataset train split, parent-video binary label only, no ensembles, no size scaling, Slurm-only); (e) serial execution and the parallel-GPU-pilot ban are unchanged and a bounded extraction counts as a GPU pilot; (f) real GPU wall time is measured by `sacct`, reported, and an over-budget run is void; (g) **F113 stands** — a raw-key arena may KILL but may not promote, so a Stage-0 PASS must be rendered on the fold-head/deployed-head path. The 4-hour cap is priced off measured single-GPU extraction jobs (8 CPU / 1 GPU / 48-64 G): `13295` 00:24:23, `13329` 00:27:18, `13302` 00:34:37, `13648` 00:46:27, `13352` 00:59:57, `13470` 01:01:44, and the largest ever observed, `13468 gen_embed_readout` 02:00:08. **What this unblocks in principle:** C02 immediately, and C05/C06/C09/C10/C11/C12 are the candidates most likely to hit the identical wall — 99 top-level `train_*.pt` caches exist (HateMM 30, MHC 26, MHC-ZH 41, ImpliHateVid 2, counted 2026-07-30) and every one of them varies image pooling, attention/readout span, LoRA weight point or PEFT merge path, never the text channel. **C03 is deliberately NOT covered:** it must *train* native policy-MNTP before it could extract anything, a different order of commitment that a 4-GPU-hour extraction budget does not price; it stays `killed_design_infeasibility_frozen` pending a separate user decision. The amendment is a procedural rule, not evidence: it predicts nothing about any candidate and no extraction, GPU, teacher, Python, cache open or test access occurred in this update.
- **Iteration 8 C02 revived under the amendment · 2026-07-30 · PREREGISTRATION REOPENED, target unmet:** C02 `Evidence-Density Quotient Geometry` moves from `killed_design_infeasibility_frozen` to `revived_under_stage0_bounded_extraction_amendment_a0_prereg`. The 2026-07-29 `KILL_C02_DESIGN_COLLISION_OR_INFEASIBILITY` is **retained verbatim as historical evidence and is not overturned on its own terms** — it ruled a sequencing/evidence-availability failure under the then-frozen pre-extraction gate and said in writing that it was not a scientific refutation. Its two genuine scientific objections had already been repaired before it was issued (P3 image-pooling proxy → orbit defined on the native text channel with representation-matched views; unsafe evidence-core deletion → the complete native transcript is retained as an ordered subsequence and only controlled repetition is added), and every reviewer requirement it left standing is carried forward into the new A0: `RANDOM_WINDOW_REPEAT`, `MIN_WINDOW_REPEAT`, `REPEAT_ONLY`, `LOCALIZED_REPEAT_ONLY`, frozen orbit radius, frozen KRR length metric, retrieval-length correlation, frozen confidence/control thresholds, declared lambda-selection status, declared Holm family, full self-orbit exclusion, and explicit fail-closed handling of examples whose transcript orbit is the identity. Revival is procedural: it authorises pre-registration and independent review only, and is no evidence for the hypothesis.
- **Iteration 8 C04 implementation-v6 CPU-preflight repair · 2026-07-30 · GO_0C_0H_0I_JOB_13840_SUBMITTED, target unmet:** v6 repairs the single ordering/contract defect that killed job 13805 and nothing else. `resolve_prompt_hashes(cfg, freeze_stage)` replaces the unconditional equality assertion: the `PENDING_CPU_PREFLIGHT_HASH_FREEZE` sentinel is accepted only on the CPU-preflight freeze run, only for the four prompt-hash keys, and only when `preflight_materialization_authorized` is exactly `True`; any other value, a mixed pending/frozen state, a foreign key set, or the sentinel on any non-freeze path HALTs. The computed hashes are written to a literal freeze artifact and `assert_literal_prompt_hashes` binds the preflight manifest, the artifact, the producer and both GPU-ledger validators, so no downstream stage can read the sentinel. Two review findings proved more important than the original bug because the first repair had *displaced* the impossibility rather than removed it: `config_contract_sha256` hashed `prompt_hashes` verbatim, making the pre-freeze and post-freeze configs mutually unsatisfiable and priming the single authorized GPU allocation to be burned before the producer's HALT; and a claim-time HALT ran the GPU wrapper's `EXIT` trap, whose `mark_exit` bumped the genesis ledger even with an empty job list, breaking the resource ticket's pin inside a no-clobber namespace. Both are closed, and a self-test fixture now asserts the config contract is invariant across the freeze while still moving under tampering. All three wrappers, all three sbatch files and all five schemas are byte-identical to v5 modulo the version rename; no scientific constant, authorization flag or guard changed in meaning. Independent static review ran nine fresh-reviewer rounds — five code/resource (`REVISE 0C/2H/2I` → `REVISE 0C/1H/2I` → `REVISE 0C/1H/2I` → `GO 0C/0H/1I` → `GO 0C/0H/1I`) then four unlock (`GO 0C/0H/4I` → `REVISE 0C/1H/2I` → `GO 0C/0H/3I` → `GO 0C/0H/0I`) — each independently recomputing every hash. Unlike v1-v5, every CPU-preflight gate was also executed read-only on the login node against the exact authorized bytes before submission, all passing, including both model tree hashes and `label_value_materialized=0` on both datasets; that check's absence is precisely what let 13805 reach an 8-hour queue and die on its own first gate. Job `13840` was submitted exactly once at `2026-07-30T21:41:35` and is `PENDING (JobHeldUser)` awaiting automatic release. **No metric, result, decision or CONTINUE/KILL verdict is published; this consumes no scientific gate.** Teacher, GPU, small tranche, reconciliation, dev/test, OCR, API/network, labels, chain, release and resubmit all remain false, and a fresh independent payload-hash review is required before anything downstream.
- **Iteration 8 C02 A0 preregistration · 2026-07-30 · FROZEN + GO (0C/0H/0I), READY NOT SUBMITTED, target unmet:** under the Stage-0 bounded-extraction amendment, C02's A0 was designed, hash-frozen and independently reviewed **before any extraction job was submitted**, which is amendment condition (a) and the whole point of the rule. The object: each video carries a discrete orbit of controlled evidence-density views of its own text channel (`NAT`; `RFULL` = T+' '+T; `RW1..RW4` = window k duplicated in place at the frozen quarter cuts `c_k=(k*len(T))//4`), and the A0 asks whether the induced quotient similarity `s_Q(i,j)=max_{a,b} cos(z_i^a, z_j^b)` clears **+0.050 acc AND +0.050 macro-F1** over the paired native floor on **both** HateMM and MHC-ZH, in the **deployed head key space** (fold-head arena, 5 frozen folds asserted against the banked `vsw_ckpt`, 3 head seeds, bank = fitting pool, queries = the held-out fifth; raw arena computed but confined to corroborating a KILL per F113). The 2026-07-29 reviewer's two scientific repairs are what the design implements: the orbit is defined on the **native text channel** with representation-matched extracted views (no P3 image-pooling proxy), and **every view retains the complete native text as an ordered subsequence**, adding only controlled repetition — proved per item per view before any forward pass, and the checker is itself tested against a deletion. All four named controls are present as sub-orbits of the single extraction (`REPEAT_ONLY`, `LOCALIZED_REPEAT_ONLY`, `RANDOM_WINDOW_REPEAT`, `MIN_WINDOW_REPEAT`, plus `MAX_WINDOW_REPEAT` secondary), with `SHUFFLE` and `NOISE` as the mechanism controls; the **primary treatment uses no P3 at all** and P3 enters only the MIN/MAX controls, whose window correspondence is a declared positional approximation. **Seven freezes, six fresh independent review rounds** (`REVISE 0C/4H/20I` → `REVISE 0C/2H/19I` → `REVISE 0C/4H/23I` → `GO 0C/0H/23I` → `GO 0C/0H/4I` → `GO 0C/0H/3I` → **`GO (0C/0H/0I)`**), each with a reviewer that had not seen the implementation reasoning. Three findings changed the science, not the prose: (i) the `SHUFFLE` control was measuring leakage rather than orbit membership — twice — and is now **displacement donation**, `z_i^v := NAT_i + (view_v(pi(i)) - NAT_pi(i))`; (ii) the `net_fix_rate` conjunct is **algebraically identical to delta_acc** (`fixed - broken = n*delta_acc`), so the registry's net-fix clause is discharged by the accuracy bar and is no longer dressed as an independent gate; (iii) the claim that `s_Q` **upper-bounds** what any orbit-contracting representation could buy is **RETRACTED** — it is one particular orbit-invariant similarity, so a KILL is a gate verdict under the frozen Stage-0 rule, not an impossibility proof. Two engineering defects were caught before any GPU spend: an exhaustive faiss search is not bit-equal to the deployed k=20 call (max |delta sim| 1.5e-07, would have broken PARITY-NAT), and `mechfix_ops._norm32` can alias its input. **No extraction, GPU, teacher, cache, label or test access has occurred; no result, decision or verdict exists.** Records `refine-logs/C02_A0_{,V2_,V3_,V4_,V5_,V6_,V7_}RECORD.md`, reviews `*_PREREG_REVIEW.md`, state block `iteration_8_c02_a0_v7_preregistration`.
- **Iteration 8 C02 A0 ERRATUM (self-detected, pre-submission) · 2026-07-30 · CLAIM RETRACTED, no result affected:** the preregistration's stated justification for searching `k = topk` per view pair asserted that **an exhaustive `k = n_bank` faiss search is NOT bit-equal to the deployed `k = 20` call**, citing a measured `|delta sim| = 1.5e-07`. **That is false and the number was misattributed.** Re-measured on synthetic arrays with private, singly-normalised operands: the exhaustive path **IS** bit-equal — similarities, neighbour ids and votes all identical, **max |delta sim| = 0.0** — and the `1.4901161193847656e-07` figure belongs **entirely** to the `mechfix_ops._norm32` **aliasing** defect (operands normalised twice), which is a separate, real and separately recorded finding. Cause: both defects were live in the same first dry run; I changed the search width, saw the discrepancy persist, then found and fixed the aliasing bug, and never re-tested the exhaustive path with the aliasing fix in place — a **fabricated companion measurement** in the exact sense the project's numeric-provenance rule prohibits. Six independent review rounds did not catch it because each was asked whether the `k = topk` exactness **argument** holds — it does, and each verified it — while the false part was the empirical claim standing beside a correct argument. **Nothing numerical changes:** `k = topk` is retained on the reasons that are actually true (provably sufficient for the top-topk, `O(topk)` instead of `O(n_bank)` per view pair, and **literally** the deployed call for a singleton orbit rather than merely equal to it). The retraction is frozen into `configs/c02/c02_a0_v8.json::oracle.search_width_erratum`, the v8 arena docstring and `refine-logs/C02_A0_V8_RECORD.md`; `C02_A0_RECORD.md §9` item 1 and `C02_A0_V2_RECORD.md §4` item 1 are corrected by reference. **No extraction, GPU, result, decision or verdict existed at any point in this episode**, so no measurement is affected — only a frozen justification.
- **Iteration 8 C02 A0 v8 erratum re-check · 2026-07-30 · GO (0C/0H/1I), READY TO SUBMIT, target unmet:** the confined re-check confirmed the eight v8 hashes, that the retracted search-width claim survives nowhere but inside its own retraction, that the exactness argument is unchanged and **never depended on the retracted claim**, and that `PARITY-NAT` still binds. One Info is **declared open, not fixed**, on the reviewer's explicit advice: `O(topk) rather than O(n_bank) per view pair` is false under the natural reading — a flat IP index computes all `n_bank` similarities regardless of `k`, which bounds the selection heap and the `(nq x topk)` result, not the scan — but no number the run produces depends on it and the other two reasons for `k = topk` over-determine the choice. At my request the reviewer also built an **unverifiable-claim register**: every statement in the frozen set asserted as *measured* that a static reviewer cannot re-derive. It converted three previously-trusted claims into verified ones (MHC-ZH whitespace counts, the fourth `max_chars`, the banked `B_fid` figures read from `headspace_fidelity{,_zh}_OUT.json`) and identified the **largest residual risk as the GPU-hour projection**, not any science claim: the forward count is derivable (independently re-derived ~8758) but the wall clock is an estimate, nothing in-job enforces the 4.0-hour cap, and an overrun **voids** the result under amendment condition (f). That is now a monitored quantity to be reported from `sacct`.
- **Iteration 8 C02 A0 v9 · 2026-07-30 · GO (0C/0H/0I), READY TO SUBMIT, target unmet:** the ninth freeze makes exactly three changes and reaches the literal token amendment condition (a) requires. (1) The last Info closed: `O(topk)` rather than `O(n_bank)` was **false of the scan** — a flat inner-product index computes every inner product regardless of `k`, which bounds only the selection heap and the `(nq x topk)` result width; reworded, with the two load-bearing reasons for `k = topk` untouched. (2) **The 4.0 GPU-hour cap is now enforced in-job**, which the v8 register had flagged as the largest downside in the whole set: condition (f) voids an over-budget run and nothing enforced it, while CLAUDE.md forbids `--time`. One absolute deadline (`14400s - 600s margin`) is computed at job start and passed to both dataset invocations; `budget_check` refuses to START work needing more than `max(2 x slowest item so far, 60s)` of headroom; a breach publishes an accounting-only `BUDGET_BREACH_<dataset>.json` and exits **5**. **The guard may only ever stop work** — every write is outside the guard window, so a breach leaves the in-progress dataset entirely unwritten and any completed dataset intact, and the A0 then fails closed on the missing manifest rather than reading a truncated bank; the reviewer verified this by tracing all call sites against all write sites. (3) **The measured-claims register is adopted** (claim / how obtained / what depends on it / statically re-derivable), the instrument that came out of the v8 erratum. The reviewer audited it and independently re-derived every `[V]` row, including the forward count at **exactly 8758**; no `[U]` proved statically re-derivable, no `[V]` failed, and nothing asserted-as-measured was missing. Constants sweep clean, with an independent structural proof that only the docstring moved (every round-8 line anchor shifted by exactly +3 while the constants block stayed put). **Nine freezes, eight independent review rounds.** Still no extraction, GPU, result, decision or verdict.
- **Iteration 8 C02 Stage-0 extraction SUBMITTED · 2026-07-31 · job 13843, no result yet, target unmet:** the single authorized extraction submission was made at `2026-07-31T05:13:25+12:00` after the v9 freeze reached `GO (0C/0H/0I)`. All three preconditions were performed **in the same step as `sbatch`**, none earlier: `sha256sum -c` over the seven v9 frozen files **7/7 OK**; `squeue -u jehc223` **EMPTY**, verified directly rather than second-hand, satisfying amendment condition (e); and `artifacts/c02_edq` absent with zero `*-c02den-*` caches. The blocker, `13840 c04_a0t_small_v1_v6_preflight`, was **COMPLETED 0:0 in 00:00:19** after being held `JobHeldUser` for **7h30m** — a 19-second CPU job — which sets the expectation for 13843's own hold and is the reason it must not be force-released. Job 13843 is `PENDING (JobHeldUser)`. **No extraction has run, no cache has been written, and no result, decision or verdict exists.**
- **Iteration 8 C04 v6 CPU preflight job 13840 · 2026-07-31 · COMPLETED_PAYLOAD_FROZEN_PENDING_PAYLOAD_REVIEW, target unmet:** the repaired preflight ran and passed. `sacct`: `COMPLETED`, exit `0:0`, submitted `2026-07-30T21:41:35`, started `2026-07-31T05:11:31`, ended `05:11:50`, elapsed `00:00:19`, 8 CPU / 64 GB, `ReqTRES billing=8,cpu=8,mem=64G,node=1` — no GPU ever requested; queue split `JobHeldUser` 1h50m45s then `Priority` 5h39m11s, auto-released, never forced. stderr 0 bytes; stdout 1399 bytes reporting `all_passed: true` over 25 checks. The 13805 ordering defect is closed **in both directions** in the real SLURM environment — sentinel accepted on the authorized freeze run and rejected on the non-freeze path, without materialization authorization, on a wrong value, on a mixed pending/frozen state and on a foreign key set, with the post-freeze guard rejecting sentinel and wrong value while accepting the literal, and the config contract invariant across the freeze yet still moving under tampering. Independent recomputation of the payload (not trusting `all_passed`) confirms it well-formed: 15 files / 5,178,606 bytes, no residue; `prompt_hashes.json` holds the four literal hashes byte-identical to those in 13805's stderr with no sentinel in any key; all 14 `staged_output_hashes` match disk, the 15th file being the manifest itself which cannot list its own hash by construction; the access ledger shows only 2 train-ASR opens and 400 train-video hashes with `label_value_materialized=0` against 1323 label fields syntactically skipped and no dev/test token; map byte-lengths equal the declared 256x3598 and 256x1024 float32 shapes with all entries ±0.0625 and every payload reproducing from the frozen generators; allowlists are 200 rows per dataset with recomputing digests and merkle roots; and the resource state is untouched — ledger `GENESIS_UNCLAIMED` revision 0 with zero jobs, ticket unconsumed with 0 GPU seconds, and no claim, entry marker, seal or checkpoint artifacts. **No metric, result, decision or CONTINUE/KILL verdict is published; this consumes no scientific gate and is no evidence for or against SPaSH-Tensor.** A passing preflight authorizes nothing downstream: teacher, GPU, small tranche, reconciliation, dev/test evaluation and chained submission all remain false, and the next legal step is an independent payload-hash review pinning `preflight_manifest_sha256` `06bf6b38f424dd53d142367abd029dfa1f485380fb1482d72beabb7f5943ad1a`. No further job was submitted; the queue holds only C02's `13843`.
- **Iteration 8 C02 Stage-0 extraction job 13843 · 2026-07-31 · EXTRACTION COMPLETE AND VERIFIED, no A0 yet, target unmet:** the first of the two authorized submissions ran and passed. `sacct`: `COMPLETED`, exit `0:0`, submitted `05:13:25`, started `07:31:04`, ended `09:59:19`, elapsed `02:28:15` on 8 CPU / 1 A100-SXM4-80GB / 64 GB at `foscsmlprd01`; the `JobHeldUser` hold auto-released after `02:17:39` and was never forced. **Amendment condition (f) HOLDS and the result is not void: 8895 s = 2.4708 GPU-h of the 4.0 GPU-h cap, 61.8 % used, 1.5292 GPU-h of headroom** (the wrapper's own `elapsed 8894s of the 14400s cap` runs 1 s under `sacct`, whose figure is the one reported). The in-job budget guard **never fired** and no `BUDGET_BREACH_*.json` exists; it used `SLURM_JOB_START_TIME` rather than the fallback (`job start 1785439864` = `sacct Start`), its deadline was `11:21:04 NZST`, the job finished **1 h 21 m 45 s early**, and its largest requirement all run was `max(2 x 19.38, 60) = 60 s` against a minimum remaining of `4906.9 s`. **24/24 view caches and 2/2 manifests** exist and all **24 manifest sha256 were recomputed against disk: 24 OK, 0 mismatch**; both manifests parse and pin `this_script bb698ab9…` / `view_module 44fbb00b…` equal to the v9 frozen hashes. Per split: `HateMM/train 744/4197/261/48 deg, support 0.935484, 1 zero-guard` · `HateMM/dev_seen 107/589/53/10 deg, 0.906542, 0` · `MHC_zh/train 579/3473/1/0 deg, 1.0, 0` · `MHC_zh/dev_seen 78/468/0/0 deg, 1.0, 0`, with degeneracy causes `EMPTY_TEXT 39 + LENGTH_GUARD 9` and `9 + 1` exactly as register rows 2-3 predicted. **The single decode failure is the SAME item as the known structural null, not a new one:** `hate_video_95.mp4` failed decord then PyAV, took the frozen zero-vector guard path (`n_forward=0`, `zero.clone()` into all six slots), and is **HateMM train row 355, label 1** — the null cited in `C01_ZERO_CONTRACT_PROBE.md` and `PROVENANCE_AUDIT_2026-07-28.md:187-193`. MHC-ZH had **40 decord failures, all recovered by PyAV**, zero zero-guards. A read-only census of the two banked train caches and twelve train view caches (no dev_seen, no test) pre-verifies zero-contract criteria 2 and 3 — HateMM zero mask `[355]` in banked text, banked img and all six views, MHC-ZH empty throughout, no non-structural tiny rows — which **upgrades measured-claims register row 12 from `[U]` to MEASURED, confirming 1 on HateMM train and 0 on MHC-ZH**. Two register reconciliations, recorded as annotations rather than silent corrections: **forwards `8727` vs the re-derived `8758`**, a shortfall of exactly **31** = 6 (the zero-guard item forwards nothing) + 19 (declared `EMPTY_WINDOW` view-level identities: 17 over 14 items on HateMM train, 1 on HateMM val, 1 on MHC-ZH train) + 6 (exact `RW`-vs-`RW` view-string collisions on six music-note-only transcripts, none colliding with `NAT`), i.e. `8758` was an upper bound that did not model two mechanisms the design itself declares, and the run came in **cheaper** than projected; and **`view_support` is NOT a mismatch** — row 5's text-only `0.9355/0.9065/1.0/1.0` is exact against the manifest and row 6's runtime-gate `0.9341`/`1.0000` is exact against the arena's own `degen_mask = degen_text | degen_zero`, checked directly (`degen_text=48, degen_zero=1, overlap=0` on HateMM; `0, 0` on ZH), both far above `VIEW_SUPPORT_MIN=0.60`. **The `MHC_zh/train copied=1` was NOT the N2 decode-failure condition:** it is item `BV1oK41127n4`, native text `基佬紫` (3 chars), whose empty first quarter triggers the declared `EMPTY_WINDOW` rule so `RW1 := T` and the dedup copies the `NAT` vector — and with `degen_mask` all-False on MHC-ZH the N2 singleton case is *unreachable* there, the degenerate donor class being empty rather than a singleton. **No A0 was submitted at the time of this entry and no metric, delta, decision or CONTINUE/KILL verdict exists; the extraction is an input and is evidence for C02 in neither direction.**
- **Iteration 8 C02 Stage-0 A0 job 13847 · 2026-07-31 · KILL_C02_DENSITY_ORBIT_UNREACHABLE, target unmet:** the second and final authorized submission ran to a real verdict. `sacct`: `COMPLETED`, exit **`0:0`** (not the fail-closed `3`), elapsed `00:29:49`, 8 CPU / 0 GPU / 32 G; the result carries no `halt` key and `result_exists: true`, so **this is a scientific verdict, not a validity-guard HALT**. Submit-time preconditions ran in the same step as `sbatch`: `sha256sum -c` **8/8 OK** (seven v9 executables plus `C02_A0_V9_RECORD.md`) and `squeue` **empty**; the v9 record was deliberately not edited to fill its §6, because it is hash-pinned at `20da7533…`. **Measured, quoted from `C02_A0_DECISION.json`: HateMM `delta_acc +0.0008960573476701761`, `delta_mf1 +0.0005790634953705132`; MHC-ZH `delta_acc -0.0011514104778355128`, `delta_mf1 -0.0027456813390588364` — against a `+0.050 / +0.050` bar on both.** Bootstrap 95 % CIs `[-0.008961, +0.010753]` and `[-0.013241, +0.011514]`; **0 of 4 Holm nulls rejected** (`p` = 0.4425 / 0.4509 / 0.5802 / 0.6414 at `alpha = 0.05`); on MHC-ZH `FULL` fails to beat `SHUFFLE` or `NOISE` on all four comparisons. Per-seed `delta_acc` straddles zero on HateMM (`-0.006720 / +0.009409 / 0.000000`) and is non-positive on MHC-ZH. **HateMM reached 1.8 % of the accuracy bar and MHC-ZH is on the wrong side of zero: a null result, not a marginal miss.** The KILL rests on a measurement the design certifies — **all five validity gates passed on both datasets**: `GATE-FID` `B_fid` `0.0093 / 0.0086` vs the 0.050 stop rule (identical to the banked register-row-7 values, `STOP_RULE_TRIGGERED false`); `GATE-EXT` `median_cos 1.0` and `max_abs_diff` **0.0**, i.e. the re-extracted `NAT` is **bit-identical** to the banked native text features so the paired floor is exact; `PARITY-NAT` bit-equal on all 15 seed x fold cells per dataset; `ARENA-2` native accuracy `0.8858-0.8884` and `0.8895-0.8946` against majority rates `0.5995` and `0.6891`, neither saturated nor collapsed; `VIEW_SUPPORT` `0.93414 / 1.0`, exactly the values precomputed from the banked masks before submission; `ZERO_CONTRACT` criteria 2-3 held with `banked_text_zero_rows [355]` (`hate_video_95`, label 1) and empty on ZH, the structural-null sensitivity read moving HateMM `delta_acc` only to `0.0008972633467922986`. **The secondary raw arena corroborates the KILL** — the only direction F113 permits, and `raw_preds` never reached the decision: HateMM `FULL +0.006720` with the largest raw delta in any arm `+0.012097`, MHC-ZH every arm at or below native. **The mechanism diagnostics say why:** `orbit_radius_median_oof` is only **0.000466 / 0.000383 / 0.000415** (HateMM seeds 0-2) and **0.000217 / 0.000203 / 0.000176** (MHC-ZH) in the deployed head key space, largest per-view being `RFULL` at `0.001727 / 0.001289` at seed 0, so duplicating a transcript barely moves the head key at all; `FULL` changes just **22-29** predictions of 744 / 579 with `net_fix` per seed `-5/+7/0` and `-1/0/-1` against the ~`+37` HateMM would need, and precision on changed items is **0.5040 / 0.4881**, i.e. chance; mean top-20 overlap with native is only `5.32-5.47` and `6.41-6.59` of 20, so the orbit reshuffles three quarters of the neighbourhood label-uninformatively; and what it *does* add is a **length artifact** — retrieval-length Spearman rises NATIVE->FULL on every seed, `0.4808/0.5316/0.4909 -> 0.7784/0.7869/0.7962` (HateMM) and `0.3797/0.3821/0.3584 -> 0.7356/0.7280/0.7377` (ZH), while the OOF KRR length probe on native keys stays near zero throughout (`R^2 = 0.0880/0.1071/0.0879` and `-0.0034/+0.0140/+0.0072`), so the correlation is introduced by the orbit max rather than inherited. `NOISE` collapses bit-identically onto `NATIVE` on both datasets: norm-matched random displacements never win the argmax at these magnitudes. **Two honesty notes.** `MIN_WINDOW_REPEAT` (`+0.004928`) outscoring `MAX_WINDOW_REPEAT` (`-0.002240`) on HateMM is **not** "anti-core beats evidence-core": both arms select largely the same window (`min RW1 634/744`, `max RW1 572/744`; `571/579` and `541/579` on ZH), the declared P3-to-text-window positional approximation having degenerated onto window 1. And `n_singleton_class_groups_dropped_head_arena` is **0 on both datasets**, so the v5->v9 **N2 singleton-DROP path never fired**, settling that open question empirically. Register row 19 is discharged by the run: both fidelity JSONs emit `raw_effect_under_test 0.0255`. **Scope: this kills the C02 Stage-0 oracle under the frozen Stage-0 rule and closes the candidate; it is NOT an impossibility proof for density invariance — `s_Q` is one particular orbit-invariant similarity, not a proven supremum over all orbit-contracting representations, exactly as the arena's own `interpretation_boundary` states. That stronger reading was overclaimed once and retracted in the v8 erratum and is not reintroduced.** Total C02 spend: **2.4708 GPU-h of the 4.0 authorized** plus a 29-minute CPU job; both authorized submissions are used; the +0.030/+0.030 two-dataset target remains unmet and the serial loop advances.
- **Iteration 8 C04 v6 payload review · 2026-07-31 · GO (0 Critical / 0 High / 3 Important), eligible only, target unmet:** a fresh independent reviewer with no exposure to the v6 implementation reasoning inspected the payload frozen by job `13840` and verified it by recomputation rather than by reading the job's own `all_passed`. The prompt-hash freeze is correct and the `13805` failure mode is closed as a contract: all four literal hashes reproduce from the frozen prompt sources (`system 1ffc0675…`, `A cecb3555…`, `B 9521bee7…`, `combined a42268e4…`), `payload_sha256 eb485b9a…` is self-consistent, the key set is exactly `{A,B,combined,system}`, and across the whole 15-file namespace the sentinel string occurs only in `prompt_hashes.json` and only as the value of `pending_sentinel_token`. The allowlists are exact: the 200+200 ID sequences reproduce from the frozen selection rule with contiguous ranks, every `selection_sha256` recomputed, strictly ascending `(digest,id)`, both merkle roots recomputed, and **zero** dev/test contamination on both datasets (selected ∩ dev = 0, selected ∩ test = 0, selected ⊆ train). All 400 selected videos were re-hashed against disk — 3,430,759,978 bytes read, **0 mismatches** on sha256/size/device/inode/paths, plus all 400 transcript hashes, scalar counts and languages. The access ledger records 402 events (2 train-ASR opens + 400 video hashes), `label_field_syntactically_skipped 1323` = 744+579 with `label_value_materialized 0`, and no dev/test/val path component. Both dense JL payloads rebuild **byte-exactly** at exactly `256×3598×4 = 3684352` and `256×1024×4 = 1048576` bytes with every float32 at ±0.0625, and all four role maps rebuild byte-exactly; the config's "documentation-only" `maps.*` geometry declarations were checked against the module constants and the materialized bytes and agree on every count. Resource state is clean: ledger `GENESIS_UNCLAIMED`/revision 0/zero jobs/zero seconds/cap 7200 s (the amendment's 2 GPU-hour first-tranche ceiling), ticket unconsumed with watchdog `7080 = 7200−120` and genesis pin equal to the ledger file on disk; independently, `sacct` shows the only two C04 jobs ever (`13805` FAILED, `13840` COMPLETED) and **neither carries `gres/gpu` in AllocTRES**. The normalized config contract `2b66775c…` was recomputed independently, agrees in all four in-payload copies, is invariant under the sentinel→literal fill-in, and still moves under tampering. Self-test honesty was probed: 13 + 7 fixtures + 5 added by `run_self_tests` = exactly the 25 reported, and four checks were confirmed non-vacuous (including that a sentinel-bearing payload with a *recomputed valid* `payload_sha256` is still rejected). The three Importants: **I-1** the HateMM identifier is itself label-bearing (`hate_video_*`/`non_hate_video_*`), so the sealed "ID-only" allowlist gives no label containment for HateMM — though the *selection* is proven label-blind by exact reproduction, the draw is demonstrably unengineered (78/122 = 0.390 vs a train prior of 298/446 = 0.401), and the id never reaches the teacher (`producer.py:768` interpolates only `{transcript}`); **I-2** `selection_deterministic` is a tautology, so the quoted "25 checks" overstates coverage by one; **I-3** the amendment's 8 GPU-hour aggregate C04 ceiling is encoded in no machine-checked artifact and is enforced by prose only. **A GO here authorizes no work.** It means only that the payload is well-formed and faithful, making C04 *eligible* for a separately-authorized teacher small tranche. All GPU work stays blocked pending explicit main-dialogue execution authorization; dev/test teacher is forbidden forever under the amendment; and this markdown verdict is **not** the machine-checked authorization — `verify_payload_review` still needs a `C04_A0T_SMALL_V1_V6_PAYLOAD_HASH_REVIEW.json` under schema `c04_payload_review_v6`, a 64-hex config pin replacing the `PENDING_CPU_PREFLIGHT_AND_PAYLOAD_REVIEW` sentinel, and an `attested_closure_sha256` over the `C04-PAYLOAD-REVIEW-GO-v6` domain, pinning `preflight_manifest_sha256 06bf6b38f424dd53d142367abd029dfa1f485380fb1482d72beabb7f5943ad1a`. No metric, result or CONTINUE/KILL verdict is published; the `+0.030/+0.030` two-dataset target and the amendment's full-bank `+0.050/+0.050` gates are untouched. Review: `refine-logs/C04_A0T_SMALL_V1_V6_PAYLOAD_REVIEW.md`.
- **Iteration 8 Gate-0 reopen after two consecutive failures · 2026-07-31 · REGISTRY ADJUDICATION, no computation, target unmet:** the `fast_fail` clause fired (C01 `KILL_CURRENT_ENDPOINT_ROUTE_ONLY` job `13738`; C02 `KILL_C02_DENSITY_ORBIT_UNREACHABLE` job `13847`) and Gate 0 was reopened over the post-C04 backlog. The advisory package `refine-logs/C05PLUS_FORENSIC_RECON_2026-07-31.md` recommended striking seven of ten candidates; it was **adjudicated, not adopted**, through four independent verification passes (a from-scratch re-measurement of every `[M]` claim on the six gt label files, plus three adversarial quote/scope audits by workers with no exposure to this reasoning), with the C01 arm table re-derived directly from `C01_A0_OUT.json`, then through **fourteen** rounds of fresh independent review closing at **`GO (0 Critical / 0 High / 0 Important)`** — `REVISE (1C/3H/10I)`, whose Critical moved C07 out of the strike column; `REVISE (0C/3H/7I)`, which found four round-1 repairs had landed in the narrative record but not in the machine-readable state block; `REVISE (1C/1H/4I)`, whose Critical moved C08 after locating source annotation files that refute its premise 1; `REVISE (0C/2H/2I)`, which confirmed all thirty prior findings applied on all four surfaces; a fifth `REVISE (0C/2H/2I)`, which re-based C13 on its own registry text after finding the arithmetic leg tag-scoped; a sixth `REVISE (0C/1H/2I)`, which established that C13's remaining basis is a proponent-satisfiable precondition and moved it to hold as well; a seventh `REVISE (0C/2H/3I)`, which confirmed **no hold is over-cautious** and downgraded the counter-text held against the promoted candidate; an eighth `REVISE (0C/2H/4I)`, an exhaustive cross-surface sweep that changed no disposition; a ninth `REVISE (0C/1H/3I)`, which found three of round 8's repairs had not landed and corrected a false provenance attestation; a tenth `REVISE (0C/1H/3I)`, which re-derived the measured layer independently at **18/18 claims matching exactly** and found only citation-precision defects; an eleventh `REVISE (0C/1H/2I)`, which recomputed the C01 arm table across all 14 arms x 2 datasets and marked one inherited attribute as inferred rather than sourced; a twelfth **`GO (0C/0H/2I)`** — the first GO — which cleared all four scope items explicitly, including that **no inference is recorded as a measurement** and **no hold is over-cautious**; a thirteenth **`GO (0C/0H/1I)`** which cleared them again; and a fourteenth **`GO (0C/0H/0I)`** — zero findings at any severity, from a reviewer running four independent fact-check passes, whose verdict on the dispositions was *"No hold should have been a strike, and no unblock is vacuous"*. **The recon's measurement layer is sound — every `[M]` figure reproduces and no fabricated number was found; the two apparent numeric discrepancies (markup p90, HateMM median) turned out to be percentile/median *convention* differences the recon applied consistently, not errors** — **but SIX of its seven strikes rest on a ban, a screen, a premise or a boundary clause read wider than its own text and are DOWNGRADED TO HOLD — exactly ONE survives fourteen rounds of adversarial review, and only because that candidate was already registry-ineligible before the recon was written.** **Strike confirmed (`struck_gate0_2026_07_31`, registry-level and reversible by user ruling, NOT a measured kill):** **C14 alone** — registry-ineligible by construction, `eligible_for_primary_target: false`, and *"Ensemble predictions are forbidden as the final method, and a positive diagnostic cannot satisfy novelty"*, reinforced by `hard_constraints`; its diagnostic-only role is preserved, its unblock is a user ruling and nothing else, and the recon's TVB "7 of 7 at ~0" support is *not* relied on, being a **prediction** rather than a measurement. **C13 was struck in revisions 2-5 and is now held**: rounds 4, 5 and 6 successively dismantled each basis offered — the plausibility argument is an *inference* about C13 rather than a measurement of it; the substrate arithmetic was measured for HTML **tags** only while C13's claim says *"HTML/title markup"* and MHC-EN carries entities on `64`+`9` rows; and the surviving registry-text basis (its claim self-scopes to *"a ZH-specific extraction nuisance"* and its boundary conditions any two-dataset route on *"a genuinely cross-dataset mechanism"* the entry never names) is a **precondition a proponent can satisfy**, which this reopen's own C07 ruling holds cannot carry a strike. **Downgraded to HOLD with the gap named:** C07 → `held_lattice_delta_unwritten_reachability_unscreened` (**the review's Critical**: its "written mathematical delta" precondition is *un-attempted*, not unwritable — unlike C05's, which was attempted and demonstrated unwritable — and **no reachability screen of C07's object has ever been run**, since F82's ban_scope splits *"vote-side Offensive reweighting closed both datasets"* from *"head-side graded auxiliary = F44-capped + admissibility-gated, only revivable by user ruling WITH a new mechanism argument"* and a cone metric is head-side; F82's `EN +0.0250 / ZH +0.0256` is a headwind to price, not a screen); C13 → `held_zh_scoped_no_cross_dataset_pairing_named` (see above); C08 → `held_title_channel_separable_route_unscreened` (**round 3's Critical**: the claim that there is no *separable* title channel "without re-deriving source metadata" is refuted by primary files — `Multihateclip/{English,Chinese}/annotation(new).json` carry a non-empty `Title` on **`891/891`** and **`897/897`** rows whole-file (`629/629` and `657/657` join-scoped to train+val), `prep_mhc.py:72-85` reads title and transcript as *separate variables*, and F88's title-length median presupposes separation, so exposing it is a CPU re-prep rather than a data-collection act and an MHC-EN + MHC-ZH title route exists — with round 4 correcting the channel's pricing, since the *"15 chars"* figure is an MHC-ZH **test-split** median — *likely* markup-stripped, an inference rather than a reading, since neither `ERRPAT:272` nor F88 states the convention and F88 describes the title as *carrying* the markup — and the title median measured per dataset is **51 characters on MHC-EN** (transcript 322) vs **27 raw / 13 stripped on MHC-ZH**, leaving the EN half `3.4x` larger and **unpriced**; premise 2 survives only at *marker* scope, premise 3 is a TIER-2 proxy read the source declines to settle); C10 → `held_eum_preconditions_unmet` (EUM's ban covers it by name, but the "legal unit space is EMPTY" leg is an extension — EUM says "as of this recon" over a three-item enumeration and a rule-based MLLM-free boundary is not in it; the BSY block is scoped to "bank-**ADDITION**" candidates); C11 → `held_thin_evidence_disjunct_unscreened` (its claim is *disjunctive* — "speech-poor **or thin-evidence**" — and the second disjunct is **not empty** on the dataset the recon calls empty: `ERRPAT_MHC-ZH §5.2`, the `[31,76)`-char band holds `11` of `22` core errors in `37` items, 2.0x enrichment, permutation `p=0.0048` — recorded at the same standard as C08's premise 3, i.e. a **TIER-2 CPU-re-mint proxy on the MHC-ZH test split `n=149`, pooled effect only** (class halves `p=0.0506`/`0.0668`), which establishes that the disjunct cannot be dismissed by a whitespace census rather than that it is a live lever; the hold carries an explicit burden after review restored the truncated clause *"No legal unmeasured lever found."* — if a proponent cannot say why representing absence escapes it, C11 should be struck without further measurement); C12 → `held_ban_scope_ambiguous_construction_unnamed` (**the deepest stretch**: two *inference-time* nulls used against a *training-time* boundary that already excludes them, and neither ever computed a cross-version disagreement, which is C12's entire quantity — with `LITSWEEP3_DATA_CENTRIC.md:80`'s "headwind to price, **not a coverage** of this mechanism" cited as a *general form only*, since its own subject is memory-bank curation and its ground is that the null used an MLLM two-vote signal, the side C12's archive-derived statistic sits on; `banned_constraints[5]` treated as settled with the counter-precedent F60/AUG — "MLLM-as-data-generator **admissible**" — unengaged, though F60 is itself gated on the open **D7 generator-role sub-ruling** — D7 proper is `RESOLVED 2026-07-14 (RESOLVED-NEGATIVE)`; it is the *sub-ruling* that is open; and decisively **F55 misread**, its "EN closed at all three levels" being three levels *of the encoder-composition question*, so C12's MHC-EN + MHC-ZH route is arithmetically available after all). **C05 → `held_nonisomorphism_gate_unwritten_as_posed`**: its four comparator quotes all verify FAITHFUL, C05's three stages are identical to Global-R2's, the only free variable is the relation matrix's *source*, and the three enumerated legal sources are individually banned or dead — the status says *unwritten as posed*, not *unwritable*, because that enumeration is not established as exhaustive; unblock = name a source that is not MLLM output, not the gold label-agreement matrix, not the Global-R2 certificate family, and carries measured conditional information over the deployed key. **C06 → `gated_on_zero_cost_falsifier`**: C01's arm table verified exact (the **best of six** matched-block-L2 random rotations matches or beats the real prompt displacement on both datasets — `orthrot_83p8` `0.8692`/net `+3` and `0.8974`/net `+3` against primary `common_displacement` `0.8598`/`+2` and `0.8590`/`0`, with the full rotation spread `0.8505–0.8692` and `0.8462–0.8974`), but the two supporting bans **do not reach C06's object** — F80's ban_scope opens with an *unconditional* on-`MHC_zh` closure (*"extraction-instruction language variations (any language, any stream, either encoder arm) on MHC_zh"*) and attaches the *"without new mechanism"* conditionality only to *elsewhere*, while F70's object is individual readout cells, so the warrant rests on **object mismatch**: orbit geometry is neither extraction-instruction language nor a readout cell and C01's arena is raw dev keys while F113 itself marks the raw-KILL direction **NOT ESTABLISHED**; falsifier = re-run C01's battery in the fold-head arena, minutes of CPU, with the design corrected for the fact that **HateMM has only `-LoRA-curric` ro-caches and MHC_zh only `-LoRA`** (one lineage each, not a matched pair) and that the `ow_` cells confound prompt with readout span. **C09 → `next_active_candidate_post_C04`**, and its promotion is stronger than the recon claimed: F88's numbers verify FAITHFUL (ZH `22` of the 25-item union wrong 3/3 with **nothing at exactly 2/3** and all 12 false negatives 3/3-stable; HateMM `89-93 %`), its Stage-0 costs **zero GPU-h** in the banked fold-head arena (all four `headspace_*.py` and all six `headspace_arena_*_OUT.json` verified present; heads re-mint at ~25-40 s CPU each since `headspace_mint.py:274-281` suppresses checkpoint writes), **label-use is affirmatively LEGAL on two written texts** (`progress.json:25` "Legal attack on selection-locked pools = trained selector/reshaper on train labels only" and `LITSWEEP3_DATA_CENTRIC.md:82`'s ruling that a train-once-apply-globally operator is symmetric and outside law-III) **though under a ruling whose viability premise `LITSWEEP5_COMPLETENESS.md §4(ii)` flags as stale — and that counter-text is itself *downgraded, not vacated*, since §4(ii)'s selector leg rests on the F114-retracted "CLIP LOO 0.998" premise (deployed Qwen heads are `0.9406/0.8915/0.8154`) and `LITSWEEP5` is not among F114's nine corrected records, while its independent train-disagreement leg `0/109`, `0/102`, `0/92` is untouched**, and **kill-risk (iii) is misattributed** — F78 measured nothing, the random-control null is F88 (3), HateMM-only, single-draw, on train-row deletion. **The reopen's main output is a corrected strategic finding: what bounds this channel is CONVERSION, not REACH.** The recon's "three independent ceilings" is withdrawn — the head-space coverage leg and the purity-conversion leg are the same 90-cell `n=78` MHC-ZH-dev proxy-head table, the permutation cap is a strictly dominated sub-case by `LITSWEEP8:200-201`'s own words, the coverage bound bounds **pool expansion only** (F114 names this misuse in advance; the rank-constrained re-ordering oracle is `+0.0780/+0.1123/+0.1876`, closed by *reduction* to the measured-dead AGGNET/VSW family rather than by an under-bar ceiling), and "exchange rate never above 1.17 in 36 cells" is stale (F96 `1.8889`/`2.2353`, F98 `1.8333`, F105 `6.0000`, F112 `2.8889`). What survives, and is sharper: **AGGNET carried an oracle of `+0.1492/+0.1520/+0.2186` — by F98's own words *"by far the largest oracle ceiling any member of this family has ever had"*, with 96-100 % of every deployed error inside its function class — and delivered `+0.0134/-0.0069/+0.0000`** — so a large oracle is no longer evidence *for* a candidate in this channel, it is the precondition every failed candidate already met, and Gate 0 must now screen on demonstrated conversion in the currency `banned_constraints[10]` already names — NET ITEMS against the `22.3/17.4/16.5` it states for `+0.030`, **scaling to** `37.2/29.0/27.5` for `+0.050` — **train-arena** requirements at `n=744/579/549`, so the arena must be stated whenever they are used. **Tally: one strike, seven holds (C05, C07, C08, C10, C11, C12, C13), one gate, one promotion — of the recon's seven recommended strikes exactly one survived fourteen rounds of adversarial verification, and every other collapsed the same way: the evidence offered closed something narrower than the candidate, or named a precondition a proponent could still satisfy. Not because the candidates are promising, but because 'looks unpromising' and 'is closed' are seven candidates apart. Replenishment, not re-ordering, is the backlog's problem.** **Paper material recorded, not lost:** the ZH `<em class="keyword">` markup is **a strong lexical shortcut** in that text channel and is a **collection artifact** — markup-bearing rows hate at **`5x` the no-markup rate** and at **`8x` the rate of rows carrying a harvested keyword *without* the markup** (`10/140 = 0.0714`); **no ranking against other lexical features was computed, so "the strongest" is not established and is not claimed** — train hate rate `0.5802` (141/243) with it vs `0.1161` (39/336) without against a `0.3109` base (val `0.5882` vs `0.1818`, base `0.3590`), 49 keywords over 254 occurrences on train (50/288 over train+val), median markup fraction `0.0000` but p90 `0.5051` (`lower`) / `0.5071` (`linear`) / `0.5155` (`higher`) and max `0.8621` with `203/579` rows above 10 % against a 106-character train median — so part of the reported ZH `0.8537` floor rests on how the corpus was harvested rather than on video content. **Cost `$0`: no GPU, SLURM, Modal, teacher call, model load, cache write, test-split contact or job submission; the C04 lineage was not touched and nothing in C02 was modified. Measurement provenance in full: the six gt census files; `Multihateclip/{English,Chinese}/annotation(new).json`, from which the per-dataset medians are **join-scoped to train+val ids** while the `Title`-presence counts were taken over **all rows** reading only `Title`/`Transcript` and consuming **no label** (join-scoped: `629/629`, `657/657`, and `277 / 0` for the `<em>`-in-`Title` corroboration); recomputation from the banked `C01_A0_OUT.json` and `C02_A0_{OUT,DECISION}.json`; and the six banked `headspace_arena_*_OUT.json`. **No label or prediction was read for any test id.**** Records: `refine-logs/GATE0_REOPEN_2026-07-31.md`, reviews `refine-logs/GATE0_REOPEN_2026-07-31_REVIEW_ROUND{1,...,14}.md` (raw appended to `TARGET_REVIEW_RAW.md`), state block `gate0_reopen_2026_07_31`; C09's Stage-0 preregistration exists only as `refine-logs/C09_A0_PREREG_DRAFT.md` — a DRAFT, not frozen, not hashed, not submitted, awaiting C04's tranche and main-dialogue authorization.

---

### F114 — the v6 teacher producer could never have produced a teacher response (2026-07-31)

`scripts/analysis/c04_a0t_small_v1_v6_producer.py:768`, the only prompt-render
call site in the frozen v6 tree, rendered with `str.format(transcript=...)`.
Both prompt templates embed `_SCHEMA_TEXT`, whose literal JSON
(`{"source_relation":…}`, `{"S":0,"P":0,"T":0,"H":0}`) `str.format` parses as
replacement fields. Static `ast` parse of the frozen bytes
(`81b10f58…`): **both forms raise `KeyError: '"source_relation"'`
unconditionally.**

No v6 self-test rendered a prompt, so 25/25 v6 checks passed with the defect
live, and the independent v6 payload reviewer quoted line 768 accurately without
executing it. The v6 job would have consumed the single-use resource ticket,
written the allocation entry marker, entered the no-clobber namespace, loaded
the 7B teacher onto an A100, and died on the first forward.

Repaired in v7 by `render_prompt()`, a literal single-placeholder substitution
with fail-closed guards. **No prompt byte changed** — the four frozen prompt
hashes still equal the v6 freeze, re-asserted by fixture, and the v7 selection
reproduces the v6 allowlists exactly.

Scope: this is an engineering defect in the v6 implementation only. It publishes
no metric, no reliability rate and no CONTINUE/KILL verdict, and it does not
touch the C04 scientific gates, which remain unwaived. Its lasting content is
methodological: **a static reviewer who reads a call site has not executed it**,
and this project has now hit "an irreversible resource consumed before the check
that rejects the run" three times in the implementation and six more times in
one review cycle.

### F114b — the failure family, stated as a reusable check (2026-07-31)

F114 is the third instance of one shape in this lineage: **an irreversible
resource consumed before the check that would reject the run.**

1. **v5 static gate** (job `13805`): the preflight asserted config↔computed
   prompt-hash equality *before* the freeze that materializes those hashes, so
   the run could never pass its own first gate — after an ~8-hour queue hold.
2. **v6 `mark_exit`**: a claim-time HALT bumped the genesis ledger's revision,
   permanently breaking the resource ticket's `genesis_gpu_ledger_sha256` pin
   inside a no-clobber namespace, turning a clean pre-claim refusal into a wedge.
3. **v6 render defect** (F114): the single-use ticket, the entry marker, the
   no-clobber namespace and 7B weights on an A100, all consumed before a
   `str.format` call that raises `KeyError` on the first forward.

The v7 review cycle found six more of the same shape across five rounds,
including a GPU wrapper that created the no-clobber namespace before any code
read an authorization flag, and a campaign ledger that bricked itself by writing
an over-cap row and then raising.

**The check that closes the family:** before any single-use ticket is consumed or
any no-clobber namespace is entered, a zero-cost dry execution must **exercise
the first real operation of the payload path** — the operation itself, on real
inputs, not a check that it exists and not a reading of its call site. The v7
template: render all 400 × 2 prompts before the model loads (measured 800/0);
round-trip every record type a later stage re-reads against that stage's actual
schema and key set; and execute every negative fixture, since a check that cannot
fail under any mutation of the thing it guards is not a check.

The corollary is the part that is easy to rationalise away, so it is recorded
explicitly: **a careful independent line-read is not evidence that code runs.**
F114 survived 25 passing self-tests *and* an accurate human reading of the exact
defective line. Only execution caught it.

Scope: methodological. This finding publishes no metric and touches no
scientific gate.

### F115 — the 13852 OOM, reconciled exactly (2026-08-01)

Job 13852 (C04 teacher small tranche) died with `torch.OutOfMemoryError: Tried
to allocate 110.50 GiB` inside `F.scaled_dot_product_attention` of the
Qwen2.5-VL vision blocks, on `MHC_zh/BV1vs4y127aA` — the first MHC_zh item,
after HateMM had completed 400/400 prompt records.

**Root cause:** the v7 producer calls `AutoProcessor.from_pretrained(snapshot,
local_files_only=True)` with **no `max_pixels`**, so frames enter the vision
tower at native resolution. The offending frames are 1080x1920 = 2,073,600
px/frame, 13.7x the project's deployed cap. The project's own extractors already
pin `max_pixels = 360*420 = 151200`.

**Arithmetic, measured on CPU through the real processor geometry path** (no
model, no CUDA): the item yields `video_grid_thw = (4, 138, 78)` =
**43,056 patch tokens** (first dim is 4, not 8, because `temporal_patch_size=2`
pairs the frames). Then `43,056^2 x 16 heads x 4 bytes = 110.50 GiB`, an **exact**
match to the reported figure.

An earlier closed-form estimate of ~13 GiB / 21,152 tokens is **RETRACTED**. It
was wrong on two independent counts: it applied the 2x2 spatial merge *before*
attention (vision SDPA runs on pre-merge patches) and assumed a bf16 score
tensor (SDPA materializes fp32). The "unreconciled 2.9x" recorded alongside it is
resolved and superseded by the measurement.

**At `max_pixels = 151200`,** measured over the 201 frame packs 13852 produced:
max 3,072 patch tokens, worst-case vision SDPA **0.56 GiB** on a 79.27 GiB card
— a ~197x reduction from the failing allocation.

Methodological content, and the reason this is a finding rather than a log line:
the v7 dry-run template exercised the text path on real transcripts (800
renderings, 0 halts) but never the **visual** path at real resolutions. The
guard class this adds to the failure family is an input-size bound measured at
zero cost before any GPU allocation. Scope: engineering. No metric, no
reliability rate and no CONTINUE/KILL verdict is published; C04's scientific
gates are untouched.

## F116 — v8: the pixel cap is protocol fidelity, and moving frame packs off the GPU is what makes the tranche fit at all (2026-08-01, 0 GPU-seconds for the finding itself)

Implementation v8 rebuilds the C04 teacher small tranche after F115. Four changes,
each flagged to two independent reviewers, both of whom returned `GO`
(code/resource `0C/0H/0I`; payload `0C/0H/5I`).

**The teacher-input change is toward the protocol, not away from it.** v8 threads
`max_pixels = 360*420 = 151200` into `AutoProcessor.from_pretrained`. This is not
a constant chosen to make the run fit: seven Qwen2.5-VL entrypoints already in
this repository default to `360*420`, and
`generate_VideoMLLM_embedding_bidir_textpool_HF.py:29` calls 151200 "the deployed
max_pixels". **v7's uncapped teacher was the anomaly** — an unreviewed library
default, not a reviewed choice, which is exactly why it survived five
code/resource rounds and a payload review. Measured over all 400 sealed items on
CPU: worst case 3,072 pre-merge patch tokens, vision SDPA **0.562 GiB** against
the 110.50 GiB that killed v7.

**The load-bearing result, and it was not the change anyone set out to make.**
The CPU preflight now materialises the 400 frame packs that v7 built inside its
GPU allocation. Measured: **273.0 s of decode + 561.7 s of PNG encode = 834.7 s**
of pure CPU work with the A100 idle. Against a usable window of 4022 s, the
conservative projection is 3817.6 s. Had that work stayed in the allocation it
would be **4652.3 s — the gate would have FAILED.** The pixel cap made the
tranche *possible*; moving the frame work off the GPU is what made it *fit*.

**Frame semantics provably did not move.** All 200 HateMM packs the v8 CPU
preflight wrote are **byte-identical** to the packs job 13852 wrote — the full
set, all 1600 PNGs, plus backend, frame total, requested-index vector and
decode-failure vector. The independent payload reviewer reproduced this. So a
stage relocation of this size can be verified rather than argued.

**A methodological gain worth keeping.** v7's pyav fallback held every decoded
frame in memory before selecting eight (measured: 2,879 frames at 1920x842 on
`MHC_zh/BV18N4y1B7qA`, of the order of 14 GiB resident, 45.8 s). The two-pass
replacement retains only the eight requested: byte-identical output, **6.6x
faster** (6.9 s). 11 of 400 items take that path.

**The projection gate is the generalisable artifact.** It measures every item's
real visual token count through the real processor on CPU — no model, CUDA
explicitly unavailable — and admits two states only: fits by measurement, or HALT
before the no-clobber namespace exists. The basis is deliberately wrong in the
safe direction (v7's *native*-resolution per-forward mean applied to all 800 v8
forwards), which the payload reviewer independently refit: at v8's actual
geometry the regression `elapsed = 1.9913 + 3.567e-4 x pre_merge_tokens` gives
3.0072 s/forward → 2683.5 s (33.3 % margin), and v7's 46 forwards on items where
native geometry *already equalled* capped geometry measured 2.9002 s → 2597.8 s
(35.4 %). Two independent estimators agreeing within 3 %, against a conservative
5.1 %.

**Two caveats that must travel with any verdict this tranche produces.**
(i) The seal requires all 800 records and resubmission is forbidden, so a budget
breach after a *complete* HateMM half still yields no seal and no verdict.
(ii) **MHC-ZH transcript poverty**: measured on the frozen selection, MHC-ZH p10 =
8 unicode scalars and **24/200 items carry fewer than 10**, against HateMM p10 =
38 / median 999. If this tranche returns `KILL_C04_TEACHER_SEMANTIC_RELIABILITY`
on MHC-ZH, that failure is **not separable from input poverty** at this sample
size. No redraw or replacement is permitted under the amendment.

Scope: engineering plus one live scientific caveat. No metric, no reliability
rate and no CONTINUE/KILL verdict is published here. C04's scientific gates are
untouched and unwaived.

## F117 — C04 teacher small tranche: KILL on semantic reliability, and the failure is the teacher, not the harness (2026-08-01, 2668 GPU-seconds)

The v8 tranche ran to completion: GPU job `13857` COMPLETED 0:0 in **2668 s**, all
800 prompt records, 400 canonical records, seal published, no breach. Terminal
state **`KILL_C04_TEACHER_SEMANTIC_RELIABILITY`** on both datasets, against the
five-rate taxonomy frozen since v6. Nothing was relaxed, invented or re-scored.

**8/8 slot cells fail; 2/2 joint gates fail.** Bars: usable >= 0.85,
conflict <= 0.20, missing <= 0.10, joint >= 0.60.

| | HateMM S / P / T / H | MHC-ZH S / P / T / H |
|---|---|---|
| usable | 0.590 / 0.390 / 0.570 / 0.575 | 0.440 / 0.405 / 0.465 / 0.445 |
| conflict | 0.330 / 0.595 / 0.410 / 0.375 | 0.445 / 0.475 / 0.490 / 0.500 |
| missing | 0.080 / 0.015 / 0.020 / 0.050 | 0.115 / 0.120 / 0.045 / 0.055 |
| parse rate | 0.6525 | 0.5425 |
| joint all-four usable | **0.2100** | **0.2100** |

The nearest miss is HateMM-S usable 0.590 against 0.85 — **0.26 absolute short**.
This is not a threshold quibble. (Slots: S = source_relation, P = proposition,
T = presenter_stance, H = protected_target;harm_act.)

**The independent recomputation agrees exactly.** A fresh reviewer with no
exposure to the build or to the two prior reviews re-implemented the parse and the
four-state slot logic from the frozen design rather than importing the project's
module: 800/800 raw strings re-parsed with **0 disagreements across 7,200
compared fields**, 1,600/1,600 slot states re-derived with 0 disagreements, all 20
rates and 32 state counts bit-for-bit, and 214 proposition cosines recomputed from
the frozen embedding matrix with max |delta| 1.7e-05 and 0 threshold flips.

**Three mechanisms, and the third is the wall.** (1) The teacher copies the
prompt's pipe-alternation *as the value* — `source_relation =
"current_presenter|quoted_or_embedded|performed_or_lyric|mixed|uncertain"` — in
134/400 HateMM and 51/400 MHC-ZH forms. (2) It drops the `confidence` map
entirely in 33 + 52 forms, invalidating all four slots at once. (3) **Where both
forms parse cleanly, they still disagree** at 0.351-0.638 (HateMM) and
0.380-0.453 (MHC-ZH).

**"The harness mis-parsed a fine teacher" is refuted, not argued away.** Three
diagnostic-only counterfactuals: a *lenient* parser recovers 106/800 forms and
lifts the parse rate to 0.71/0.675 but makes **joint worse** (0.21 -> 0.175 and
0.135), because recovered forms convert `single_valid` into `conflict`; dropping
the confidence gate entirely still tops out at usable 0.72-0.76 / joint 0.33-0.44;
and the **perfect-parser ceiling** — every form parsing — makes usable identically
equal to A/B agreement, i.e. **0.362-0.649, structurally below 0.85 in all eight
cells**. No harness fix reaches the bar.

**The MHC-ZH transcript-poverty confound is discharged, not merely noted.** The
payload review flagged that 24/200 MHC-ZH items carry fewer than 10 unicode
scalars (p10 = 8, median 63.5, vs HateMM p10 = 38 / median 999). A monotone length
gradient is real — and also present on HateMM, so it is not ZH-specific — but
every exclusion counterfactual still fails: dropping all 24 short items gives best
usable 0.483 / joint 0.216; keeping only >= median gives 0.570 / 0.240; keeping
only >= p75 (171 scalars, n=51) gives 0.667 / 0.255. **No stratum at any cut
reaches 0.85 on any slot or 0.60 joint.** The kill survives deleting every
poor-transcript item.

**Methodological datum: the projection gate was validated in the only way that
counts.** The pre-submit gate projected 3817.6 s on its deliberately conservative
basis and 2597.8 s on the capped-regime corroboration. Realized: **2668 s**, with
a per-forward mean of **3.0577 s** against the regression's 3.0072 s prediction
(1.7% off) and the corroborating 2.9002 s estimate (5% off). The conservative
basis over-predicted by 43%, exactly as it was designed to.

Budget: campaign accumulator now revision 2, **4646 s** (1978 + 2668) of the
7200 s FIRST_TRANCHE ceiling, leaving **2554 s** — because the accumulator records
actual sacct seconds, not the 5222 s reservation.

**Scope.** Pre-label survival screen on teacher semantic reliability only.
`labels_opened: false`; no label was read and no accuracy metric computed. It says
nothing about the C04 method — `FULL_Q4` and its controls are untested — nor about
whether a different teacher could pass. The amendment's non-waived full-bank gates
and the +0.030/+0.030 two-dataset target are untouched and unwaived. No full-bank
tranche is licensed: that needs PASS plus a fresh result-to-claim GO plus a new
code/resource review, none of which exist.

**If C04 continues**, each option is a *new* pre-registered tranche, never a
re-scoring of these 800 forwards (which `prompt_model_threshold_rewrite_after_failure:
false` and `no_retry_redraw_prompt_rewrite: true` forbid outright): (a)
constrained/grammar-forced decoding or per-field single-token classification,
worth roughly +0.13-0.18 usable — but it only touches mechanisms 1 and 2; (b) a
different or larger teacher, the only lever that touches mechanism 3; (c) dropping
two-form self-consistency, which removes the `conflict` state and the reliability
instrument together. Since mechanism 3 alone caps usable at 0.65 against a 0.85
bar, measure A/B agreement for any candidate teacher cheaply **before** funding (a).

- **Iteration 8 C09 A0 preregistration · 2026-08-01 · DESIGN GO (0C/0H/0I) AT ROUND 17, NOT FROZEN, NOT IMPLEMENTED, NOT SUBMITTED, no measurement, target unmet:** the C09 Stage-0 design was repaired from `REVISE (4C/8H/10I)` to **`GO (0 Critical / 0 High / 0 Important)`** across **seventeen** rounds of fresh independent review (`R1 4C/8H/10I → 1C/8H/10I → 1C/8H/11I → 1C/4H/9I → 0C/3H/10I → 0C/2H/10I → 0C/1H/4I → 0C/1H/3I → 0C/0H/3I → 0C/0H/2I → 0C/0H/2I → 0C/1H/2I → 0C/0H/5I → 0C/0H/3I → 0C/0H/5I → 0C/0H/3I → GO`), each reviewer fresh and unexposed to the repair reasoning. **Design of record: `refine-logs/C09_A0_V17_RECORD.md`; v1–v16 superseded in full.** Reviews: `refine-logs/C09_A0_PREREG_DRAFT_REVIEW_ROUND1.md` and `C09_A0_V{2..17}_PREREG_REVIEW.md`. **Cost `$0`** — zero GPU, SLURM, Modal, teacher call, model load, cache write, test contact or job submission. **What the rounds actually fixed, in order of consequence:** the round-1 identifiability probe was fed the scored item's own gold label (top-20 purity is defined against `i`'s gold label) and its discriminator had **no outcome for the realistic case**, so it could only return CONTINUE; the repaired probe is a **stratum-conditional INCREMENTAL** statistic `ΔAUC = AUC_strat(FULL) − AUC_strat(BASE)` with an item-level marginal permutation null **and** an exact within-`ITEM-STRATUM` conditional null, **both** required, on a per-scoring-fold analysis set whose threshold, class composition and fitting target all use the same out-of-fold `τ_hi^{(f)}`. `K-DEG` — the campaign's standing degeneracy guard — was carrying F96/F98's `0.95` constant on **selected-set overlap** when F96 and F98 measure **prediction-vector agreement over all `n`**; at `k/n ≈ 79/744` that demanded `pred_agree ≥ 0.9894` and would have passed an `S` three-quarters identical to a bare threshold shift. `NET`'s currency was re-bound to the C09 registry entry's own **`37.2 / 29.0`** with a `+0.050` macro-F1 leg. `F51`, `F66` and `F99` were added as headwinds **against** C09 — F99 caps the successor's set-preserving channel at `≤ +0.0279 / +0.0470`, and F51 reaches the *encoder-level* object C09's registry claim names though not the *encoder-frozen* successor §11 licenses, so **a CONTINUE would license an operator narrower than the registered claim**. **Load-bearing arithmetic established, derived rather than asserted:** transferring BOTH F88 ratios per-seed-denominated from `ERRPAT_HateMM §1/§1.1` (final rows `28/26/26`, consensus `25/2/1`) and `ERRPAT_MHC-ZH §2` (`23/24/22`, `22/0/3`) onto this arena's measured per-seed error means `253/3` and `187/3` gives **`|P_0| ≈ 79 / 60`, `n_unstable ≈ 9 / 8`, `|P_{τ_hi}| ≈ 40 / 30`**, closing `3|P_0| + 2a + b = 253 / 187` exactly — the long-carried `76 / 55` was mis-derived (the ZH figure applied a **union**-denominated rate to a **per-seed** base) and is retired; `K-NET` is **arithmetically unreachable** for `k ≥ 132` (HateMM) or `k ≥ 96` (MHC-ZH); and the `τ_hi` co-primary is **live but marginal on both legs on both datasets** (reach `0.0538 / 0.0518` against `+0.050`; the `p_w ≥ 30` power rule needs `≥ 75 %` of HateMM's positives and **all** of MHC-ZH's in two-class strata). **`§8` (nine HALT gates + three reporting instruments) and `§9` (the decision rule) are byte-identical across v8→v17 by hash (`md5 a17b56954ee6955013327f82a03904f7`), independently re-verified in rounds 15, 16 and 17.** **Pre-declared expectation: BAND B — identifiability real, conversion sub-bar — from F97's own `HONEST POSITIVE DATUM` (`+0.0269 / +0.0104 / +0.0182` for this exact feature family, raw arena, sub-bar on all three) and F98's banked `+0.0269` ceiling, both far below the `+0.050`-sized bar; a KILL is a complete result.** **NOT DONE and explicitly not claimed: the analysis script and the sbatch driver do not exist, no config is written, no sha256 is frozen, no C09 namespace exists (`artifacts/c09*`, `configs/c09`, `scripts/analysis/c09*`, `scripts/slurm/c09*` all verified absent), and NO metric, result, decision or verdict was produced. C09 is neither killed nor continued.** The single CPU submission the main-dialogue brief authorized is **UNSPENT**. State block: `c09_a0_design_freeze_readiness_2026_08_01`.

## F118 — C09 A0: KILL, and it is a memorisation measurement — the stable-inversion pool is arithmetically big enough to pay the bar and carries no label-free handle at all (2026-08-04, 52.07 CPU-hours, 0 GPU-h)

CPU job `13885` COMPLETED `0:0` in `2-04:04:18`. The frozen arena published
**`DECISION.verdict = KILL`** (`verdict_provisional` also `KILL`) with
`rules_fired = ["K-FELDMAN@tau_0", "K-REACH@tau_hi"]`. All nine §8.1 HALT gates and the
`SHUFFLE-POP` band passed on both datasets, so the run publishes a verdict rather than a
HALT; `GATE-LEDGER` measured `test_path_opens = 0` with 39 of 39 expected processes
reporting. Decision file:
`artifacts/c09_topo/v1/a0/C09-A0-v1/C09_A0_DECISION.json`.

**The KILL is over-determined — three independent routes, not one.** `K-FELDMAN` fails at
both `τ` on both datasets; `K-NET` clears in none of the six `(τ, k)` cells; `K-DEG` fires
at `τ_0` at all three multipliers. At `τ_hi` even `K-REACH` fails. Any one of these alone
kills.

**The crucial asymmetry, and it is the part worth keeping.** `K-REACH` **clears at `τ_0`
on both datasets**: flipping the OOF-stable high-confidence inversions is worth
**+0.0981 acc / +0.1021 mF1** on HateMM and **+0.0864 / +0.1010** on MHC-ZH, against a
`+0.050` bar — the pool is roughly twice big enough to pay the Stage-0 price. What is
absent is any way to *find* it without the labels. So this is **not a null result about a
small population; it is a memorisation measurement about a large one.**

**The candidate's own promoted discriminator returns the Feldman answer.** `D-FELDMAN`
asks whether stable inversions are incrementally predictable from label-free structure
over a baseline that already contains the definitional confidence term. Measured `ΔAUC =
AUC_strat(FULL) − AUC_strat(BASE)`:

| dataset | τ | FULL | BASE | `ΔAUC` | `PERM_STRUCT` p | `PERM_STRUCT_COND` p |
|---|---|---|---|---|---|---|
| HateMM | `τ_0` | 0.50640 | 0.53487 | **−0.02848** | 0.85614 | 0.83017 |
| HateMM | `τ_hi` | 0.52009 | 0.48932 | +0.03076 | 0.26474 | 0.16384 |
| MHC-ZH | `τ_0` | 0.59704 | 0.61347 | **−0.01643** | 0.51349 | 0.48152 |
| MHC-ZH | `τ_hi` | 0.56391 | 0.56545 | −0.00155 | 0.35864 | 0.29870 |

Nothing rejects at `α = 0.05` after Holm; the smallest p in the table is `0.164`, the
largest `0.856`. **At `τ_0` — the primary — `ΔAUC` is negative on both datasets.** The
structural block does not merely fail to add signal, it costs a little. The GBM capacity
arm (read by no rule) does not rescue it: `+0.0318 / +0.0207 / −0.0050 / −0.0205`.
Against the design's own §6.1 hypothesis table this is the **H-MEMORISATION** row on both
of its predictive columns simultaneously (`ΔAUC ≈ 0`; `NET ≈ 0 or negative`), and it is
the first time this campaign has *measured* the Feldman objection rather than argued
about it: **stable inversions behave as singletons, each wrong for its own reason.**

**Conversion is not merely sub-bar, it is negative in every cell.** `K-NET`'s bar is
`mean_s net_s ≥ 37.2` (HateMM) / `≥ 29.0` (MHC-ZH) with `ΔmF1 ≥ +0.050`. Measured
`mean_s net_s` at the three multipliers: HateMM `−8.33 / −27.33 / −53.33` (`τ_0`) and
`−4.00 / −11.33 / −16.00` (`τ_hi`); MHC-ZH `−10.00 / −15.67 / −28.67` and
`−5.33 / −14.33 / −21.33`. The `+0.030`-sized secondary (`22.3 / 17.4`) is cleared
nowhere either. The mechanism is a precision gap, priced per cell: the operation needs
`π*` of `0.63–1.06` and realises `0.29–0.44`. Four cells are `ARITHMETICALLY_DEAD_ON_NET`
rather than tested-and-failed, confirming the prereg's *"unreachable for `k ≥ 132 / 96`"*
at `k = 146 / 100`.

**And at `τ_0` the surgery is very nearly a threshold shift.** `K-DEG` fires because the
surgered prediction vector is **95.2–96.8 %** identical to a bare threshold twin's
(HateMM `0.9677 / 0.9570 / 0.9516`; MHC-ZH `0.9620 / 0.9551 / 0.9482`) against a `0.95`
kill line. This is where the code-review lineage paid: R3 H-1 found `K-DEG` reading only
the per-seed scale, and it is the **per-item** scale that supplies these maxima
(`0.9677` per-item vs `0.9418` per-seed-mean at HateMM `τ_0, k=73`). Under the pre-repair
reading `K-DEG` would have under-fired at exactly these cells.

**Null-row robustness: the verdict survives; the disagreement runs the KILL way.** The
pre-registered sensitivity leg re-runs the whole battery with HateMM row 355 (the
exact-zero vector in both streams) removed: `verdict_primary = KILL` and
`verdict_remove_null = KILL`. `GATE_NULL_AGREEMENT.agrees` is nonetheless `false`, on four
`τ_hi` cells where **`K-DEG` fires *more* without the null row** — the sensitivity leg is
strictly more hostile to C09 than the primary. **`kill_scope.scoped_to_null_row_disagreement`
carries boilerplate reading *"this KILL does NOT survive removal of the structural-zero
row"* — that sentence is FALSE for this run and must not be quoted.** It is emitted by
`c09_a0_arena.py:1615-1618` whenever the coarse agreement flag is false, without inspecting
what disagreed. The frozen executable is not edited; the erratum is recorded in
`refine-logs/C09_A0_RECORD.md §8.7`.

**Prediction scoring.** The prereg pre-declared **BAND B** (*identifiability real,
conversion sub-bar*). The measured outcome is **worse than BAND B**: identifiability is
not real either, at this power, in this feature family. Its transferred F88 arithmetic
also over-estimated the pools (`|P_0|` 79/60 predicted vs **73/50** measured; `|P_τhi|`
40/30 vs **36/26**), and the `τ_hi` co-primary it called *"live but marginal on both legs
on both datasets"* at `0.0538 / 0.0518` came in at **`0.0484 / 0.0449`** — below the bar,
and below it on the accuracy leg alone, since both macro-F1 legs clear (`+0.0505 /
+0.0530`). The banked floors reproduced exactly on every seed and fold.

**Scope, per §9/§10 unchanged.** The KILL closes the C09 Stage-0 oracle under the frozen
Stage-0 rule at `τ ∈ {τ_0, τ_hi}` only — `K-REACH` did **not** fire at `τ_0`, so the
"closes every `τ ≥ 0` by arithmetic" clause does not apply. `IDENTIFIABILITY_UNDERPOWERED`
is `true` on MHC-ZH at `τ_hi` (and false in the three other cells), so the `τ_hi`
identifiability leg reads *"not identifiable at this power"* on that dataset; the `τ_0`
leg, where `K-FELDMAN` fired, is underpowered on neither. It is **not** an impossibility
proof for encoder-level topology intervention: one feature set, one estimator family, one
stratification, one power regime. The raw fused-key leg is confined to KILL corroboration
(F113) and corroborates. No test-split artifact was opened.

**PROCESS FINDING, and it cost more than the science did: a compute projection that was
never multiplied through, plus a 51.6-hour blind spot.** The freeze record's claim 19
estimated **45–65 CPU-min** (mints 20–36 + arena 20–30), flagged `[U]`, and declared it
*"inert: no `--time` is set"*. Measured: the mints took **27.9 min** — inside the band —
and the arena took **51.61 h**. That is **103×–155×** over the arena estimate and
**48×–69×** over the whole-job estimate, and it held the campaign's serial-execution lock
for 2 days 4 hours. The mechanism is stated in claim 19 itself: the figure was
*"extrapolated from timed dry runs at reduced draw counts"*, and the realised counts
(`D_perm 1000`, `D_shuffle 200`, `D_random 200`, `B_boot 10 000` — per fold, per seed, per
`τ`, per space, over five spaces) were never re-multiplied through. The *measured*
resource prediction, peak RSS, was right to within 2 % (`1.22 GiB` against `< 1.2 GB`);
only the extrapolated one failed. Compounding it, the arena's five per-phase log lines were
block-buffered and flushed only at exit, so nothing appeared between the arena's start at
`08:42:10` on 2026-08-02 and the flush at `12:18:32` on 2026-08-04; the external watcher
could report only `state=RUNNING mints=36/36 decision=no`, unchanged. For 51.6 h a healthy
run was indistinguishable from a hang. **Standing rule adopted:** every A0/verdict freeze
must now carry a measured-unit-cost × explicit-count compute projection (the C04 v8
preflight pattern, extended to CPU jobs) and the executable must emit a line-buffered
per-phase heartbeat to a progress file. Recorded in `TARGET_STATE.json` under
`process_rule_compute_projection_and_heartbeat_2026_08_04`.

Cost: **52.07 h wall, CPU-only, 8 cores, ZERO GPU-hours, $0 cloud.** Records:
`refine-logs/C09_A0_RECORD.md §8` (post-run), design of record
`refine-logs/C09_A0_V17_RECORD.md`. State block: `c09_a0_verdict_2026_08_04`.

---

## F119 — C06 CLOSED: prompt-orbit displacement carries no per-item signal in matched head space — the $0 falsifier ran, and the valid instrument returned 0/92 (2026-08-05, 53m34s CPU, 0 GPU-h)

CPU job `13988` COMPLETED `0:0` in `00:53:34`, 8 cores, peak RSS `1.45 GB`, **zero GPU**.
The frozen arena published **`verdict = HALT`** with all twenty gate names on the face,
`GATE-LEDGER` measuring `test_path_opens = 0`, `processes_reporting = 74`,
`dev_path_opens = 70 = mints_executed 66 + expected_sha_dev_opens 4`,
`stale_attempt_files = 0` and `predicate_failures = []`. Verdict file:
`artifacts/c06_falsifier/C06_VERDICT.json`.

**The chain, in the order it actually happened.**

1. **Hypothesis.** The prompt-orbit *displacement* direction (`ro_ow − ro_std`, tangent/chord,
   first-order) carries per-item signal in the deployed fold-head space that the endpoints do
   not — the head-space analogue of C01's raw-space route.
2. **Valid instrument result — lineage R (in-domain head), the binding evidence.**
   `R` passed every per-lineage gate on **both** datasets and was tested.
   S4's Holm family is frozen at **92 hypotheses per dataset**; **46 were TESTED (R's) and
   `holm_reject = 0` on both datasets** — no hypothesis survives multiplicity correction.
   S5's permutation null agrees: all four displacement arms per dataset sit **below** their
   own shuffled-pair `p95`, `above_p95 = false` throughout, with one-sided raw `p` between
   `0.366` and `0.498`:

   | dataset | arm · metric | observed | null p95 | raw p |
   |---|---|---|---|---|
   | HateMM | `displacement` · acc | `0.889637` | `0.894235` | `0.4708` |
   | HateMM | `common_displacement` · acc | `0.890534` | `0.894123` | `0.3658` |
   | MHC-ZH | `displacement` · acc | `0.890616` | `0.895222` | `0.4981` |
   | MHC-ZH | `common_displacement` · acc | `0.891192` | `0.895941` | `0.4319` |

   The displacement arms do not merely fail to clear a bar — they land **below the centre of
   their own null**. This is consistent with C01's raw-space result and with the
   pre-registered expectation.
3. **Instrument-a disqualification, recorded as a measured fact, not as doubt.** Lineage `N`
   (the deployed L28-trained head applied to L24 inputs) was dropped on both datasets by
   `GATE-ORBITDISP` — **11 drop reasons, all of them `GATE-ORBITDISP`**: `ρ_head` reached
   `0.968691–0.991691` against HateMM's bar `0.968176`, and `0.978742–0.998484` against
   MHC-ZH's bar `0.977223`. At `ρ ≈ 0.998` the head-space keys collapse toward a single
   direction, so the arena is measuring a degenerate geometry rather than the hypothesis.
   The gate did its job: it refused to let a degenerate instrument report.
4. **Published verdict `HALT`, promoted to `CLOSE` by team-lead ruling** under the user's
   autonomy directive. **The rule gap, named honestly:** the frozen two-lineage decision rule
   had no *R-alone* lane. §5.6 rule 3 maps "not every lineage passed" to `HALT`, so the
   realized outcome — exactly one lineage valid, and that one returning a clean negative —
   fell through the rule as written. The N-drop was a *predicted* scenario (the design's own
   §3.3 flags Head-N as out-of-domain) and the rule still lacked a lane for it. The evidence
   is not ambiguous; the rule was incomplete. Recorded so the next prereg carries the lane.

**Scope — from the verdict's own `scope` block, not paraphrased.** The close covers C06's
**first-order (tangent/chord) prompt-orbit route in the fold-head arena at `ro_L24`**, on
HateMM (`-LoRA-curric`, `n = 743`) and MHC-ZH (`-LoRA`, `n = 579`), under the lineage that
passed its instrument gates. It **does not establish** anything about: `curvature`, a
`per-arm retrained head`, `C01's per-modality contrast`, `a different readout span`, `L28`,
`the test split`, or `directions off the primary's own Givens family`. The standing confound
is unchanged: `generate_VideoMLLM_embedding_readout_HF.py:73-89` — the `ow_` cell changes the
prompt kind **and** both readout spans, so no result here attributes an effect to the prompt
alone.

**Untested cell, banked and cheap if ever wanted.** The **28-layer displacement pair** was
never run. The caches exist (`ro_L28` banked), and the cell is **~1 hour of CPU** — the same
shape of job as this one. It is not queued and needs no GPU; it is recorded here so a future
round does not have to rediscover that it is affordable.

**Process note.** The falsifier's own arithmetic held end to end: §8 projected `3674.0 s`
and the run took `3214 s` wall, inside the projection. Two blocking defects found by the
single code-review round (`GATE-COMPLETENESS` missing 7 of 20 gate names; a 75th
ledger-writing process) were fixed pre-run; a third — the sbatch activating no conda
environment — surfaced only on the first real submission (job `13987`, dead in 1 s at
`ModuleNotFoundError: torch`, before any science executed) and cost one second, not one hour.
Freeze record: `refine-logs/C06_FALSIFIER_FREEZE.md`. Design of record:
`refine-logs/C06_FALSIFIER_PREREG_DRAFT_V15E2.md`. State block: `c06_verdict_2026_08_05`.

Cost: **53m34s wall, CPU-only, 8 cores, ZERO GPU-hours, $0 cloud.**
