# C03 Independent Design Review

**Date:** 2026-07-29 (Pacific/Auckland)  
**Final verdict:** `CONFIRM_KILL`  
**Decision label:** `KILL_C03_DESIGN_INFEASIBILITY`

The independent concept review first returned `REVISE — 3 Critical / 5 High /
4 Important`. Its binding issue was the absence of a two-dataset,
representation-matched Stage-0 asset. Existing F72/F92/F93/C01 assets respectively
measure mask-only, readout, incompatible transplant, or confounded endpoint changes;
none isolates native policy-conditioned MNTP under matched pooling/head/retrieval.

After the formal proposal, experiment plan, tracker and file-level asset audit were
written, the reviewer exact-matched all four submitted SHA256 values and returned:

`CONFIRM_KILL — 0 Critical / 0 High`

The reviewer confirmed the full chain:

1. the registry requires actual fold/deployed-head `+0.050` accuracy and macro-F1 on
   two datasets before teacher/GPU expenditure;
2. no existing HateMM+MHC-ZH bank represents the C03 training mechanism;
3. manufacturing that bank requires native training plus re-extraction and therefore
   violates the ordering rule;
4. old caches may strengthen a KILL but cannot PASS the missing policy-native axis.

The scope is narrow: this kills design executability under the current registry, not
the abstract native-MNTP hypothesis.

Two non-blocking documentation points were incorporated: the fixed policy prefix is
only generic policy conditioning, not an identified per-example relation mechanism;
and a future reopened design would still require a formal preregistration for
matched-compute and control schemas.

No reviewer or design task ran Python, opened a cache, touched test data, used a
teacher/GPU, submitted SLURM, or produced a performance result.
