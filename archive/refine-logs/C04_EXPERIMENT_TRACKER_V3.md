# C04 V3 Experiment Tracker

**Status:** `DESIGN_V3_FROZEN_PENDING_FRESH_REVIEW`  
**Execution authority:** none

| V2 review item | V3 disposition |
|---|---|
| H1 fallback controls | exact content/state producer, split-local fit, positive direction, small/full `+.020` plus CI |
| H2 failure taxonomy | semantic unreliability is scientific KILL; prompt rewrite/retry fork forbidden |
| H3 tuning/seeds | unique five-outer aggregation, seed-0 small/full OOF, every strong arm independently receives identical grid/tie rule |
| H4 lower order | dense 256x3598 SHA-Rademacher projection covers every coordinate/block; payload hash blocks execution |
| I1 zero frames | `N=0` becomes eight black frames plus failure flag; no retry |
| I2 full perturbations | every named perturbation requires `+.020` accuracy/mF1 and paired CI lower `>0` on both datasets |
| I3 resource overshoot | pre-submit ledger plus internal watchdog/forward checks and 120-second reserve; no `--time` |

User amendment, full-bank `+.050/+.050`, no dev/test teacher, serial resource
caps and all V2 negative authority remain unchanged. No implementation,
teacher/cache, Python/test, GPU, SLURM, test access, result or metric exists.

