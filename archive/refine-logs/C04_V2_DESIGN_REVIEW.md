# C04 V2 Fresh Independent Design Review

**Verdict:** `REVISE`  
**Severity:** `0 Critical / 4 High / 3 Important`  
**Execution authority:** none

The reviewer exact-matched the immutable anchor and all seven submitted V2
package hashes. C04 needs no further user-contract amendment. Accepted portions
include the matched-teacher amendment/proxy disposition, reliability states,
retained P4/concat controls, DIRECT/STUDENT arenas, novelty boundary, gold/native
split, three-seed skeleton, resource envelope and SLURM boundary.

Required V3 repairs:

1. Define producer semantics, split fitting, expected direction and small/full
   gates for all three fallback controls.
2. Treat validly produced low coverage/high conflict/semantic unreliability as a
   scientific KILL, not infrastructure HALT; forbid a prompt-rewrite fork.
3. Freeze the unique outer-fold hyperparameter aggregation, small/full OOF seed,
   and identical inner tuning/tie rules for controls.
4. Replace coordinate-sampling LOWER_ORDER compression with provably
   block-covered dense deterministic compression and freeze its algorithm/hash.
5. Define the `N=0` frame case.
6. Add numeric full-bank margins/CI for every perturbation.
7. Add an internal wall-clock hard stop that prevents GPU-hour overshoot without
   using SLURM `--time`.

No file was written by the reviewer. No implementation, teacher, computation,
GPU, SLURM or test action is authorized.

