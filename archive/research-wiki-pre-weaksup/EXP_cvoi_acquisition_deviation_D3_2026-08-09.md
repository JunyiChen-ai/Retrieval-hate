# CVoI deviation D3 — C6 preflight review v1 predates a code edit and is superseded

- Timestamp: `2026-08-09T05:12:00+12:00`
- Scope: C6 preflight/review provenance only.
- Registered **before** any C6 cost number exists. No candidate metric opened. Test contact count 0.
- Prereg and appendix bytes unchanged.

## 1. Defect

`artifacts/cvoi_acq/premetric-v2/c6-cost-v1/preflight_review_v1.json` (mtime
`2026-08-08 10:56:08 +1200`, sha256
`861508f4bed31dcdf64a28f2fb7f968aaa2aa30a9ee832f01cac226df4c7a1dd`) pins the four C6 sources as:

```text
cost_audit.py           e3ac9ba2bead1cc4e0c97353055a1e5986ab3d09f93152f4a089c519656c182c
cost_driver.py          b44cdbc45e5945b4798d51a17002ac144c5edf90ab4cf63c46d136cd11810acf
cost_overhead_driver.py 310225766a4194e4d6508877a61331f7694fd6f4d40918972ab1e213dac597d3
costs.py                78c1f7721fc7eecae97635c8457d44a8d7c84b979a3fb92fcc0f1fd60a602392
```

Three of the four no longer match the working tree. `cost_audit.py`, `cost_driver.py` and
`costs.py` all carry mtime `2026-08-08 10:58:06 +1200` — two minutes **after** the review was
written — with current digests:

```text
cost_audit.py           af41b4c07dd7b775c663dda9fbb5a0a98cad49223bc4161af062efdfaf57df46
cost_driver.py          8a8761178b0fd3409c55ac806f882f3b1baad04052f6fe8f36d4f93c9be0095c
cost_overhead_driver.py 310225766a4194e4d6508877a61331f7694fd6f4d40918972ab1e213dac597d3   (unchanged)
costs.py                715ce942c0b3f032e4247af6b3ac8aead415d39bd9a9dee47cd8febd81627c40
```

The retired foreground poller was launched at `10:58` against the edited files. Had it ever
acquired the GPU, it would have produced C6 timings from code that no review certifies, with
nothing in the artifacts recording the discrepancy. The drift was found by the D2 daemon's
pinned-source check, which halted with `HALT_C6_PINNED_SOURCE_DRIFT` rather than start a run.

`scripts/cvoi_acq/` is untracked in git, so the reviewed 10:56 bytes are **unrecoverable** and the
content of the 10:58 edit cannot be reconstructed or diffed.

## 2. Consequences

No C6 cost number exists: before this deviation `c6-cost-v1/` contained only
`preflight_review_v1.json`. Nothing is invalidated, and no downstream artifact, gate or number
depended on v1. C6 was and remains `PENDING`.

The one substantive residual risk is that the 10:58 edit is unreviewed. Against that, the
registered protocol constants were re-verified directly in the current bytes and are intact:
warm-ups 100 drawn from train only, five repetitions, binding repetitions 2--5 by median, batch
size 1, seed 20260814, phase order decode/preprocess/inference/postprocess,
`torch.cuda.synchronize()` plus CUDA events, and the coverage contract of 851 videos by 30 windows
by two action types. `cost_preflight.py` re-run on the current bytes reports zero errors and the
identical coverage block (25,530 OCR actions, 25,530 dense actions, 102,120 dense frames, 744 train
and 107 val videos).

## 3. Registered rule

- **R1.** `preflight_review_v1.json` is superseded. It is retained as evidence but may never be
  cited as the review of record for any C6 artifact.
- **R2.** The review of record is
  `artifacts/cvoi_acq/premetric-v2/c6-cost-v1/preflight_review_v2.json`
  (`e54d48c34ad1fa570d3e8d573ae72bafa088893ca0550b9a9c39951cb0736e32`), generated by the unmodified
  CPU-only `cost_preflight.py` against the current bytes.
- **R3.** The exact measured bytes are archived at
  `artifacts/cvoi_acq/premetric-v2/c6-cost-v1/pinned_sources_v2/` so that "which code produced
  these timings" is answerable after the fact:

  | archived copy | sha256 |
  |---|---|
  | `pinned_sources_v2/cost_audit.py` | `af41b4c07dd7b775c663dda9fbb5a0a98cad49223bc4161af062efdfaf57df46` |
  | `pinned_sources_v2/cost_driver.py` | `8a8761178b0fd3409c55ac806f882f3b1baad04052f6fe8f36d4f93c9be0095c` |
  | `pinned_sources_v2/cost_overhead_driver.py` | `310225766a4194e4d6508877a61331f7694fd6f4d40918972ab1e213dac597d3` |
  | `pinned_sources_v2/costs.py` | `715ce942c0b3f032e4247af6b3ac8aead415d39bd9a9dee47cd8febd81627c40` |

- **R4.** Any further edit to those four files before C6 passes voids the current review of record
  and requires a new `preflight_review_v*.json` plus a new deviation. The D2 daemon enforces this
  mechanically: it recomputes all four digests against `preflight_review_v2.json` and refuses to
  time anything on mismatch.
- **R5.** C6's outstanding independent code review (appendix §15 step 5) is performed against
  `preflight_review_v2.json` and the archived bytes, never against v1. C6 remains `PENDING` until
  that review, the cost registry, the independent audit and the D2 `EXCLUSIVE_OK` verdict all
  exist.

## 4. Patch

`scripts/cvoi_acq/run_c6_exclusive_daemon.sh` now verifies against v2
(new sha256 `0094111b98e254818c6c33a092f81929401c094b3ed5f445eaba386881bf802e`, superseding
`b8dfbb36fc0135954118476beca8eece529d432dc6c66118f70034443d8bc605` recorded in D2 §4). No pinned
module, protocol constant, endpoint, arm, budget, threshold or decision rule changes.

## 5. Standing recommendation

`scripts/cvoi_acq/` should be committed to git before C6 executes, so that a future source drift is
diffable rather than merely detectable. This is a process note, not a gate condition.
